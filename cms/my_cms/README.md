## README

Установка PHP на Apache включает несколько шагов. Вот краткое руководство:

1. **Установите Apache**:
   - На Linux:
     ```bash
     sudo apt-get update
     sudo apt-get install apache2
     ```
   - На Windows скачайте Apache с официального сайта и следуйте инструкциям.

2. **Установите PHP**:
   - На Linux:
     ```bash
     sudo apt-get install php libapache2-mod-php
     ```
   - На Windows скачайте PHP с [официального сайта PHP](https://www.php.net/manual/ru/install.php) и распакуйте в папку.

3. **Настройте Apache для работы с PHP**:
   - Откройте файл конфигурации Apache (`httpd.conf` или `apache2.conf`).
   - Добавьте или раскомментируйте строки:
     ```apache
     LoadModule php_module modules/libphp.so
     AddType application/x-httpd-php .php
     ```
   - Убедитесь, что путь к PHP указан правильно.

4. **Перезапустите Apache**:
   - На Linux:
     ```bash
     sudo systemctl restart apache2
     ```
   - На Windows перезапустите службу Apache через панель управления.

5. **Проверьте установку**:
   - Создайте файл `info.php` в корневой директории вашего веб-сервера:
     ```php
     <?php
     phpinfo();
     ?>
     ```
   - Откройте в браузере `http://localhost/info.php`.

Если у вас возникнут трудности на каком-то этапе, дайте знать, и я помогу разобраться! 😊

Для работы с API важно использовать модули PHP, которые помогут вам взаимодействовать с внешними сервисами, обрабатывать данные и выполнять запросы. Вот список основных модулей, которые идеально подойдут для работы с API:

### Основные модули для работы с API
1. **php-curl**: 
   - Используется для выполнения HTTP-запросов (GET, POST, PUT, DELETE и т.д.).
   - Поддерживает работу с заголовками, токенами авторизации и HTTPS.

2. **php-json**: 
   - Для кодирования и декодирования данных в формате JSON.
   - Это незаменимый модуль, так как большинство API используют JSON для обмена данными.

3. **php-mbstring**: 
   - Обеспечивает корректную работу с многобайтовыми строками (например, для обработки текста на разных языках).

4. **php-xml**: 
   - Для обработки данных в формате XML. Полезен, если API возвращает данные в этом формате.

5. **php-intl**: 
   - Для интернационализации и обработки данных, связанных с локалями (например, даты, числа и строки).

6. **php-opcache**: 
   - Оптимизирует выполнение скриптов PHP и ускоряет обработку запросов.

### Дополнительные инструменты
- Если вы работаете с авторизацией OAuth (например, с API таких сервисов, как Google, Facebook или Twitter), вы можете установить библиотеки вроде [oauth2-client](https://oauth2-client.thephpleague.com/) или использовать пакеты из Composer.

### Установка нужных модулей
Установить эти модули можно с помощью команды (на Linux/Ubuntu):
```bash
sudo apt-get install php-curl php-json php-xml php-mbstring php-intl
```

После установки обязательно перезапустите Apache:
```bash
sudo systemctl restart apache2
```

Если вы планируете работать с определённым API и не уверены, какие модули могут понадобиться, просто дайте знать, и я подскажу! 😊



### Установить MariaDB

 которая является форком MySQL и поддерживает большинство его функций.

### Решение:
**Установите MariaDB**:
Выполните команду:

```bash
sudo apt-get update
sudo apt-get install mariadb-server
```



Теперь давайте создадим базу данных, к которой вы сможете подключиться из файла `DB_connect.php`. Вот пошаговая инструкция:

---

### 1. **Откройте MariaDB**
   Войдите в консоль MariaDB с правами администратора:
   ```bash
   sudo mysql -u root -p
   ```
   Введите пароль, если он запрашивается.

---

### 2. **Создайте базу данных**
   Создайте новую базу данных для вашего проекта:
   ```sql
   CREATE DATABASE bryanskpivo CHARACTER SET cp1251 COLLATE cp1251_general_ci;
   ```
   Эта команда создаёт базу данных с нужной кодировкой для поддержки русского языка.

---

### 3. **Создайте пользователя**
   Создайте нового пользователя и задайте ему пароль:
   ```sql
   CREATE USER 'new_user'@'localhost' IDENTIFIED BY 'password';
   ```
   Замените `new_user` и `password` на ваши значения.

---

### 4. **Предоставьте доступ**
   Дайте пользователю права доступа к базе данных:
   ```sql
   GRANT ALL PRIVILEGES ON bryanskpivo.* TO 'new_user'@'localhost';
   ```
   Это разрешит пользователю полностью управлять базой данных.

---

### 5. **Примените изменения**
   Обновите права доступа:
   ```sql
   FLUSH PRIVILEGES;
   ```

---

### 6. **Тестирование подключения**
   Попробуйте подключиться к базе данных из консоли:
   ```bash
   mysql -u new_user -p bryanskpivo
   ```
   Если всё работает, вы увидите приглашение для работы с базой данных.

---

### 7. **Настройте DB_connect.php**
   Обновите файл `DB_connect.php`, чтобы подключиться к созданной базе данных:
   ```php
   <?php
   // Подключение к базе данных
   $host = 'localhost';
   $username = 'new_user'; // Ваш новый пользователь
   $password = 'password'; // Ваш пароль
   $database = 'bryanskpivo';

   // Создание соединения
   $link = new mysqli($host, $username, $password, $database);

   // Установка кодировки
   if (!$link->set_charset('cp1251')) {
       echo "Ошибка при установке кодировки: " . $link->error;
       exit();
   }

   // Проверка на ошибки соединения
   if ($link->connect_error) {
       echo 'Ошибка подключения к базе данных (' . $link->connect_errno . '): ' . $link->connect_error;
       exit();
   }

   echo "Соединение установлено успешно!";
   ?>
   ```

---

Теперь вы можете работать с базой данных из вашего PHP-кода! Если что-то пойдет не так, поделитесь ошибкой, и мы разберёмся вместе. 😊

Когда вы впервые устанавливаете MariaDB, пароль для пользователя `root` может быть настроен следующим образом:

### Возможные ситуации:
1. **Пароль не установлен**:
   - Некоторые дистрибутивы Linux (например, Ubuntu) могут настроить MariaDB так, что доступ к консоли `root` осуществляется без пароля, если вы используете `sudo`.
   - Попробуйте просто нажать **Enter**, если пароль не был задан при установке.

2. **Вы задали пароль во время установки**:
   - Если во время установки MariaDB вам предлагали ввести пароль для пользователя `root`, используйте этот пароль.

3. **Используется аутентификация по системному пользователю**:
   - В некоторых системах (особенно Ubuntu) пользователь `root` в MariaDB настроен на использование аутентификации через системный пользователь. В этом случае вам нужно запускать консоль MariaDB с помощью команды:
     ```bash
     sudo mysql
     ```

4. **Забыли пароль**:
   Если вы забыли пароль `root`, его можно сбросить. Вот шаги:
   - Остановите MariaDB:
     ```bash
     sudo systemctl stop mariadb
     ```
   - Запустите MariaDB в режиме пропуска проверки привилегий:
     ```bash
     sudo mysqld_safe --skip-grant-tables &
     ```
   - Подключитесь к базе без пароля:
     ```bash
     mysql -u root
     ```
   - Обновите пароль:
     ```sql
     ALTER USER 'root'@'localhost' IDENTIFIED BY 'new_password';
     FLUSH PRIVILEGES;
     ```
   - Перезапустите MariaDB:
     ```bash
     sudo systemctl restart mariadb
     ```

Попробуйте эти варианты, чтобы войти в консоль. Если возникнут сложности, расскажите, что именно происходит, и я помогу! 🚀

Отличная работа! Чтобы выйти из консоли MariaDB, просто выполните следующую команду:

```sql
exit
```

После ввода этой команды вы вернётесь в терминал вашей операционной системы. Если возникнут другие вопросы или вам понадобится дополнительная помощь, обращайтесь! 🚀

Похоже, вы пытаетесь выполнить команду `mysql -u new_user -p bryanskpivo` внутри консоли MariaDB. Это не сработает, так как вы уже находитесь в консоли MariaDB. Команду `mysql -u ...` нужно вводить из **терминала вашей системы**, а не из среды MariaDB.

Чтобы выйти из консоли MariaDB, просто выполните команду:

```sql
exit;
```

Или нажмите `Ctrl + D`. 

После этого вы вернётесь в терминал вашей системы и сможете вводить команды там. Попробуйте снова и сообщите, если что-то не получается! 🚀

Чтобы проверить работу файла `functions.php` вместе с обновлённым `DB_connect.php`, вы можете создать небольшой тестовый файл, который будет подключать оба этих файла и вызывать определённые функции. Вот пошаговое руководство:

---

### 1. **Создайте тестовый файл**
Назовите его, например, `test_functions.php`, и поместите его в ту же папку, где находятся `functions.php` и `DB_connect.php`.

Пример кода для тестового файла:
```php
<?php
// Включаем вывод ошибок для отладки
error_reporting(E_ALL);
ini_set('display_errors', 1);

// Подключаем DB_connect.php и functions.php
require_once("DB_connect.php");
require_once("functions.php");

// Проверяем работу функции get_Page()
$page = "main";
$PageInfo = get_Page($page);

echo "<h1>Тест функции get_Page</h1>";
if ($PageInfo) {
    echo "<p>Заголовок страницы: " . $PageInfo['page_title'] . "</p>";
    echo "<p>Описание страницы: " . $PageInfo['page_description'] . "</p>";
    echo "<p>Контент страницы: " . $PageInfo['page_content'] . "</p>";
} else {
    echo "<p>Страница не найдена в базе данных.</p>";
}

// Проверяем работу функции get_Articles()
echo "<h1>Тест функции get_Articles</h1>";
$articles = get_Articles();
if ($articles) {
    foreach ($articles as $article) {
        echo "<p>Название статьи: " . $article['article_title'] . "</p>";
    }
} else {
    echo "<p>Статьи не найдены.</p>";
}

// Протестируйте остальные функции аналогичным образом
?>
```

---

### 2. **Добавьте тестовые данные в базу данных**
Убедитесь, что в вашей базе данных уже есть данные для тестирования. Например:

- Таблица `pages` должна содержать запись с `page_name = 'main'`.
- Таблица `articles` должна содержать хотя бы одну статью.

Пример для добавления тестовой записи в таблицу `pages`:
```sql
INSERT INTO pages (page_name, page_title, page_description, page_content)
VALUES ('main', 'Главная страница', 'Описание главной страницы', 'Содержимое главной страницы');
```

---

### 3. **Откройте тестовый файл в браузере**
В браузере перейдите по адресу, например:  
`http://localhost/test_functions.php`

Если всё работает правильно:
- Вы увидите результат работы функций, например, данные страницы `main` или список статей.
- В случае ошибок или проблем появятся соответствующие сообщения.

---

### 4. **Отслеживайте ошибки**
Если что-то не работает:
- Проверьте, есть ли ошибки PHP, которые выводятся на экран.
- Посмотрите логи веб-сервера для дополнительной информации:
  - Для Apache:
    ```bash
    sudo tail -f /var/log/apache2/error.log
    ```
  - Для Nginx:
    ```bash
    sudo tail -f /var/log/nginx/error.log
    ```

---

Эти шаги помогут вам протестировать и отладить функции. Если вы столкнетесь с какими-либо проблемами, расскажите, что именно происходит, и я помогу разобраться! 😊



Давайте пошагово добавим тестовые данные в вашу базу данных MariaDB, чтобы функции в `functions.php` могли успешно работать.

---

### **1. Откройте консоль MariaDB**
Для начала войдите в консоль MariaDB от имени пользователя с доступом к базе данных. Например:
```bash
sudo mysql -u root -p
```
Введите пароль, который вы установили ранее.

new_password

---

### **2. Выберите нужную базу данных**
Переключитесь на базу данных, в которой вы хотите добавить тестовые данные. В вашем случае это `bryanskpivo`:
```sql
USE bryanskpivo;
```

---

### **3. Добавьте тестовые данные в таблицу `pages`**
Предположим, в вашей таблице `pages` есть такие столбцы: `id`, `page_name`, `page_title`, `page_description`, `page_content`. Добавим пример записи для главной страницы:
```sql
INSERT INTO pages (page_name, page_title, page_description, page_content)
VALUES ('main', 'Главная страница', 'Описание главной страницы', 'Это содержимое главной страницы.');
```

---

### **4. Добавьте тестовые данные в таблицу `articles`**
Предположим, в таблице `articles` есть такие столбцы: `id`, `article_title`, `article_date`, `article_content`. Добавим примерные записи:
```sql
INSERT INTO articles (article_title, article_date, article_content)
VALUES ('Статья 1', '2025-03-21', 'Это содержимое первой статьи.'),
       ('Статья 2', '2025-03-20', 'Это содержимое второй статьи.');
```

---

### **5. Добавьте тестовые данные в таблицу `products`**
Если у вас есть таблица `products` со столбцами `id`, `product_type`, `product_name`, `product_price`, вы можете добавить данные следующим образом:
```sql
INSERT INTO products (product_type, product_name, product_price)
VALUES ('Напитки', 'Пиво Брянское', 100),
       ('Напитки', 'Пиво Московское', 120);
```

---

### **6. Добавьте тестовые данные в таблицу `promo`**
Для таблицы `promo` со столбцами `id`, `promo_date`, `promo_description` добавьте записи:
```sql
INSERT INTO promo (promo_date, promo_description)
VALUES ('2025-03-21', 'Скидка 10% на пиво Брянское!'),
       ('2025-03-22', 'Купи 2, получи 1 бесплатно!');
```

---

### **7. Добавьте тестовые данные в таблицу `photos`**
Если в таблице `photos` есть столбцы `id`, `photo_url`, `photo_description`, вы можете добавить примерные данные:
```sql
INSERT INTO photos (photo_url, photo_description)
VALUES ('/images/photo1.jpg', 'Фотография 1'),
       ('/images/photo2.jpg', 'Фотография 2');
```

---

### **8. Добавьте тестовые данные в таблицу `contacts`**
Для таблицы `contacts` со столбцами `id`, `contact_name`, `contact_email`, `contact_phone` добавьте записи:
```sql
INSERT INTO contacts (contact_name, contact_email, contact_phone)
VALUES ('Иван Иванов', 'ivan@example.com', '+7-900-123-45-67'),
       ('Петр Петров', 'petr@example.com', '+7-900-765-43-21');
```

---

### **9. Проверьте добавленные данные**
После добавления выполните команду для проверки, что данные успешно добавились. Например, для таблицы `pages`:
```sql
SELECT * FROM pages;
```

---

### **10. Завершите работу**
Чтобы выйти из консоли MariaDB, выполните:
```sql
exit;
```

---

Теперь вы можете протестировать ваш файл `main.php` или `test_functions.php`. Если что-то пойдёт не так или понадобятся дополнительные данные, дайте знать! 😊



Ошибка `ERROR 1146 (42S02): Table 'bryanskpivo.pages' doesn't exist` указывает на то, что в базе данных `bryanskpivo` не существует таблицы `pages`. Перед добавлением данных необходимо создать таблицу. Вот пошаговое руководство для решения этой проблемы:

---

### **1. Создайте таблицу `pages`**
   Выполните команду SQL для создания таблицы с нужной структурой. Например:
   ```sql
   CREATE TABLE pages (
       id INT AUTO_INCREMENT PRIMARY KEY, -- Уникальный идентификатор
       page_name VARCHAR(255) NOT NULL,   -- Имя страницы
       page_title VARCHAR(255),           -- Заголовок страницы
       page_description TEXT,             -- Описание страницы
       page_content TEXT                  -- Содержимое страницы
   );
   ```
   Эта команда создаёт таблицу `pages` с нужными столбцами.

---

### **2. Повторите добавление данных**
   После успешного создания таблицы выполните команду для добавления данных:
   ```sql
   INSERT INTO pages (page_name, page_title, page_description, page_content)
   VALUES ('main', 'Главная страница', 'Описание главной страницы', 'Это содержимое главной страницы.');
   ```

---

### **3. Проверьте добавленные данные**
   Убедитесь, что данные добавились в таблицу:
   ```sql
   SELECT * FROM pages;
   ```

---

### **Пояснение полей**
- `id`: Уникальный идентификатор записи, который создаётся автоматически.
- `page_name`: Имя страницы, например, `main`.
- `page_title`: Заголовок страницы.
- `page_description`: Краткое описание страницы.
- `page_content`: Полный контент страницы.

Если у вас возникнут дополнительные вопросы или потребуется уточнение, дайте знать! 🚀



Отличная работа! Вы успешно создали таблицу `pages`, добавили в неё данные, а затем убедились, что всё работает, с помощью команды `SELECT`. Это важный шаг к тому, чтобы ваш `main.php` корректно выполнял запросы к базе данных.



> http://127.0.0.1/test_functions.php показываает Fatal error: Uncaught Error: Class "mysqli" not found in /var/www/html/DB_connect.php:9 Stack trace: #0 /var/www/html/test_functions.php(7): require_once() #1 {main} thrown in /var/www/html/DB_connect.php on line 9



Ошибка `Class "mysqli" not found` указывает на то, что расширение `mysqli` не установлено или не активировано на вашем сервере PHP. Это легко исправить! Вот что нужно сделать:

---

### **1. Проверьте, установлено ли расширение `mysqli`**
На Linux можно проверить, установлено ли расширение, с помощью команды:
```bash
php -m | grep mysqli
```
Если команда не выводит `mysqli`, значит расширение не установлено.

---

### **2. Установите или активируйте `mysqli`**
#### На Debian/Ubuntu:
Установите `php-mysqli`, используя команду:
```bash
sudo apt-get install php-mysqli
```

Если вы используете определённую версию PHP (например, PHP 8.1), укажите её явно:
```bash
sudo apt-get install php8.1-mysqli
```

#### На Windows:
- Откройте файл `php.ini` (обычно находится в папке с установленным PHP, например, `C:\php\`).
- Найдите строку:
  ```
  ;extension=mysqli
  ```
- Уберите символ `;` в начале строки, чтобы активировать расширение:
  ```
  extension=mysqli
  ```

- Сохраните файл `php.ini` и перезапустите сервер.

#### На macOS:
Если вы используете Homebrew для управления PHP, выполните команду:
```bash
brew reinstall php
```

---

### **3. Перезапустите веб-сервер**
После установки или активации `mysqli` перезапустите веб-сервер (например, Apache):
```bash
sudo systemctl restart apache2
```

---

### **4. Проверьте `phpinfo()`**
Создайте тестовый файл `info.php` в корневой папке вашего сервера:
```php
<?php
phpinfo();
?>
```
Откройте его в браузере (`http://127.0.0.1/info.php`) и убедитесь, что секция `mysqli` отображается. Это означает, что расширение успешно установлено и работает.

---

### **5. Повторите проверку**
Теперь снова откройте `http://127.0.0.1/test_functions.php`. Если всё настроено правильно, ошибка должна исчезнуть.

Если проблема всё равно сохраняется, дайте знать, что именно произошло, и я помогу разобраться дальше!Вам удалось успешно установить расширение `php8.2-mysql`, которое включает поддержку `mysqli`. Отличная работа! Теперь осталось несколько важных шагов для завершения настройки:

---

### **1. Перезапустите ваш веб-сервер**
После установки нового расширения перезапустите веб-сервер, чтобы изменения вступили в силу:
```bash
sudo systemctl restart apache2
```

---

### **2. Проверьте доступность `mysqli`**
Чтобы убедиться, что `mysqli` активен, создайте тестовый PHP-файл. Назовите его, например, `info.php`, в папке вашего веб-сервера (обычно `/var/www/html/`):
```php
<?php
phpinfo();
?>
```
Откройте этот файл в браузере:  
`http://127.0.0.1/info.php`

Ищите секцию `mysqli`. Если она есть, значит, расширение активно.

---

### **3. Проверьте ваш файл `test_functions.php`**
После перезапуска веб-сервера попробуйте снова открыть `http://127.0.0.1/test_functions.php`. Если проблема с `mysqli` была решена, тестовый файл должен работать корректно.

---

### **4. Удалите файл `info.php`**
Если вы использовали `info.php` для проверки, не забудьте удалить его из папки сервера после проверки:
```bash
sudo rm /var/www/html/info.php
```
Это важно для безопасности, чтобы никто извне не мог увидеть информацию о вашем сервере.

---

Если теперь файл `test_functions.php` работает, но возникают какие-то другие проблемы, напишите, и я помогу разобраться дальше! 🚀🚀



> на http://127.0.0.1/test_functions.php новая ошибка  Fatal error: Uncaught mysqli_sql_exception: Access denied for user 'root'@'localhost' in /var/www/html/DB_connect.php:9 Stack trace: #0 /var/www/html/DB_connect.php(9): mysqli->__construct() #1 /var/www/html/test_functions.php(7): require_once('...') #2 {main} thrown in /var/www/html/DB_connect.php on line 9

Эта ошибка означает, что пользователь `root` не может подключиться к базе данных с указанными в `DB_connect.php` данными. Причиной может быть неправильный пароль, отсутствие привилегий или другая настройка доступа. Вот что нужно проверить и исправить:

---

### **1. Проверьте настройки доступа для пользователя `root`**
В консоли MariaDB выполните следующие команды:
```bash
sudo mysql -u root -p
```

После входа выполните:
```sql
SELECT user, host FROM mysql.user;
```
Убедитесь, что пользователь `root` имеет доступ с хоста `localhost`. Вы должны увидеть запись вида:
```
| root | localhost |
```

---

### **2. Сбросьте пароль пользователя `root`**
Если вы подозреваете, что пароль указан неверно, сбросьте его:
```sql
ALTER USER 'root'@'localhost' IDENTIFIED BY 'new_password';
FLUSH PRIVILEGES;
```
Замените `new_password` на новый пароль, а затем используйте его в `DB_connect.php`.

---

### **3. Проверьте файл `DB_connect.php`**
Убедитесь, что вы используете правильные данные для подключения. Например:
```php
<?php
$host = 'localhost';
$username = 'root';
$password = 'new_password'; // Ваш новый пароль
$database = 'bryanskpivo';

$link = new mysqli($host, $username, $password, $database);

// Проверка соединения
if ($link->connect_error) {
    die('Ошибка подключения к базе данных (' . $link->connect_errno . '): ' . $link->connect_error);
}
?>
```
Проверьте, чтобы пароль совпадал с тем, который вы указали для пользователя `root`.

Ваш файл `DB_connect.php` выглядит корректным и включает основные элементы для подключения к базе данных через `mysqli`. Однако я могу предложить улучшить код, добавив больше проверок и обработчиков ошибок. Вот исправленный и улучшенный вариант:

---

### Исправленный файл `DB_connect.php`:
```php
<?php
// Подключение к базе данных
$host = 'localhost';
$username = 'root';
$password = 'new_password';
$database = 'bryanskpivo';

// Создание соединения
$link = new mysqli($host, $username, $password, $database);

// Установка кодировки на utf8mb4
if (!$link->set_charset('utf8mb4')) {
    echo "Ошибка при установке кодировки: " . $link->error;
    exit();
}

// Проверка на ошибки соединения
if ($link->connect_error) {
    echo 'Ошибка подключения к базе данных (' . $link->connect_errno . '): ' . $link->connect_error;
    exit();
}

echo "Соединение установлено успешно!";
?>
```

---

Улучшения:

1. **Использование `try-catch`:**
   - Исключения (`Exception`) добавляют больше контроля над обработкой ошибок.
   - Это особенно полезно для записи ошибок в лог-файлы, вместо их вывода на экран в рабочей среде.

2. **Обработка ошибок подключения:**
   - Проверка подключения с использованием `throw new Exception`, чтобы чётко выявлять причины сбоев.

3. **Обработка ошибок при установке кодировки:**
   - Кодировка теперь проверяется, и ошибка выводится только при наличии проблемы.

4. **Сообщение об успешном подключении:**
   - Добавлено сообщение для отладки (рекомендуется удалить в рабочей среде).

---

### Дополнительные рекомендации
1. **Использование `.env` файлов для хранения конфиденциальных данных**:
   - Вместо того чтобы хранить логин, пароль и имя базы данных в открытом виде, можно использовать `.env` файл:
     ```php
     $host = getenv('DB_HOST');
     $username = getenv('DB_USER');
     $password = getenv('DB_PASSWORD');
     $database = getenv('DB_NAME');
     ```
     Это помогает защитить данные.

2. **Добавление логирования ошибок:**
   - Вместо вывода ошибок пользователям стоит записывать их в лог-файлы:
     ```php
     error_log($e->getMessage(), 3, '/var/log/db_errors.log');
     ```

---

Если вы примените этот улучшенный код, он станет более стабильным и безопасным в использовании. Если потребуется дополнительная помощь, просто напишите! 😊

---

### **4. Проверьте права пользователя**
Убедитесь, что пользователь `root` имеет все необходимые привилегии для работы с базой данных `bryanskpivo`. Выполните:
```sql
GRANT ALL PRIVILEGES ON bryanskpivo.* TO 'root'@'localhost';
FLUSH PRIVILEGES;
```

---

### **5. Используйте другого пользователя (рекомендуется)**
Для повышения безопасности создайте отдельного пользователя, вместо использования `root`:
```sql
CREATE USER 'new_user'@'localhost' IDENTIFIED BY 'password';
GRANT ALL PRIVILEGES ON bryanskpivo.* TO 'new_user'@'localhost';
FLUSH PRIVILEGES;
```
После этого обновите файл `DB_connect.php`, указав имя `new_user` и его пароль.

---

### **6. Проверьте работу сервера MariaDB**
Если вы изменили настройки, перезапустите сервер:
```bash
sudo systemctl restart mariadb
```

---

После выполнения этих шагов ошибка должна исчезнуть. Если что-то всё ещё не работает, сообщите, что именно происходит, и я помогу дальше! 😊



Отлично, вы подтвердили, что пользователь `root` существует и привязан к `localhost`. Это значит, что проблема, скорее всего, связана с неправильным паролем или отсутствием необходимых привилегий у пользователя `root` для базы данных `bryanskpivo`.

Вот что нужно сделать для устранения проблемы:

---

### **1. Сбросьте пароль для пользователя `root`**
Если пароль для пользователя `root` был забыт или вы не уверены, выполните следующие команды в консоли MariaDB:
```sql
ALTER USER 'root'@'localhost' IDENTIFIED BY 'new_password';
FLUSH PRIVILEGES;
```
Замените `new_password` на новый пароль, который будете использовать в файле `DB_connect.php`.

---

### **2. Проверьте привилегии пользователя `root`**
Убедитесь, что пользователь `root` имеет полный доступ к базе данных `bryanskpivo`. Выполните:
```sql
GRANT ALL PRIVILEGES ON bryanskpivo.* TO 'root'@'localhost';
FLUSH PRIVILEGES;
```

---

### **3. Обновите файл `DB_connect.php`**
Убедитесь, что в файле `DB_connect.php` указаны правильные учетные данные:
```php
<?php
$host = 'localhost';
$username = 'root';
$password = 'new_password'; // Укажите пароль, который вы только что установили
$database = 'bryanskpivo';

$link = new mysqli($host, $username, $password, $database);

// Проверка соединения
if ($link->connect_error) {
    die('Ошибка подключения к базе данных (' . $link->connect_errno . '): ' . $link->connect_error);
}
?>
```

---

### **4. Перезапустите MariaDB и веб-сервер**
После внесения изменений перезапустите сервер MariaDB и Apache:
```bash
sudo systemctl restart mariadb
sudo systemctl restart apache2
```

---

### **5. Проверьте подключение**
Теперь откройте `http://127.0.0.1/test_functions.php` в браузере. Если пароль и привилегии указаны правильно, ошибка должна исчезнуть.

Если проблема сохраняется или возникает новая ошибка, напишите, и я помогу разобраться! 🚀



> теперь http://127.0.0.1/test_functions.php пишет - Соединение установлено успешно! Тест функции get_Page Заголовок страницы: ������� �������� Описание страницы: �������� ������� �������� Контент страницы: ��� ���������� ������� ��������. Тест функции get_Articles Fatal error: Uncaught mysqli_sql_exception: Table 'bryanskpivo.articles' doesn't exist in /var/www/html/functions.php:16 Stack trace: #0 /var/www/html/functions.php(16): mysqli->query() #1 /var/www/html/test_functions.php(25): get_Articles() #2 {main} thrown in /var/www/html/functions.php on line 16

  

Спасибо за подробное описание! Проблема делится на две части: символы отображаются некорректно в тесте функции `get_Page`, а функция `get_Articles` выдаёт ошибку из-за отсутствия таблицы `articles`.

---

### Часть 1: Неверное отображение текста (`������� ��������`)
Это связано с кодировкой базы данных или PHP. Давайте исправим это:

#### Шаги:
1. **Убедитесь, что кодировка базы данных настроена на UTF-8.**
   Выполните в консоли MariaDB:
   ```sql
   SHOW VARIABLES LIKE 'character_set_database';
   ```
   Если кодировка установлена не в `utf8` или `utf8mb4`, измените её:
   ```sql
   ALTER DATABASE bryanskpivo CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
   ```
   Для таблицы `pages`:
   ```sql
   ALTER TABLE pages CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
   ```

2. **Измените установку кодировки в файле `DB_connect.php`.**
   Добавьте следующую строку после соединения с базой данных:
   ```php
   $link->set_charset('utf8mb4');
   ```

3. **Перепроверьте данные.**
   В консоли MariaDB убедитесь, что данные в таблице `pages` отображаются корректно:
   ```sql
   SELECT * FROM pages;
   ```

Теперь текст должен корректно отображаться на странице.

---

### Часть 2: Ошибка `Table 'bryanskpivo.articles' doesn't exist`
Эта ошибка возникает, так как таблица `articles` отсутствует. Нужно её создать.

#### Шаги:
1. **Создайте таблицу `articles`.**
   Выполните следующую команду в MariaDB:
   ```sql
   CREATE TABLE articles (
       id INT AUTO_INCREMENT PRIMARY KEY,
       article_title VARCHAR(255) NOT NULL,
       article_date DATE NOT NULL,
       article_content TEXT
   );
   ```

2. **Добавьте тестовые данные в таблицу.**
   Например:
   ```sql
   INSERT INTO articles (article_title, article_date, article_content)
   VALUES 
   ('Статья 1', '2025-03-21', 'Содержимое первой статьи.'),
   ('Статья 2', '2025-03-20', 'Содержимое второй статьи.');
   ```

3. **Проверьте данные.**
   Убедитесь, что данные добавлены:
   
   ```sql
   SELECT * FROM articles;
   ```

---

### Повторная проверка
После выполнения всех изменений:
1. Перезагрузите страницу `http://127.0.0.1/test_functions.php`.
2. Если ошибки не пропали или возникают новые, предоставьте обновлённый вывод ошибки, и я помогу дальше! 😊

Всё получится! 🚀



Для исправления кодировки лучше использовать `utf8mb4`, которая является современной и поддерживает широкий набор символов, включая эмодзи. Замените настройку кодировки в файле `DB_connect.php`, как указано ниже:

---

### Обновлённый код:
```php
<?php
// Подключение к базе данных
$host = 'localhost';
$username = 'root';
$password = 'new_password';
$database = 'bryanskpivo';

// Создание соединения
$link = new mysqli($host, $username, $password, $database);

// Установка кодировки на utf8mb4
if (!$link->set_charset('utf8mb4')) {
    echo "Ошибка при установке кодировки: " . $link->error;
    exit();
}

// Проверка на ошибки соединения
if ($link->connect_error) {
    echo 'Ошибка подключения к базе данных (' . $link->connect_errno . '): ' . $link->connect_error;
    exit();
}

echo "Соединение установлено успешно!";
?>
```

---

### Почему `utf8mb4`?
- Она расширяет классический `utf8` и поддерживает больше символов, что полезно для работы с текстами, включая символы разных языков.
- Это стандартная кодировка для веб-приложений и баз данных в современном мире.

---

После внесения изменений перезапустите ваш веб-сервер и проверьте, исправилось ли отображение текста. Если всё в порядке, данные будут правильно выводиться без странных символов. 🚀



Ошибка `No database selected` возникает, потому что вы не выбрали базу данных перед выполнением команды `CREATE TABLE`. Не переживайте, это легко исправить.

---

### Шаги для исправления:

1. Сначала выберите базу данных `bryanskpivo`, чтобы MariaDB знала, куда создавать таблицу:
   ```sql
   USE bryanskpivo;
   ```

2. Затем повторите команду для создания таблицы `articles`:
   ```sql
   CREATE TABLE articles (
       id INT AUTO_INCREMENT PRIMARY KEY,
       article_title VARCHAR(255) NOT NULL,
       article_date DATE NOT NULL,
       article_content TEXT
   );
   ```

---

### Проверка:
После создания таблицы выполните команду, чтобы убедиться, что таблица появилась:
```sql
SHOW TABLES;
```
Вы должны увидеть таблицу `articles` в списке.

---

Если будут новые трудности или ошибки, дайте знать, я помогу разобраться! 😊



Вы на правильном пути! Функция `get_Page` работает и корректно возвращает данные. А вот функция `get_Articles` указывает, что в таблице `articles` пока отсутствуют записи. Решение простое — добавим тестовые данные в таблицу `articles`.

---

### Как добавить тестовые статьи
Выполните следующие шаги в консоли MariaDB:

1. **Убедитесь, что вы используете базу данных `bryanskpivo`:**
   ```sql
   USE bryanskpivo;
   ```

2. **Добавьте тестовые записи в таблицу `articles`:**
   ```sql
   INSERT INTO articles (article_title, article_date, article_content)
   VALUES 
   ('Статья 1', '2025-03-21', 'Содержимое первой статьи.'),
   ('Статья 2', '2025-03-20', 'Содержимое второй статьи.'),
   ('Статья 3', '2025-03-19', 'Содержимое третьей статьи.');
   ```

3. **Проверьте добавленные записи:**
   Убедитесь, что данные были добавлены:
   ```sql
   SELECT * FROM articles;
   ```

---

### Повторное тестирование
После добавления записей снова откройте `http://127.0.0.1/test_functions.php`. Теперь функция `get_Articles` должна вернуть тестовые статьи.

Если результат всё ещё не отображается корректно или возникают другие проблемы, расскажите, что именно происходит, и мы решим их вместе! 😊



Замечательно! Теперь и `get_Page`, и `get_Articles` функционируют должным образом, а тестовые данные успешно отображаются. Вы проделали отличную работу, устранив все проблемы!

На данном этапе ваш файл `test_functions.php` демонстрирует корректную работу функций. Если есть планы добавить новые функции, оптимизировать код или подключить другой функционал (например, формы для ввода данных), я с радостью помогу. Какие у вас следующие шаги? 😊





> `<?=$PageInfo['page_content']?>` объясни этот код



Этот код `<?=$PageInfo['page_content']?>` использует синтаксис PHP для короткого вывода переменной прямо внутри HTML. Разберем его подробнее:

---

### Расшифровка:

1. **`<?= ... ?>`**:
   - Это короткая форма записи `<?php echo ... ?>`. Она используется для вывода значения переменной или выражения непосредственно в HTML.
   - Эквивалентная запись: 
     ```php
     <?php echo $PageInfo['page_content']; ?>
     ```

2. **`$PageInfo`**:
   - Это ассоциативный массив, который, скорее всего, был заполнен данными из функции `get_Page`.
   - В данном случае массив содержит данные страницы, такие как заголовок, описание, контент и т. д.

3. **`['page_content']`**:
   - Это ключ массива `$PageInfo`. Он указывает на конкретное значение в массиве, которое соответствует контенту страницы.

4. **Результат вывода**:
   - Если, например, переменная `$PageInfo['page_content']` содержит текст `"Это содержимое главной страницы."`, этот текст будет выведен прямо в HTML-страницу.

---

### Пример в контексте:

Если в массиве `$PageInfo` есть следующие данные:
```php
$PageInfo = [
    'page_title' => 'Главная страница',
    'page_description' => 'Описание главной страницы',
    'page_content' => 'Это содержимое главной страницы.'
];
```

То в результате выполнения строки:
```php
<p><?=$PageInfo['page_content']?></p>
```

Будет сгенерировано:
```html
<p>Это содержимое главной страницы.</p>
```

---

### Примечание:
- Убедитесь, что массив `$PageInfo` заполнен корректно. Если ключ `['page_content']` не существует или содержит `null`, на странице может быть пустое место.
- Короткий синтаксис `<?= ... ?>` должен быть включён в настройках PHP (это стандартно для большинства современных версий PHP).

Если вам что-то нужно уточнить или хотите подробнее рассмотреть другой фрагмент кода, дайте знать! 😊



> где нахадится массив $PageInfo



Массив `$PageInfo` формируется в вашем коде через функцию `get_Page($link, $page)` в файле `functions.php`. Давайте разберёмся, как и где это происходит:

1. **Определение массива `$PageInfo`:**
   - Вызов функции `get_Page($link, $page)` в вашем коде возвращает ассоциативный массив, содержащий данные о странице. Этот массив и записывается в переменную `$PageInfo`.
   - Пример вызова из файла `main.php`:
     ```php
     $PageInfo = get_Page($link, $page);
     ```
     Здесь:
     - `$link` — соединение с базой данных.
     - `$page` — имя страницы, например, `'main'`.

2. **Что делает функция `get_Page`:**
   
   - Функция ищет в таблице `pages` запись, где поле `page_name` совпадает с указанным значением (`'main'`).
   - Если находится такая запись, то её данные возвращаются как ассоциативный массив.
   
   Пример структуры массива, который возвращает функция:
   ```php
   $PageInfo = [
       'page_name' => 'main',
       'page_title' => 'Главная страница',
       'page_description' => 'Описание главной страницы',
       'page_content' => 'Это содержимое главной страницы.',
       'page_keywords' => 'главная, описание'
   ];
   ```
   
3. **Где `$PageInfo` используется:**
   В файле `main.php` переменная `$PageInfo` используется для вставки данных о странице в HTML. Примеры:
   ```php
   <title><?=$PageInfo['page_title']?></title>
   <meta name="description" content="<?=$PageInfo['page_description']?>">
   <p><?=$PageInfo['page_content']?></p>
   ```

4. **Где формируется массив `$PageInfo`:**
   Он формируется в `main.php`, когда вызывается:
   ```php
   $PageInfo = get_Page($link, $page);
   ```

---

Если этот массив пустой или функция не возвращает данных, значит:
- Либо в таблице `pages` нет данных с заданным `page_name`.
- Либо есть ошибка в работе функции `get_Page`.

Чтобы проверить это, можно временно добавить отладочный вывод:
```php
print_r($PageInfo);
```

Если нужно больше деталей или есть вопросы, как именно формируется массив, расскажите, и мы разберёмся! 😊



> как именно формируется массив



Массив `$PageInfo` формируется в функции `get_Page`, которую вы используете в своём `functions.php`. Давайте разберём её работу пошагово, чтобы понять, как этот массив создаётся.

---

### Как работает `get_Page`:
1. **Принимает параметры**:
   - `$link` — это объект соединения с базой данных, созданный в `DB_connect.php`.
   - `$page` — это значение, которое идентифицирует страницу, например, `'main'`.

   ```php
   function get_Page($link, $page) {
   ```

2. **Готовит SQL-запрос**:
   - Запрос ищет запись в таблице `pages`, где поле `page_name` совпадает с переданным значением `$page`:
     ```sql
     SELECT * FROM `pages` WHERE `page_name` = ?;
     ```

   В коде:
   ```php
   $sql = "SELECT * FROM `pages` WHERE `page_name` = ?";
   ```

3. **Создаёт подготовленный запрос**:
   - Функция готовит SQL-запрос с использованием метода `$link->prepare($sql)`. Это предотвращает SQL-инъекции.

   В коде:
   ```php
   $stmt = $link->prepare($sql);
   ```

4. **Привязывает параметры**:
   - Значение переменной `$page` передаётся в SQL-запрос с использованием `bind_param`. Тип параметра указывается как `"s"` (строка).

   В коде:
   ```php
   $stmt->bind_param("s", $page);
   ```

5. **Выполняет запрос**:
   - Подготовленный запрос выполняется с помощью `$stmt->execute()`.

   В коде:
   ```php
   $stmt->execute();
   ```

6. **Получает результат**:
   - Результат запроса возвращается в виде объекта с помощью `$stmt->get_result()`.

   В коде:
   ```php
   $result = $stmt->get_result();
   ```

7. **Извлекает данные в массив**:
   - Результат извлекается как ассоциативный массив с помощью `$result->fetch_assoc()`. Каждый ключ массива соответствует названию столбца в таблице базы данных.

   В коде:
   ```php
   $PageData = $result->fetch_assoc();
   ```

8. **Возвращает массив**:
   - Полученный массив с данными страницы возвращается из функции.

   В коде:
   ```php
   return $PageData;
   ```

---

### Пример создания массива:
Предположим, в вашей таблице `pages` есть такая запись:
| id   | page_name | page_title       | page_description          | page_content                    | page_keywords     |
| ---- | --------- | ---------------- | ------------------------- | ------------------------------- | ----------------- |
| 1    | main      | Главная страница | Описание главной страницы | Это содержимое главной страницы | главная, описание |

Когда вы вызываете:
```php
$page = 'main';
$PageInfo = get_Page($link, $page);
```

Функция `get_Page` вернёт массив:
```php
$PageInfo = [
    'id' => 1,
    'page_name' => 'main',
    'page_title' => 'Главная страница',
    'page_description' => 'Описание главной страницы',
    'page_content' => 'Это содержимое главной страницы',
    'page_keywords' => 'главная, описание'
];
```

---

### Где используется массив `$PageInfo`:
После того, как массив сформирован, он используется для динамического отображения данных на странице. Например:
```php
<title><?=$PageInfo['page_title']?></title>
<p><?=$PageInfo['page_content']?></p>
```

Этот код подставит значения из массива `$PageInfo` в HTML.

---

Если у вас возникнут дополнительные вопросы о массиве или о работе функций, я с радостью помогу разобраться! 😊





Судя по вашему коду, основная структура корректна, однако есть несколько возможных причин, по которым данные `product_name` и `product_desc` не отображаются. Давайте разберём основные моменты и решим проблему.

---

### 1. **Ошибка в вызове функции `get_Products`**
Функция `get_Products` должна принимать два аргумента: `$link` (соединение с базой данных) и `$product_type` (тип продукта). Если в функции отсутствует корректная передача `$link`, запрос к базе данных не выполнится. 

Проверьте определение вашей функции `get_Products` в файле `functions.php`. Она должна быть написана примерно так:
```php
function get_Products($link, $product_type) {
    $sql = "SELECT * FROM `products` WHERE `product_type` = ?";
    $stmt = $link->prepare($sql);
    if (!$stmt) {
        die('Ошибка подготовки запроса: ' . $link->error);
    }
    $stmt->bind_param("s", $product_type);
    $stmt->execute();
    $result = $stmt->get_result();
    if (!$result) {
        die('Ошибка выполнения запроса: ' . $stmt->error);
    }
    $products = [];
    while ($row = $result->fetch_assoc()) {
        $products[] = $row;
    }
    return $products;
}
```

---

### 2. **Проблема с передачей аргумента `$link`**
Убедитесь, что при вызове функции `get_Products` вы передаёте объект `$link` (подключение к базе данных):
```php
$ProductInfo = get_Products($link, $product_type);
```

---

### 3. **Проблема с пустыми данными в `$ProductInfo`**
Добавьте отладочный вывод перед HTML-таблицей, чтобы убедиться, что переменная `$ProductInfo` содержит данные:
```php
<pre><?php print_r($ProductInfo); ?></pre>
```

Откройте страницу `beer.php` в браузере и убедитесь, что массив `$ProductInfo` содержит данные. Например:
```php
Array
(
    [0] => Array
        (
            [product_name] => Брянское пиво
            [product_desc] => Традиционное местное пиво.
        )
    [1] => Array
        (
            [product_name] => Московское пиво
            [product_desc] => Классическое пиво из Москвы.
        )
)
```

Если массив пустой, значит, данные из таблицы `products` не были загружены. Проверьте запрос вручную в консоли MariaDB:
```sql
SELECT * FROM products WHERE product_type = 'beer';
```

---

### 4. **Исправление цикла `foreach`**
Тег `</table>` в вашем коде закрывается внутри цикла `foreach`, что нарушает структуру HTML. Исправьте это следующим образом:
```php
<table>
    <tr>
        <th>Название</th>
        <th>Описание</th>
    </tr>
    <?php foreach ($ProductInfo as $product): ?>
    <tr>
        <td><?= htmlspecialchars($product['product_name'], ENT_QUOTES, 'UTF-8'); ?></td>
        <td><?= htmlspecialchars($product['product_desc'], ENT_QUOTES, 'UTF-8'); ?></td>
    </tr>
    <?php endforeach; ?>
</table>
```

---

### 5. **Кодировка**
Вы используете `<meta charset="Windows-1251">`. Убедитесь, что данные в базе данных хранятся в той же кодировке. Если это не так, измените кодировку страницы на `utf-8`:
```html
<meta charset="utf-8">
```
И убедитесь, что соединение с базой данных использует ту же кодировку:
```php
$link->set_charset('utf8mb4');
```

---

### Итог
После выполнения всех изменений убедитесь, что:
1. Таблица `products` содержит данные с `product_type = 'beer'`.
2. Функция `get_Products` возвращает массив данных.
3. Цикл `foreach` правильно закрывает таблицу HTML.

Если после этих шагов проблема сохраняется, поделитесь отладочным выводом (`print_r($ProductInfo)`), и я помогу разобраться дальше! 😊





Отлично, данные успешно загружаются в массив `$ProductInfo`. Это значит, что функция `get_Products` корректно работает, и данные из таблицы `products` извлекаются. Однако если данные всё ещё не отображаются в HTML, проблема, скорее всего, в структуре вашего `beer.php`.

Давайте исправим это окончательно:

---

### **1. Исправление цикла `foreach`**
Текущий цикл `foreach` имеет проблему: тег `</table>` закрывается внутри цикла, нарушая структуру HTML.

Исправьте таблицу следующим образом:
```php
<div class="content">
    <table>
        <tr>
            <th>Название</th>
            <th>Описание</th>
        </tr>
        <?php foreach ($ProductInfo as $product): ?>
        <tr>
            <td><?= htmlspecialchars($product['product_name'], ENT_QUOTES, 'UTF-8'); ?></td>
            <td><?= htmlspecialchars($product['product_desc'], ENT_QUOTES, 'UTF-8'); ?></td>
        </tr>
        <?php endforeach; ?>
    </table>
</div>
```

Теперь тег `</table>` закрывается после завершения цикла `foreach`.

---

### **2. Добавьте проверку данных**
Если данные отсутствуют, вы можете показать сообщение:
```php
<?php if (!empty($ProductInfo)): ?>
    <?php foreach ($ProductInfo as $product): ?>
    <tr>
        <td><?= htmlspecialchars($product['product_name'], ENT_QUOTES, 'UTF-8'); ?></td>
        <td><?= htmlspecialchars($product['product_desc'], ENT_QUOTES, 'UTF-8'); ?></td>
    </tr>
    <?php endforeach; ?>
<?php else: ?>
    <tr>
        <td colspan="2">Продукты не найдены.</td>
    </tr>
<?php endif; ?>
```

---

### **3. Убедитесь, что данные отображаются в браузере**
Если данные всё ещё не выводятся:
1. Убедитесь, что массив `$ProductInfo` передаётся корректно, а вывод через `print_r($ProductInfo)` показывается выше таблицы.
2. Убедитесь, что файл `beer.php` сохранён и перезагружен.

---

### **4. Кодировка**
Проверьте, что ваш сервер правильно интерпретирует данные. Используйте `utf-8` в `<meta charset>`:
```html
<meta charset="utf-8">
```

Также убедитесь, что соединение с базой данных установлено с этой же кодировкой:
```php
$link->set_charset('utf8mb4');
```

---

Результат

После выполнения этих изменений данные `product_name` и `product_desc` должны корректно отображаться в таблице на странице `beer.php`. Если всё ещё возникают сложности, расскажите подробнее, и я помогу разобраться дальше! 🚀



### Отображение ошибок файла PHP

Добавьте следующий код в начало вашего файла PHP, чтобы включить отображение ошибок для диагностики:

```php
<?php
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);
?>
```

Это поможет вам увидеть любые ошибки или предупреждения, возникающие в процессе выполнения скрипта. После добавления этого кода попробуйте снова выполнить ваш PHP-код. Если будут выявлены ошибки, я готов помочь разобраться с ними! 😊



> Соединение установлено успешно! Фатальная ошибка: неперехваченное исключение mysqli_sql_exception: таблица 'bryanskpivo.photos' не существует в /var/ www/html/functions.php:52 Трассировка стека: #0 /var/www/html/functions.php (52): mysqli-> запрос() #1 /var/www/html/gallery.php (12): добавлено get_Photo() # 2 {main} /var/www/html/functions.php в строке 52



Ошибка указывает на то, что таблица `photos` в вашей базе данных под названием `bryanskpivo` не существует. Вот что нужно проверить и исправить:

1. **Проверьте наличие таблицы `photos`**:
   - Откройте вашу базу данных (например, через phpMyAdmin или другой инструмент для работы с MySQL).
   - Убедитесь, что таблица `photos` существует. Если она отсутствует, ее необходимо создать.

2. **Создайте таблицу `photos` (если ее нет)**:
   Если таблица отсутствует, используйте следующий SQL-запрос для ее создания (это пример, подстройте его под свои требования):
   
   ```sql
   CREATE TABLE photos (
       id INT AUTO_INCREMENT PRIMARY KEY,
       photo_name VARCHAR(255) NOT NULL,
       photo_caption VARCHAR(255) NOT NULL
   );
   ```
   
3. **Проверьте запрос в `functions.php`**:
   Убедитесь, что запрос, связанный с `get_Photo()`, корректен. Например:
   
   ```php
   function get_Photo() {
       global $mysqli; // Подключение к базе данных
       $query = "SELECT photo_name, photo_caption FROM photos";
       $result = $mysqli->query($query);
   
       if (!$result) {
           throw new mysqli_sql_exception("Ошибка выполнения запроса: " . $mysqli->error);
       }
       return $result->fetch_all(MYSQLI_ASSOC);
   }
   ```
   
4. **Убедитесь, что база данных и таблица настроены правильно**:
   - Проверьте правильность имени базы данных в вашем `DB_connect.php` (например, `bryanskpivo`).
   - Убедитесь, что у пользователя MySQL, которого вы используете, есть права доступа к этой базе.

5. **Вывод ошибок**:
   Если проблема сохраняется, оставьте включенным вывод ошибок, чтобы видеть подробности.

После внесения изменений протестируйте код. Если ошибки сохраняются или нужна помощь с SQL-запросом, дайте знать! 😊



### Создайте таблицу `photos`

Если таблица отсутствует, используйте следующий SQL-запрос для ее создания (это пример, подстройте его под свои требования): 

#### 1. Войдите в MariaDB

```sql
sudo mysql -u root -p
```

#### 2. Выберите базу данных с которой хотите работать. 

Используйте команду `USE` перед созданием таблицы:

```sql
USE bryanskpivo;
```

#### 3. Создайте таблицу `CREATE TABLE`:

```sql
CREATE TABLE photos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    photo_name VARCHAR(255) NOT NULL,
    photo_caption VARCHAR(255) NOT NULL
);
```

Теперь таблица будет создана в указанной базе данных (`bryanskpivo`).

#### 4. Добавьте данные в таблицу

Используйте SQL-запрос для вставки записей:

```sql
INSERT INTO photos (photo_name, photo_caption) 
VALUES 
('image1.jpg', 'Описание первой фотографии'),
('image2.jpg', 'Описание второй фотографии'),
('image3.jpg', 'Описание третьей фотографии');
```

#### 5. Проверьте наличие добавленных данных

После вставки выполните:

```sql
SELECT * FROM photos;
```

Это должно отобразить записи, которые вы только что добавили.



```
Предупреждение: попытка получить доступ к смещению массива со значением типа null в /var/www/html/gallery.php в строке 33
Предупреждение: неопределённая переменная $photo в /var/www/html/gallery.php в строке 41
Предупреждение: попытка получить доступ к смещению массива со значением типа null в /var/www/html/gallery.php в строке 41
```

Ошибки, которые вы видите, связаны с тем, что переменные, которые вы пытаетесь использовать, не имеют значений (то есть равны `null`). Вот возможные причины и пути их устранения:

1. **Ошибка в массиве `$PhotoInfo`**:
   - Проверьте, что функция `get_Photo()` возвращает массив с данными. Если она возвращает `null`, то цикл `foreach` в строке 33 пытается обойти несуществующий массив.
   - Вы можете добавить проверку перед использованием `$PhotoInfo`:
     ```php
     <?php if (!empty($PhotoInfo)): ?>
         <? foreach ($PhotoInfo as $photo): ?>
             <tr>
                 <td><img src='<?=$photo['photo_name'];?>'></td>
             </tr>
             <tr>
                 <td><?=$photo['photo_caption'];?></td>
             </tr>
         <? endforeach; ?>
     <?php else: ?>
         <p>Фотографии не найдены.</p>
     <?php endif; ?>
     ```

2. **Неопределённая переменная `$photo` (строка 41)**:
   - Убедитесь, что переменная `$PhotoInfo` содержит данные, а массив `$photo` заполняется корректно на каждой итерации цикла `foreach`.
   - Если `$PhotoInfo` пустой, переменная `$photo` будет неопределённой.

3. **Диагностика функции `get_Photo()`**:
   Проверьте, что запрос в функции `get_Photo()` возвращает данные. Например, временно добавьте отладочный код:
   
   ```php
   var_dump($PhotoInfo);
   exit;
   ```
   Это позволит увидеть, что именно возвращает `get_Photo()`.
   
4. **Защита от ошибок**:
   Добавьте проверки в самом коде:
   
   ```php
   if (isset($photo['photo_name']) && isset($photo['photo_caption'])) {
       echo "<img src='{$photo['photo_name']}'>";
       echo "<p>{$photo['photo_caption']}</p>";
   } else {
       echo "<p>Ошибка в данных фотографии.</p>";
   }
   ```

Примените эти рекомендации и повторно проверьте ваш код. Если функция всё ещё возвращает пустые значения или ошибки, напишите, и я помогу разобраться! 😊

Пустой результат (`Empty set`) указывает, что в таблице `photos` отсутствуют данные. Чтобы ваш код начал отображать фотографии, нужно добавить записи в таблицу. Вот пример, как это сделать:

1. **Добавьте данные в таблицу**:
   Используйте SQL-запрос для вставки записей:
   
   ```sql
   INSERT INTO photos (photo_name, photo_caption) 
   VALUES 
   ('image1.jpg', 'Описание первой фотографии'),
   ('image2.jpg', 'Описание второй фотографии'),
   ('image3.jpg', 'Описание третьей фотографии');
   ```
   
2. **Проверьте наличие добавленных данных**:
   После вставки выполните:
   ```sql
   SELECT * FROM photos;
   ```
   Это должно отобразить записи, которые вы только что добавили.

3. **Убедитесь в правильности путей к изображениям**:
   Убедитесь, что файлы `image1.jpg`, `image2.jpg`, и `image3.jpg` находятся в правильной директории на сервере, чтобы они отображались на странице. Например, если изображения находятся в папке `/var/www/html/images/`, проверьте, чтобы путь `src` в вашем коде соответствовал реальному расположению файлов.

После добавления данных и проверки их наличия страница должна корректно отображать фотографии. Если что-то всё ещё не работает, сообщите, и я помогу разобраться дальше! 😊

Да, совершенно верно! Использование `var_dump($PhotoInfo);` в сочетании с `exit;` — это полезный способ диагностики. Этот подход позволит вам точно понять, что функция `get_Photo()` возвращает.

Вот как это выглядит в контексте вашего кода:

```php
<?php
require_once("DB_connect.php");
require_once("functions.php");

// Диагностика функции
$page = "gallery";
$PhotoInfo = get_Photo();
var_dump($PhotoInfo);
exit;
?>
```

Если возвращается `null` или пустой массив, значит, проблема в запросе к базе данных. Убедитесь, что:

1. В таблице `photos` действительно есть данные.
2. SQL-запрос в функции `get_Photo()` правильно выбирает необходимые поля и соответствует структуре таблицы.

Если все данные присутствуют в базе, а результат все еще пустой, попробуйте включить вывод ошибок MySQL в функции `get_Photo()` для лучшей диагностики:

```php
function get_Photo() {
    global $mysqli;
    $query = "SELECT photo_name, photo_caption FROM photos";
    $result = $mysqli->query($query);

    if (!$result) {
        throw new mysqli_sql_exception("Ошибка выполнения запроса: " . $mysqli->error);
    }
    return $result->fetch_all(MYSQLI_ASSOC);
}
```

Сообщите, если результат анализа вызова функции дает дополнительную информацию. Если потребуется дальнейшая помощь, я готов помочь! 😊



Проблемы, которые вы описали, связаны с тем, что массивы или переменные, использованные в вашем коде, содержат `null`. Это может произойти из-за неправильной обработки данных или пустых результатов функций. Вот улучшенный подход к вашему коду, чтобы предотвратить эти предупреждения:

### Исправленный код Галереи
```php
<?php
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);
?>

<?php
require_once("DB_connect.php");
require_once("functions.php");
$page = "gallery";
$PageInfo = get_Page($page);
$PhotoInfo = get_Photo();
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
<title><?=$PageInfo['page_title'];?></title>
<meta charset="utf-8">
<meta name="description"
content="<?=$PageInfo['page_description'];?>">
<meta name="keywords"
content="<?=$PageInfo['page_keywords'];?>">
<link rel="stylesheet" href="style.css">
</head>
<body>
<div class="header">
<?php include(__DIR__ . "/header.inc"); ?>
</div>
<div class="nav">
<?php include(__DIR__ . "/nav.inc"); ?>
</div>
<div class="content">

<!-- добавил отладочный вывод перед HTML-таблицей, чтобы убедиться,
что переменная $PhotoInfo содержит данные -->
<pre><?php print_r($PhotoInfo); ?></pre>

<h1><?= htmlspecialchars($PageInfo['page_header'] ?? 'Галерея'); ?></h1>

<table align="center">
    <?php if (!empty($PhotoInfo)): ?>
        <?php foreach ($PhotoInfo as $photo): ?>
            <tr>
                <td>
                    <?php if (!empty($photo['photo_name'])): ?>
                        <img src="<?= htmlspecialchars($photo['photo_name']); ?>" alt="Фото">
                    <?php else: ?>
                        <p>Изображение отсутствует.</p>
                    <?php endif; ?>
                </td>
            </tr>
            <tr>
                <td><?= htmlspecialchars($photo['photo_caption'] ?? 'Без подписи'); ?></td>
            </tr>
        <?php endforeach; ?>
    <?php else: ?>
        <tr>
            <td>Фотографии отсутствуют.</td>
        </tr>
    <?php endif; ?>
</table>

</div>
</div>
<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>
</body>
</html>

```

Что сделано:

1. **Проверка `$PageInfo` и `$PhotoInfo`**:
   - Если `$PageInfo` или `$PhotoInfo` пусты, выводятся соответствующие сообщения вместо вызова ошибок.

2. **Безопасный вывод данных**:
   - Добавлена функция `htmlspecialchars()` для экранирования данных перед выводом на страницу, чтобы избежать XSS-атак.

3. **Проверка переменных внутри цикла**:
   - Перед выводом данных о каждой фотографии проверяется наличие значений в ключах массива `$photo`.

4. **Сообщения об отсутствии данных**:
   - Если фотографии отсутствуют, выводится текстовое сообщение "Фотографии не найдены".

5. **Обработка пустых значений**:
   - Если отдельные поля массива `$photo` отсутствуют, выводятся текстовые заглушки ("Изображение отсутствует" и "Без подписи").

Протестируйте этот код. Если проблемы остаются, возможно, нужно проверить содержимое функций `get_Page()` и `get_Photo()` для точности их выполнения. Если потребуется помощь с этим, напишите! 😊



### Добавим отладочный код вывода ошибок для news.php

```php
<!-- добавил отладочный вывод ошибок -->
<?php
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);
?>
<!-- *** -->
<?php
require_once("DB_connect.php");
require_once("functions.php");
$page = "news";
$PageInfo = get_Page($page);
$ArticleInfo = get_Articles();
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
<title><?=$PageInfo['page_title'];?></title>
<meta name="description"
content="<?=$PageInfo['page_description'];?>">
<meta name="keywords"
content="<?=$PageInfo['page_keywords'];?>">
<link rel="stylesheet" href="style.css">
</head>
<body>
<div class="header">
<?php include(__DIR__ . "/header.inc"); ?>
</div>
<div class="nav">
<?php include(__DIR__ . "/nav.inc"); ?>
</div>
<div class="content">

<!-- добавил отладочный вывод, чтобы убедиться,
что переменная $ArticleInfo содержит данные -->
<pre><?php print_r($ArticleInfo); ?></pre>
<!-- *** -->
<h1><?=$PageInfo['page_header']; ?></h1>

<div class="ArticleList">
<table>
<? foreach($ArticleInfo as $article):?>
<tr>
<td><?=$article['article_title'];?></td>
<td><?=$article['article_date'];?></td>
</tr>
<tr>
<td
colspan="2"><?=$article['article_desc'];?></td>
</tr>
<tr>
</tr>
<?endforeach;?>
</table>
</div>
</div>
<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>
</body>
</html>
```

теперь в браузере пишет:

```
Array
(
    [0] => Array
        (
            [id] => 1
            [article_title] => Статья 1
            [article_date] => 2025-03-21
            [article_content] => Содержимое первой статьи.
        )
    [1] => Array
        (
            [id] => 2
            [article_title] => Статья 2
            [article_date] => 2025-03-20
            [article_content] => Содержимое второй статьи.
        )
    [2] => Array
        (
            [id] => 3
            [article_title] => Статья 3
            [article_date] => 2025-03-19
            [article_content] => Содержимое третьей статьи.
        )
)
Предупреждение: Попытка получить доступ к смещению массива по значению типа null в /var/www/html/news.php в строке 33
Предупреждение: Неопределенная переменная $article в /var/www/html/news.php в строке 38
Предупреждение: Попытка получить доступ к смещению массива по значению типа null в /var/www/html/news.php в строке 38
Предупреждение: Неопределенная переменная $article в /var/www/html/news.php в строке 39
Предупреждение: Попытка получить доступ к смещению массива по значению типа null в /var/www/html/news.php в строке 39
Предупреждение: Неопределенная переменная $article в /var/www/html/news.php в строке 43
Предупреждение: Попытка получить доступ к смещению массива по значению типа null в /var/www/html/news.php в строке 43
```

Ваш код почти работает правильно, и видно, что массив `$ArticleInfo` действительно содержит данные (как показано в `print_r`). Однако предупреждения показывают, что вы пытаетесь обратиться к несуществующим элементам массива или массиву `null`. Давайте исправим это.

---

#### Ошибки и решения
1. **Попытка доступа к `article_desc`:**
   В массиве `$ArticleInfo` элемент `article_desc` не существует. Судя по вашему отладочному выводу, правильный ключ должен быть `article_content`. Замените:
   ```php
   <?=$article['article_desc'];?>
   ```
   на:
   ```php
   <?=$article['article_content'];?>
   ```

---

2. **Проверка на существование данных перед доступом:**
   Чтобы избежать ошибок, добавьте проверку на существование ключей перед их использованием:
   ```php
   <td><?= htmlspecialchars($article['article_title'] ?? 'Без заголовка'); ?></td>
   <td><?= htmlspecialchars($article['article_date'] ?? 'Дата не указана'); ?></td>
   <td colspan="2"><?= htmlspecialchars($article['article_content'] ?? 'Содержание отсутствует'); ?></td>
   ```

---

3. **Обновленный цикл `foreach`:**
   Вот как можно переписать цикл для обработки массива:
   ```php
   <?php if (!empty($ArticleInfo)): ?>
       <?php foreach ($ArticleInfo as $article): ?>
           <tr>
               <td><?= htmlspecialchars($article['article_title'] ?? 'Без заголовка'); ?></td>
               <td><?= htmlspecialchars($article['article_date'] ?? 'Дата не указана'); ?></td>
           </tr>
           <tr>
               <td colspan="2"><?= htmlspecialchars($article['article_content'] ?? 'Содержание отсутствует'); ?></td>
           </tr>
       <?php endforeach; ?>
   <?php else: ?>
       <tr>
           <td colspan="2">Нет доступных статей.</td>
       </tr>
   <?php endif; ?>
   ```

---

4. **Исправление заголовков страницы:**
   Убедитесь, что `$PageInfo` содержит данные. Если они отсутствуют, отобразите заглушки:
   ```php
   <title><?= htmlspecialchars($PageInfo['page_title'] ?? 'Новости'); ?></title>
   <meta name="description" content="<?= htmlspecialchars($PageInfo['page_description'] ?? 'Описание страницы новостей отсутствует'); ?>">
   <meta name="keywords" content="<?= htmlspecialchars($PageInfo['page_keywords'] ?? 'новости'); ?>">
   ```

---

5. **Финальная версия:**
   Вот итоговый код с учётом всех исправлений:
   ```php
   <?php
   ini_set('display_errors', 1);
   ini_set('display_startup_errors', 1);
   error_reporting(E_ALL);
   
   require_once("DB_connect.php");
   require_once("functions.php");
   
   $page = "news";
   $PageInfo = get_Page($page);
   $ArticleInfo = get_Articles();
   ?>
   <!DOCTYPE html>
   <html lang="ru-RU">
   <head>
       <title><?= htmlspecialchars($PageInfo['page_title'] ?? 'Новости'); ?></title>
       <meta name="description" content="<?= htmlspecialchars($PageInfo['page_description'] ?? 'Описание отсутствует'); ?>">
       <meta name="keywords" content="<?= htmlspecialchars($PageInfo['page_keywords'] ?? 'новости'); ?>">
       <link rel="stylesheet" href="style.css">
   </head>
   <body>
       <div class="header">
           <?php include(__DIR__ . "/header.inc"); ?>
       </div>
       <div class="nav">
           <?php include(__DIR__ . "/nav.inc"); ?>
       </div>
       <div class="content">
           <pre><?php print_r($ArticleInfo); ?></pre>
           <h1><?= htmlspecialchars($PageInfo['page_header'] ?? 'Новости'); ?></h1>
           <div class="ArticleList">
               <table>
                   <?php if (!empty($ArticleInfo)): ?>
                       <?php foreach ($ArticleInfo as $article): ?>
                           <tr>
                               <td><?= htmlspecialchars($article['article_title'] ?? 'Без заголовка'); ?></td>
                               <td><?= htmlspecialchars($article['article_date'] ?? 'Дата не указана'); ?></td>
                           </tr>
                           <tr>
                               <td colspan="2"><?= htmlspecialchars($article['article_content'] ?? 'Содержание отсутствует'); ?></td>
                           </tr>
                       <?php endforeach; ?>
                   <?php else: ?>
                       <tr>
                           <td colspan="2">Нет доступных статей.</td>
                       </tr>
                   <?php endif; ?>
               </table>
           </div>
       </div>
       <div class="footer">
           <?php include(__DIR__ . "/footer.inc"); ?>
       </div>
   </body>
   </html>
   ```

---

Что исправлено

1. Заменён неверный ключ `article_desc` на `article_content`.
2. Добавлены проверки на существование данных с использованием `htmlspecialchars()` для безопасности.
3. Обработаны случаи, когда `$PageInfo` или `$ArticleInfo` пусты.

Протестируйте обновлённый код. Теперь он должен работать без предупреждений. Если проблемы всё ещё остаются, дайте знать! 😊



### Файл addnews.php для добавления новостей

Чтобы создать файл `addnews.php` для добавления новостей в вашу базу данных, вам понадобится форма для ввода данных и обработка этих данных с использованием PHP. Вот пример реализации:

#### Пример кода `addnews.php`:

```php
<?php
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);

require_once("DB_connect.php");

// Проверяем подключение
echo "<pre>";
var_dump($link);
echo "</pre>";

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    // Получение данных из формы
    $title = $_POST['title'] ?? '';
    $date = $_POST['date'] ?? '';
    $content = $_POST['content'] ?? '';

    // Проверка заполненности полей
    if (!empty($title) && !empty($date) && !empty($content)) {
        $query = "INSERT INTO articles (article_title, article_date, article_content) VALUES (?, ?, ?)";
        $stmt = $link->prepare($query);

        if ($stmt) {
            $stmt->bind_param("sss", $title, $date, $content);
            if ($stmt->execute()) {
                $message = "Новость успешно добавлена!";
            } else {
                $message = "Ошибка выполнения: " . $stmt->error;
            }
        } else {
            $message = "Ошибка подготовки запроса: " . $link->error;
        }
    } else {
        $message = "Пожалуйста, заполните все поля!";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Добавление новости</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <h1>Добавление новости</h1>
    <?php if (!empty($message)): ?>
        <p><?= htmlspecialchars($message); ?></p>
    <?php endif; ?>
    <form method="POST" action="addnews.php">
        <label for="title">Заголовок:</label><br>
        <input type="text" id="title" name="title" required><br><br>

        <label for="date">Дата:</label><br>
        <input type="date" id="date" name="date" required><br><br>

        <label for="content">Содержание:</label><br>
        <textarea id="content" name="content" rows="5" required></textarea><br><br>

        <button type="submit">Добавить новость</button>
    </form>
</body>
</html>
```

---

#### Пояснения

1. **HTML-форма**:
   - Форма включает поля для заголовка, даты и содержимого.
   - Используется метод `POST` для безопасной отправки данных.

2. **Обработка данных**:
   - Проверяется, что все поля формы заполнены.
   - С помощью подготовленного запроса (`prepare`) данные добавляются в таблицу `news`.

3. **Сообщения об успехе или ошибке**:
   - Выводится сообщение о результате добавления новости.

4. **Безопасность**:
   - Используется экранирование вывода через `htmlspecialchars` для защиты от XSS-атак.

---

### Настройка таблицы в базе данных для addnews.php и news.php

Нужно создать таблицу `articles` в вашей базе данных. Вот пошаговое решение:

---

#### 1. **Подключитесь к базе данных**
Подключитесь к вашей базе данных через консоль MySQL или используйте инструмент, например, phpMyAdmin.

Для подключения через консоль:
```bash
mysql -u root -p
```
Затем выберите вашу базу данных:
```sql
USE bryanskpivo;
```

---

#### 2. **Создайте таблицу `news`**
Выполните следующий SQL-запрос для создания таблицы `news`. Подстройте структуру таблицы под ваши требования:

```sql
CREATE TABLE articles (
    id INT AUTO_INCREMENT PRIMARY KEY,
    article_title VARCHAR(255) NOT NULL,
    article_date DATE NOT NULL,
    article_content TEXT NOT NULL
);
```

- **id**: Уникальный идентификатор для каждой статьи.
- **article_title**: Поле для заголовка статьи (максимальная длина 255 символов).
- **article_date**: Поле для даты публикации статьи.
- **article_content**: Поле для содержимого статьи.

---

#### 3. **Проверьте, что таблица создана**
После создания таблицы убедитесь, что она существует:
```sql
SHOW TABLES;
```
Если в выводе есть `articles`, то таблица успешно создана. Также проверьте её структуру:
```sql
DESCRIBE articles;
```

---

#### 4. **Тест добавления данных**
Попробуйте вручную вставить тестовую запись, чтобы убедиться, что таблица работает:
```sql
INSERT INTO articles (article_title, article_date, article_content)
VALUES ('Тестовая статья', '2025-03-23', 'Это содержимое тестовой статьи.');
```

Проверьте данные:
```sql
SELECT * FROM articles;
```

---

#### 5. **Повторно проверьте `addnews.php`**
Теперь ваш файл `addnews.php` должен работать корректно. Если ошибка повторяется, проверьте, совпадает ли структура запроса с полями таблицы `news`. Например:
```php
$query = "INSERT INTO articles (article_title, article_date, article_content) VALUES (?, ?, ?)";
```

---

Результат:

Файл `addnews.php` позволит вам добавлять новости в базу данных через удобную форму. Если потребуется дополнительная помощь или доработка, дайте знать! 😊



### CSS-код с комментариями

Вот CSS-код с комментариями для каждой строки, чтобы подробно объяснить его работу:

```css
/* Базовые стили */
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Устанавливает семейство шрифтов для всей страницы */
  margin: 0; /* Убирает внешние отступы вокруг тела документа */
  padding: 0; /* Убирает внутренние отступы вокруг тела документа */
  background-color: #F8F9FA; /* Устанавливает светло-серый фон для страницы */
  color: #212529; /* Устанавливает цвет текста (тёмно-серый) */
}

.header {
  display: flex; /* Включает Flexbox для выравнивания содержимого заголовка */
  justify-content: space-between; /* Размещает элементы заголовка по краям */
  align-items: center; /* Центрирует элементы по вертикали */
  padding: 1rem; /* Добавляет внутренние отступы */
  background-color: #E9ECEF; /* Устанавливает светло-серый фон заголовка */
  color: #212529; /* Цвет текста в заголовке */
  text-align: center; /* Центрирует текст в мобильной версии */
  border-bottom: 1px solid #DEE2E6; /* Добавляет нижнюю границу заголовка */
  position: relative; /* Устанавливает относительное позиционирование для использования дочерних элементов */
}

.hamburger {
  display: flex; /* Делает кнопку-гамбургер флекс-элементом */
  cursor: pointer; /* Изменяет курсор на "указатель" при наведении */
  flex-direction: column; /* Располагает полоски кнопки вертикально */
  gap: 0.4rem; /* Задаёт расстояние между полосками */
  position: absolute; /* Абсолютное позиционирование для точного расположения */
  right: 1rem; /* Смещает кнопку от правого края */
  top: 50%; /* Вертикальное смещение от верхнего края */
  transform: translateY(-50%); /* Центрирует кнопку по вертикали */
  z-index: 10; /* Задаёт высокий приоритет отображения кнопки */
}

.hamburger span {
  display: block; /* Делает полоски кнопки блочными элементами */
  width: 25px; /* Задаёт ширину полоски */
  height: 3px; /* Задаёт толщину полоски */
  background-color: #212529; /* Устанавливает тёмный цвет полосок */
  border-radius: 2px; /* Скругляет углы полосок */
}

.nav ul {
  list-style-type: none; /* Убирает стандартные маркеры списка */
  padding: 0; /* Убирает внутренние отступы */
  margin: 0; /* Убирает внешние отступы */
  display: flex; /* Использует Flexbox для элементов списка */
  gap: 0.01rem; /* Минимальное расстояние между элементами списка */
  position: fixed; /* Фиксированное положение относительно окна браузера */
  left: 0; /* Располагает список слева */
  transform: translateX(-100%); /* Скрывает меню за пределами экрана */
  transition: transform 0.3s ease; /* Добавляет плавное появление меню */
  top: 0; /* Закрепляет меню в верхней части экрана */
  height: 100%; /* Устанавливает высоту списка на весь экран */
  width: 250px; /* Задаёт ширину меню */
  background-color: #444; /* Устанавливает тёмный серый фон для меню */
  flex-direction: column; /* Располагает элементы вертикально */
  gap: 1rem; /* Задаёт расстояние между элементами списка */
  padding-top: 2rem; /* Добавляет отступ сверху для содержимого списка */
  z-index: 5; /* Приоритет отображения ниже кнопки */
}

.nav ul.active {
  transform: translateX(0); /* Выдвигает меню на экран */
}

.nav a {
  color: #007BFF; /* Устанавливает синий цвет ссылок */
  text-decoration: none; /* Убирает подчеркивание у ссылок */
  padding: 0.5rem 1rem; /* Добавляет внутренние отступы для ссылок */
  border-radius: 0.25rem; /* Скругляет углы фона ссылок */
  transition: background-color 0.3s ease; /* Делает плавный переход фона при наведении */
}

.nav a:hover {
  background-color: #F1F1F1; /* Светло-серый фон при наведении */
}

.content {
  background-color: #FFFFFF; /* Устанавливает белый фон для содержимого */
  padding: 2rem; /* Добавляет внутренние отступы для контента */
  margin: 1rem auto; /* Центрирует блок и добавляет отступы сверху и снизу */
  max-width: 800px; /* Ограничивает максимальную ширину содержимого */
  border-radius: 0.5rem; /* Скругляет углы блока */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Добавляет легкую тень для создания визуального эффекта */
}

.content p {
  font-size: 1rem; /* Устанавливает стандартный размер шрифта */
  color: #495057; /* Устанавливает темно-серый цвет текста */
}

.footer {
  background-color: #E9ECEF; /* Устанавливает светло-серый фон для подвала */
  padding: 1rem; /* Добавляет внутренние отступы */
  text-align: center; /* Центрирует текст внутри подвала */
  border-top: 1px solid #DEE2E6; /* Добавляет верхнюю границу */
  color: #6C757D; /* Устанавливает светло-серый цвет текста */
  font-size: 0.875rem; /* Уменьшает размер шрифта */
}

@media (max-width: 768px) {
  .nav ul {
    display: none; /* Скрывает меню на маленьких экранах по умолчанию */
    flex-direction: column; /* Располагает элементы вертикально */
    position: absolute; /* Абсолютное позиционирование для списка */
    top: 60px; /* Отступ сверху */
    left: 0; /* Выравнивание по левому краю */
    background-color: #F8F9FA; /* Устанавливает светлый фон для меню */
    width: 100%; /* Меню занимает всю ширину экрана */
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Добавляет тень */
    padding: 1rem; /* Добавляет внутренние отступы */
  }

  .nav ul.active {
    display: flex; /* Показывает меню, когда оно активно */
  }

  .hamburger {
    display: flex; /* Показывает кнопку-гамбургер на маленьких экранах */
  }
}
```

Этот код теперь полностью снабжён комментариями для каждой строки, чтобы объяснить его назначение и использование. 



###  Google Material Icons

Вот обновленный код с использованием Google Material Icons:

1. **Добавьте ссылку в `<head>` вашей HTML-страницы для подключения библиотеки Material Icons:**
   ```html
   <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
   ```

2. **Обновите ваш HTML-код, добавив значки:**
   ```html
   <div class="header">
         <a href="/main.php"><i class="material-icons">home</i></a>
         <a href="/articles.php"><i class="material-icons">article</i></a>
         <a href="/products.php"><i class="material-icons">inventory</i></a>
         <a href="/photos.php"><i class="material-icons">photo</i></a>
         <a href="/index.php"><i class="material-icons">person_add</i></a>
         <a target="_blank" href="/admin.php"><i class="material-icons">admin_panel_settings</i></a>
     <div class="hamburger">
       <span></span>
       <span></span>
       <span></span>
     </div>
     <nav class="nav">
       <ul>
       <li><a href="/main.php">Главная</a></li>
       <li><a href="/articles.php">Статьи</a></li>
       <li><a href="/products.php">Продукция</a></li>
       <li><a href="/photos.php">Галерея</a></li>
       <li><a href="/index.php">Регистрация</a></li>
       <li><a target="_blank" href="/admin.php">Администратор</a></li>
       <li><a target="_blank" href="/adminer.php">Базы Данных</a></li>
       </ul>
     </nav>
   </div>
   ```
   
3. **Стилизация (по желанию):**
   Вы можете настроить размер и цвет значков с помощью CSS:
   ```css
   i.material-icons {
       font-size: 24px;
       color: #333;
       margin-right: 8px;
   }
   ```

Теперь все ссылки отображаются с добавлением значков. Если хотите, я могу помочь доработать стили или протестировать интеграцию! 😊



Вот пример, как можно добавить форму регистрации на ваш существующий HTML. Я поместил ее в секцию `content`, чтобы она была видна на странице. Если нужно другое расположение, вы легко можете изменить это.

### Измененный HTML с формой регистрации

```php
<?php
require_once("DB_connect.php");
require_once("functions.php");
$page = "main";
$PageInfo = get_Page($page);
//print_r($PageInfo);
?>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<link rel="stylesheet" href="/style_index.css" rel="stylesheet">
<title>АО "Брянскпиво"</title>
</head>
<body>

<?php include(__DIR__ . "/header.inc"); ?>
<?php include(__DIR__ . "/nav.inc"); ?>

<div class="content">
  <h2>Форма регистрации</h2>
  <form action="register.php" method="POST">
    <div class="form-group">
      <label for="username">Имя пользователя:</label>
      <input type="text" id="username" name="username" required>
    </div>
    <div class="form-group">
      <label for="email">Электронная почта:</label>
      <input type="email" id="email" name="email" required>
    </div>
    <div class="form-group">
      <label for="password">Пароль:</label>
      <input type="password" id="password" name="password" required>
    </div>
    <button type="submit" class="submit-btn">Зарегистрироваться</button>
  </form>
</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav ul');

  // Открытие/закрытие меню при клике на иконку "гамбургер"
  hamburger.addEventListener('click', () => {
    navMenu.classList.toggle('active');
    console.log('Класс active применён:', navMenu.classList.contains('active'));
    console.log('Элемент hamburger:', hamburger);
    console.log('Элемент navMenu (ul):', navMenu);
  });

  // Закрытие меню при клике в любом месте страницы
  document.addEventListener('click', (event) => {
    // Проверяем, что клик был не на меню и не на кнопке "гамбургер"
    if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
      navMenu.classList.remove('active');
      console.log('Класс active удалён: меню закрыто');
    }
  });
});
</script>

</body>
</html>
```

### Объяснение изменений
1. **Форма регистрации**:
   - Поля для ввода имени пользователя, электронной почты и пароля.
   - Атрибуты `required` сделают поля обязательными для заполнения.
   - Кнопка отправки для регистрации.

2. **Расположение**:
   - Форма добавлена в `content` секцию, следуя вашей структуре.

3. **Стили**:
   - Вы можете добавить стили для классов `.form-group`, `.submit-btn` в вашем `style_index.css`, чтобы оформить элементы регистрации.

Если вам нужны дополнительные изменения, например, стили или функционал для регистрации, дайте знать! 😊



Вот дополненные стили для классов `.form-group` и `.submit-btn`, которые можно добавить в ваш `style_index.css`. Они помогут оформить элементы регистрации и выровнять поля ввода данных:

```css
/* Стили для элементов регистрации */
.form-group {
  margin-bottom: 1rem; /* Отступ между полями ввода */
  display: flex;
  flex-direction: column; /* Расположить подписи и поля ввода вертикально */
}

.form-group label {
  margin-bottom: 0.5rem; /* Отступ между подписью и полем ввода */
  font-weight: bold; /* Полужирный шрифт для подписей */
  color: #495057; /* Темно-серый цвет */
}

.form-group input {
  width: 100%; /* Поле занимает всю ширину контейнера */
  padding: 0.5rem; /* Внутренний отступ для удобного ввода текста */
  border: 1px solid #DEE2E6; /* Светло-серая рамка */
  border-radius: 4px; /* Слегка скругленные углы */
  font-size: 1rem; /* Удобный размер текста */
  color: #495057; /* Темно-серый цвет текста */
}

.form-group input:focus {
  border-color: #007BFF; /* Синий цвет рамки при фокусе */
  outline: none; /* Убрать стандартное выделение */
  box-shadow: 0 0 5px rgba(0, 123, 255, 0.5); /* Светящийся эффект */
}

.submit-btn {
  display: block; /* Блоковая кнопка занимает всю ширину родителя */
  width: 100%; /* Кнопка по ширине равна контейнеру */
  padding: 0.75rem; /* Внутренний отступ */
  background-color: #007BFF; /* Синий фон */
  border: none; /* Без рамки */
  border-radius: 4px; /* Скругленные углы */
  font-size: 1rem; /* Размер текста */
  color: #fff; /* Белый текст */
  cursor: pointer; /* Курсор в виде руки */
  text-align: center; /* Выравнивание текста внутри кнопки */
  transition: background-color 0.3s ease; /* Плавный переход цвета фона */
}

.submit-btn:hover {
  background-color: #0056b3; /* Темно-синий фон при наведении */
}
```

**Что делают эти стили:**
1. **`form-group`**:
   - Организует подписи (`label`) и поля ввода (`input`) вертикально.
   - Создает пространство между элементами.
   - Гарантирует, что поле ввода адаптируется к ширине контейнера.

2. **`form-group input`**:
   - Поля ввода имеют удобный размер, скругленные углы и цвета, которые соответствуют общей цветовой гамме сайта.
   - Добавлен фокусный эффект (синяя рамка и светящийся эффект).

3. **`submit-btn`**:
   - Кнопка регистрации имеет синий фон, белый текст и гладкий эффект при наведении.
   - Полная ширина кнопки упрощает взаимодействие.

Теперь поля ввода будут аккуратно выровнены, а кнопка — заметной и интерактивной. После добавления этих стилей обновите вашу страницу и оцените результат! 



### JavaScript для панели навигации сайта

Извините за возникшую проблему! Ошибка `TypeError: hamburger is null` указывает, что скрипт не может найти элемент с классом `.hamburger` в DOM. Судя по предоставленной информации, сам элемент `.hamburger` присутствует в HTML, но JavaScript не находит его при загрузке. Вот что можно сделать:

---

### Причины и исправления

1. **Проблема с порядком загрузки JavaScript**
   Если JavaScript код выполняется до того, как DOM загружен, элемент может быть недоступен.

   **Исправление:**
   Проверьте, чтобы весь JavaScript код был помещён в `DOMContentLoaded`, что вы уже сделали. Также важно, чтобы HTML правильно структурировался:
   ```html
   <script>
   document.addEventListener('DOMContentLoaded', () => {
     const hamburger = document.querySelector('.hamburger');
     const navMenu = document.querySelector('.nav ul');
   
     // Открытие/закрытие меню при клике на иконку "гамбургер"
     hamburger.addEventListener('click', () => {
       navMenu.classList.toggle('active');
       console.log('Класс active применён:', navMenu.classList.contains('active'));
       console.log('Элемент hamburger:', hamburger);
       console.log('Элемент navMenu (ul):', navMenu);
     });
   
     // Закрытие меню при клике в любом месте страницы
     document.addEventListener('click', (event) => {
       // Проверяем, что клик был не на меню и не на кнопке "гамбургер"
       if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
         navMenu.classList.remove('active');
         console.log('Класс active удалён: меню закрыто');
       }
     });
   });
   </script>
   ```
   
2. **Режим "Quirks Mode"**
   Убедитесь, что вы исправили проблему с режимом Quirks Mode, добавив `<!DOCTYPE html>` в самый верх вашего файла `main.php`.

3. **HTML структура**
   Убедитесь, что HTML разметка соответствует вашим стилям и скрипту. Например:
   ```html
   <div class="nav">
     <div class="hamburger">
       <span></span>
       <span></span>
       <span></span>
     </div>
     <ul>
       <li><a href="/main.php">Главная</a></li>
       <li><a href="/articles.php">Статьи</a></li>
       <li><a href="/products.php">Продукция</a></li>
       <li><a href="/photos.php">Галерея</a></li>
       <li><a target="_blank" href="/admin.php">Администратор</a></li>
       <li><a target="_blank" href="/adminer.php">Базы Данных</a></li>
     </ul>
   </div>
   ```

4. **Проверка отладки**
   Добавьте отладочный код, чтобы проверить, находится ли элемент:
   ```javascript
   console.log(document.querySelector('.hamburger')); // Должно показать элемент в консоли
   console.log(document.querySelector('.nav ul')); // Проверка навигационного меню
   ```

5. **Кэширование браузера**
   Возможно, браузер кэширует старый файл. Очистите кэш браузера (нажмите `Ctrl + F5`) и проверьте.

---

### Диагностика

Если после выполнения вышеуказанных действий проблема сохраняется, выполните следующие шаги:

- Убедитесь, что скрипт действительно выполняется, добавив тестовую строку:
  ```javascript
  console.log("Скрипт выполняется!");
  ```
  Если эта строка не отображается в консоли, значит скрипт не загружается.

- Проверьте файлы через инструменты разработчика в браузере (вкладка "Network"). Убедитесь, что все файлы (особенно `style.css` и `main.php`) загружены без ошибок.

---

Попробуйте предложенные исправления и дайте знать, если что-то всё ещё не работает. Мы вместе это разберём! 🚀



## Разберемся с index.php и admin.php

Чтобы дизайн файла `admin.php` совпадал с дизайном файла `index.php`, вам нужно подключить те же файлы CSS и включить элементы интерфейса, такие как шапка, навигационное меню и футер. Вот обновлённый код для файла `admin.php`:

```php
<?php
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);

require_once("DB_connect.php");

// вывод содержимого переменной $link
echo "<pre>";
var_dump($link);
echo "</pre>";

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $title = $_POST['title'] ?? '';
    $date = $_POST['date'] ?? '';
    $content = $_POST['content'] ?? '';

    if (!empty($title) && !empty($date) && !empty($content)) {
        $query = "INSERT INTO articles (article_title, article_date, article_content) VALUES (?, ?, ?)";
        $stmt = $link->prepare($query);

        if ($stmt) {
            $stmt->bind_param("sss", $title, $date, $content);
            if ($stmt->execute()) {
                $message = "Новость успешно добавлена!";
            } else {
                $message = "Ошибка выполнения: " . $stmt->error;
            }
        } else {
            $message = "Ошибка подготовки запроса: " . $link->error;
        }
    } else {
        $message = "Пожалуйста, заполните все поля!";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Добавление новости</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/style_index.css" rel="stylesheet">
</head>
<body>
<script src="https://kit.fontawesome.com/yourkit.js" crossorigin="anonymous"></script>

<?php include(__DIR__ . "/header.inc"); ?>
<?php include(__DIR__ . "/nav.inc"); ?>

<div class="content">
    <h1>Добавление новости</h1>
    <?php if (!empty($message)): ?>
        <p><?= htmlspecialchars($message); ?></p>
    <?php endif; ?>
    <form method="POST" action="addnews.php">
        <div class="form-group">
            <label for="title">Заголовок:</label><br>
            <input type="text" id="title" name="title" required><br><br>
        </div>
        <div class="form-group">
            <label for="date">Дата:</label><br>
            <input type="date" id="date" name="date" required><br><br>
        </div>
        <div class="form-group">
            <label for="content">Содержание:</label><br>
            <textarea id="content" name="content" rows="5" required></textarea><br><br>
        </div>
        <button type="submit" class="submit-btn">Добавить новость</button>
    </form>
</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

</body>
</html>
```

### Изменения:
1. **Подключены стили `style_index.css`**, как в файле `index.php`.
2. **Добавлены шапка, навигационное меню и футер** с помощью `include`, аналогично `index.php`.
3. **Оформление формы** используется то же самое CSS (классы `form-group` и `submit-btn`).

Этот код сохранит визуальную целостность между двумя файлами. Теперь оба будут выглядеть одинаково!



### Надеюсь, это поможет понять логику

Вот разбор кода с комментариями:

```php
require_once("DB_connect.php");
// Подключение файла "DB_connect.php", который, вероятно, устанавливает соединение с базой данных ($link).

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
// Проверяем, является ли текущий запрос POST-запросом, что говорит о том, что форма была отправлена.

    $title = $_POST['title'] ?? '';
    $date = $_POST['date'] ?? '';
    $content = $_POST['content'] ?? '';
    // Извлекаем данные из POST-запроса: заголовок, дату и содержимое. Если значения не переданы, по умолчанию задаём пустую строку.

    if (!empty($title) && !empty($date) && !empty($content)) {
    // Проверяем, что все три поля (заголовок, дата и содержимое) заполнены.

        $query = "INSERT INTO articles (article_title, article_date, article_content) VALUES (?, ?, ?)";
        // Готовим SQL-запрос на добавление данных в таблицу "articles".

        $stmt = $link->prepare($query);
        // Используем подготовленный запрос для защиты от SQL-инъекций.

        if ($stmt) {
        // Проверяем, успешно ли создан объект подготовленного запроса.

            $stmt->bind_param("sss", $title, $date, $content);
            // Привязываем параметры к запросу. Типы данных: "sss" означает три строки (string).

            if ($stmt->execute()) {
            // Выполняем запрос на добавление данных в базу.

                $message = "Новость успешно добавлена!";
                // Если выполнение прошло успешно, выводим сообщение об успехе.

            } else {
                $message = "Ошибка выполнения: " . $stmt->error;
                // Если выполнение завершилось с ошибкой, сохраняем её текст.
            }
        } else {
            $message = "Ошибка подготовки запроса: " . $link->error;
            // Если подготовка запроса провалилась, сохраняем ошибку соединения.
        }
    } else {
        $message = "Пожалуйста, заполните все поля!";
        // Если одно из полей не заполнено, выводим сообщение об ошибке.
    }
}
?>
```

Этот код предназначен для обработки данных из формы. Он добавляет новую статью в таблицу `articles` базы данных, если все поля заполнены корректно. Если возникают ошибки, соответствующие сообщения отображаются. Этот подход использует подготовленные запросы, что улучшает безопасность против SQL-инъекций. 



### Как улучшить безопасность этого кода?

Для улучшения безопасности вашего кода можно внести ряд изменений:

### 1. **Защита от SQL-инъекций**
Вместо использования `prepare` и `bind_param`, переходите на PDO (PHP Data Objects) с использованием подготовленных выражений:
```php
$stmt = $link->prepare($query);
```
PDO предоставляет мощные средства для защиты от SQL-инъекций.

### 2. **Очистка входных данных**
Применяйте функции фильтрации входных данных, такие как `filter_var()`:
```php
$email = filter_var($_POST['email'], FILTER_SANITIZE_EMAIL);
```

### 3. **Хэширование паролей**
Никогда не храните пароли в открытом виде! Используйте `password_hash()` для хэширования и `password_verify()` для проверки:
```php
$hashedPassword = password_hash($_POST['password'], PASSWORD_DEFAULT);
```

### 4. **Валидация данных**
Проверяйте данные на сервере на соответствие требованиям, например:
```php
if (!filter_var($email, FILTER_VALIDATE_EMAIL)) {
    die('Некорректный адрес электронной почты.');
}
```

### 5. **Защита от XSS**
Очистка вывода через `htmlspecialchars()`:
```php
echo htmlspecialchars($message, ENT_QUOTES, 'UTF-8');
```

### 6. **Используйте CSRF-токены**
Защитите формы с помощью CSRF (Cross-Site Request Forgery) токенов:
```php
session_start();
$csrfToken = bin2hex(random_bytes(32));
$_SESSION['csrf_token'] = $csrfToken;
```
И проверяйте перед отправкой формы.

### 7. **Ограничение доступа к административным файлам**
Ограничьте доступ к файлу `admin.php`, добавив авторизацию:
```php
session_start();
if (!isset($_SESSION['logged_in']) || !$_SESSION['logged_in']) {
    header('Location: login.php');
    exit();
}
```

### 8. **Установите заголовки безопасности**
Устанавливайте заголовки безопасности HTTP, такие как:
```php
header("Content-Security-Policy: default-src 'self';");
header("X-Frame-Options: SAMEORIGIN");
```

### 9. **Ведение логов**
Включите ведение логов для анализа подозрительных действий:
```php
error_log("Попытка SQL-инъекции: $query");
```

### 10. **Ограничение прав пользователя базы данных**
Убедитесь, что у пользователя базы данных есть только необходимые права (например, `INSERT` и `SELECT`, а не `DROP` или `DELETE`).

Внедрение этих улучшений значительно повысит безопасность вашего кода. Если у вас есть конкретные вопросы или вы хотите разобраться в какой-то из этих тем подробнее, пишите! 😊

## Добавить авторизацию в файл `admin.php`

Добавить авторизацию в файл `admin.php` с использованием указанного вами кода — это отличная идея! Вот как это можно реализовать, чтобы ограничить доступ:

```php
<?php
session_start();

// Проверка авторизации пользователя
if (!isset($_SESSION['logged_in']) || !$_SESSION['logged_in']) {
    // Перенаправление на страницу входа, если пользователь не авторизован
    header('Location: login.php');
    exit();
}
?>
```

### Как улучшить:
1. **Убедитесь, что пользователь действительно проходит авторизацию** в `login.php` и переменная `$_SESSION['logged_in']` устанавливается при успешном входе:
   ```php
   // Пример в login.php
   session_start();
   
   if ($user_is_authenticated) { // Логика проверки пользователя
       $_SESSION['logged_in'] = true;
       $_SESSION['user_id'] = $user_id; // Можно сохранить ID пользователя для других целей
       header('Location: admin.php');
       exit();
   } else {
       $login_error = "Неправильный логин или пароль.";
   }
   ```

2. **Добавьте выход из системы (Logout)**
   Это позволит пользователям завершать сессию:
   ```php
   <?php
   session_start();
   session_destroy(); // Удаление всех данных из сессии
   header('Location: login.php');
   exit();
   ?>
   ```

3. **Убедитесь, что сессии защищены**
   Для большей безопасности:
   - Используйте `session_regenerate_id()` после авторизации для предотвращения угонов сессий.
   - Настройте параметры PHP для использования безопасных cookies:
     ```php
     ini_set('session.cookie_httponly', true);
     ini_set('session.cookie_secure', true); // Только если работает HTTPS
     ```

4. **Используйте HTTPS**
   Защитите все соединения, особенно страницы с логином, с помощью SSL (HTTPS).

Теперь ваш файл `admin.php` будет защищён от неавторизованного доступа. Если что-то ещё нужно уточнить, не стесняйтесь обращаться! 😊

### Вот доработанный файл `admin.php`

Вот доработанный файл `admin.php`, в который добавлены все предложенные улучшения:

### Изменения:
1. **Авторизация на основе сессии** для ограничения доступа.
2. **Защита от SQL-инъекций** с помощью PDO.
3. **Очистка и валидация входных данных**.
4. **Защита от XSS** при выводе сообщений.
5. **Настройки безопасности сессии и использование HTTPS.**

Обновлённый код:

```php
<?php
session_start();

// Ограничение доступа: проверка авторизации
if (!isset($_SESSION['logged_in']) || !$_SESSION['logged_in']) {
    header('Location: login.php');
    exit();
}

// Настройки безопасности сессии
ini_set('session.cookie_httponly', true);
ini_set('session.cookie_secure', true); // Только если работает HTTPS
session_regenerate_id(true); // Регистрация нового ID сессии для предотвращения угона

// Отображение ошибок для разработки
ini_set('display_errors', 1);
ini_set('display_startup_errors', 1);
error_reporting(E_ALL);

require_once("DB_connect.php");

$message = '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    // Очистка входных данных
    $title = htmlspecialchars(trim($_POST['title'] ?? ''));
    $date = htmlspecialchars(trim($_POST['date'] ?? ''));
    $content = htmlspecialchars(trim($_POST['content'] ?? ''));

    // Валидация входных данных
    if (!empty($title) && !empty($date) && !empty($content)) {
        try {
            // Используем PDO для работы с БД
            $query = "INSERT INTO articles (article_title, article_date, article_content) VALUES (:title, :date, :content)";
            $stmt = $link->prepare($query);

            // Привязка параметров
            $stmt->bindParam(':title', $title, PDO::PARAM_STR);
            $stmt->bindParam(':date', $date, PDO::PARAM_STR);
            $stmt->bindParam(':content', $content, PDO::PARAM_STR);

            if ($stmt->execute()) {
                $message = "Новость успешно добавлена!";
            } else {
                $message = "Ошибка выполнения запроса.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, заполните все поля!";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Добавление новости</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/style_index.css" rel="stylesheet">
</head>
<body>
<script src="https://kit.fontawesome.com/yourkit.js" crossorigin="anonymous"></script>

<?php include(__DIR__ . "/header.inc"); ?>
<?php include(__DIR__ . "/nav.inc"); ?>

<div class="content">
    <h1>Добавление новости</h1>
    <?php if (!empty($message)): ?>
        <p><?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="admin.php">
        <div class="form-group">
            <label for="title">Заголовок:</label><br>
            <input type="text" id="title" name="title" required><br><br>
        </div>
        <div class="form-group">
            <label for="date">Дата:</label><br>
            <input type="date" id="date" name="date" required><br><br>
        </div>
        <div class="form-group">
            <label for="content">Содержание:</label><br>
            <textarea id="content" name="content" rows="5" required></textarea><br><br>
        </div>
        <button type="submit" class="submit-btn">Добавить новость</button>
    </form>
</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

</body>
</html>
```

### Что улучшено:
1. **Авторизация с использованием сессии**: гарантирует, что доступ к `admin.php` получают только авторизованные пользователи.
2. **PDO**: безопасный способ работы с базой данных.
3. **Очистка и защита данных**: с помощью `htmlspecialchars()` и фильтрации данных.
4. **Безопасность сессии**: защита от угонов сессий с помощью `session_regenerate_id()` и безопасных cookies.
5. **HTTPS**: для предотвращения перехвата данных рекомендуется использовать `ini_set('session.cookie_secure', true)`.

Теперь файл безопаснее и защищён от большинства распространённых уязвимостей. Если вам нужно настроить дополнительные функции, например, роль пользователя, я рад помочь! 😊

## Напиши код для login.php

Вот файл `login.php` в том же стиле, который подходит для базовой системы аутентификации. Этот код позволяет пользователям вводить свои учётные данные для входа, а при успешной проверке устанавливается сессия, подтверждающая авторизацию.

### Код `login.php`
```php
<?php
session_start();
require_once("DB_connect.php");

// Инициализация сообщения об ошибке
$error_message = '';

// Проверка, отправлена ли форма
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $username = htmlspecialchars(trim($_POST['username'] ?? ''));
    $password = htmlspecialchars(trim($_POST['password'] ?? ''));

    // Проверка заполненности полей
    if (!empty($username) && !empty($password)) {
        try {
            // Подготовленный запрос для проверки логина
            $query = "SELECT user_id, password FROM users WHERE username = :username";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':username', $username, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user && password_verify($password, $user['password'])) {
                // Авторизация успешна
                $_SESSION['logged_in'] = true;
                $_SESSION['user_id'] = $user['user_id'];
                $_SESSION['username'] = $username;

                // Перенаправление на страницу admin.php
                header('Location: admin.php');
                exit();
            } else {
                $error_message = 'Неправильное имя пользователя или пароль.';
            }
        } catch (PDOException $e) {
            $error_message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $error_message = 'Пожалуйста, заполните все поля.';
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Вход</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/style_index.css" rel="stylesheet">
</head>
<body>
<script src="https://kit.fontawesome.com/yourkit.js" crossorigin="anonymous"></script>

<?php include(__DIR__ . "/header.inc"); ?>
<?php include(__DIR__ . "/nav.inc"); ?>

<div class="content">
    <h1>Вход</h1>
    <?php if (!empty($error_message)): ?>
        <p style="color: red;"><?= htmlspecialchars($error_message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="login.php">
        <div class="form-group">
            <label for="username">Имя пользователя:</label><br>
            <input type="text" id="username" name="username" required><br><br>
        </div>
        <div class="form-group">
            <label for="password">Пароль:</label><br>
            <input type="password" id="password" name="password" required><br><br>
        </div>
        <button type="submit" class="submit-btn">Войти</button>
    </form>
</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

</body>
</html>
```

---

### Что нужно для работы этого кода?

1. **Таблица `users` в базе данных**:
   ```sql
   CREATE TABLE users (
       user_id INT AUTO_INCREMENT PRIMARY KEY,
       username VARCHAR(50) UNIQUE NOT NULL,
       password VARCHAR(255) NOT NULL
   );
   ```
   Пароль должен быть сохранён в зашифрованном виде с использованием `password_hash()`.

2. **Регистрация пользователей** (по необходимости):
   Вы можете добавить отдельную страницу для регистрации, где пользователь создаёт имя пользователя и пароль. Хэшируйте пароли перед сохранением:
   ```php
   $hashed_password = password_hash($password, PASSWORD_DEFAULT);
   ```

3. **Настройка сессий**:
   Убедитесь, что в вашем `php.ini` файл настроены безопасные параметры для cookies:
   ```ini
   session.cookie_httponly = 1
   session.cookie_secure = 1 // Если сайт работает по HTTPS
   ```

4. **Перенаправление**:
   После успешного входа пользователь будет перенаправлен на `admin.php`.

---

Этот код настроен для безопасного входа и авторизации. Если хотите улучшить или дополнить его (например, добавить систему ролей или двухфакторную аутентификацию), дайте знать! 😊



> для регистрации пользователей (по необходимости) я добавил отдельную страницу index.php, где пользователь создаёт имя пользователя и пароль, если нужно исправь ее 

Вот доработанный код для вашей страницы регистрации пользователей `index.php`. Я добавил улучшения для безопасности и функциональности.

### Обновленный файл `index.php`
```php
<?php
require_once("DB_connect.php");
require_once("functions.php");

// Инициализация переменной для сообщений
$message = '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    // Очистка и получение данных из формы
    $username = htmlspecialchars(trim($_POST['username'] ?? ''));
    $email = filter_var($_POST['email'] ?? '', FILTER_SANITIZE_EMAIL);
    $password = htmlspecialchars(trim($_POST['password'] ?? ''));

    // Проверка заполненности полей
    if (!empty($username) && !empty($email) && !empty($password)) {
        try {
            // Хэширование пароля
            $hashedPassword = password_hash($password, PASSWORD_DEFAULT);

            // Подготовленный запрос для добавления нового пользователя
            $query = "INSERT INTO users (username, email, password) VALUES (:username, :email, :password)";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':username', $username, PDO::PARAM_STR);
            $stmt->bindParam(':email', $email, PDO::PARAM_STR);
            $stmt->bindParam(':password', $hashedPassword, PDO::PARAM_STR);

            if ($stmt->execute()) {
                $message = "Регистрация прошла успешно!";
            } else {
                $message = "Ошибка выполнения запроса.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, заполните все поля!";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
<meta charset="UTF-8">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<link rel="stylesheet" href="/style_index.css" rel="stylesheet">
<title>НКО "Развитие СПО и ЦТ"</title>
</head>
<body>
<script src="https://kit.fontawesome.com/yourkit.js" crossorigin="anonymous"></script>

<?php include(__DIR__ . "/header.inc"); ?>
<?php include(__DIR__ . "/nav.inc"); ?>

<div class="content">
  <h2>Форма регистрации</h2>
  <?php if (!empty($message)): ?>
    <p><?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8'); ?></p>
  <?php endif; ?>
  <form action="index.php" method="POST">
    <div class="form-group">
      <label for="username">Имя пользователя:</label>
      <input type="text" id="username" name="username" required>
    </div>
    <div class="form-group">
      <label for="email">Электронная почта:</label>
      <input type="email" id="email" name="email" required>
    </div>
    <div class="form-group">
      <label for="password">Пароль:</label>
      <input type="password" id="password" name="password" required>
    </div>
    <button type="submit" class="submit-btn">Зарегистрироваться</button>
  </form>
</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav ul');

  // Открытие/закрытие меню при клике на иконку "гамбургер"
  hamburger.addEventListener('click', () => {
    navMenu.classList.toggle('active');
    console.log('Класс active применён:', navMenu.classList.contains('active'));
  });

  // Закрытие меню при клике в любом месте страницы
  document.addEventListener('click', (event) => {
    if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
      navMenu.classList.remove('active');
      console.log('Класс active удалён: меню закрыто');
    }
  });
});
</script>

</body>
</html>
```

---

### Основные улучшения:
1. **Очистка данных**:
   - Использованы `htmlspecialchars` и `filter_var` для защиты от XSS.
2. **Хэширование пароля**:
   - Пароль хэшируется с помощью `password_hash()` перед сохранением.
3. **Проверка данных**:
   - Все поля проверяются на пустоту.
4. **Безопасное взаимодействие с базой данных**:
   - Используется PDO для предотвращения SQL-инъекций.
5. **Сообщения для пользователя**:
   - Вывод сообщений об успешной регистрации или ошибках.

---

### Необходимые дополнения:
1. **Таблица пользователей в базе данных**:
   ```sql
   CREATE TABLE users (
       user_id INT AUTO_INCREMENT PRIMARY KEY,
       username VARCHAR(50) UNIQUE NOT NULL,
       email VARCHAR(255) UNIQUE NOT NULL,
       password VARCHAR(255) NOT NULL
   );
   ```
2. **Страница входа**:
   После регистрации пользователь может войти на сайт через `login.php`.

---

Если что-то ещё нужно уточнить или расширить (например, добавить подтверждение email или роли пользователей), я рад помочь! 😊



> для файла входа login.php или лучше переделать этот файл чтобы таи тоже было поле ввода с email если так тогда напиши полностью исправленный код login.php



Понял! Лучше добавить поле ввода для **email** в `login.php` и использовать его вместе с паролем для входа. Это позволит вашему коду работать с первой таблицей `users` (с полем `email`) в базе данных. Вот полностью исправленный код `login.php`:

### Исправленный файл `login.php`

```php
<?php
session_start();
require_once("DB_connect.php");

// Инициализация сообщения об ошибке
$error_message = '';

// Проверка, отправлена ли форма
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $email = filter_var($_POST['email'] ?? '', FILTER_SANITIZE_EMAIL);
    $password = htmlspecialchars(trim($_POST['password'] ?? ''));

    // Проверка заполненности полей
    if (!empty($email) && !empty($password)) {
        try {
            // Подготовленный запрос для проверки email
            $query = "SELECT user_id, username, password FROM users WHERE email = :email";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':email', $email, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user && password_verify($password, $user['password'])) {
                // Авторизация успешна
                $_SESSION['logged_in'] = true;
                $_SESSION['user_id'] = $user['user_id'];
                $_SESSION['username'] = $user['username'];

                // Перенаправление на страницу admin.php
                header('Location: admin.php');
                exit();
            } else {
                $error_message = 'Неправильный адрес электронной почты или пароль.';
            }
        } catch (PDOException $e) {
            $error_message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $error_message = 'Пожалуйста, заполните все поля.';
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Вход</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/style_index.css" rel="stylesheet">
</head>
<body>
<script src="https://kit.fontawesome.com/yourkit.js" crossorigin="anonymous"></script>

<?php include(__DIR__ . "/header.inc"); ?>
<?php include(__DIR__ . "/nav.inc"); ?>

<div class="content">
    <h1>Вход</h1>
    <?php if (!empty($error_message)): ?>
        <p style="color: red;"><?= htmlspecialchars($error_message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="login.php">
        <div class="form-group">
            <label for="email">Электронная почта:</label><br>
            <input type="email" id="email" name="email" required><br><br>
        </div>
        <div class="form-group">
            <label for="password">Пароль:</label><br>
            <input type="password" id="password" name="password" required><br><br>
        </div>
        <button type="submit" class="submit-btn">Войти</button>
    </form>
</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

</body>
</html>
```

---

### Основные изменения:

1. **Добавлено поле ввода email**:
   - Теперь пользователь будет использовать адрес электронной почты для входа.

2. **Использование таблицы `users` с полем `email`**:
   - Запрос в базе данных ищет пользователя по `email`.

3. **Сообщение об ошибке**:
   - Сообщает пользователю, если введён неправильный адрес электронной почты или пароль.

4. **Очистка и фильтрация**:
   - Функции `filter_var` и `htmlspecialchars` применяются для защиты от XSS.

5. **Безопасное взаимодействие с базой данных**:
   - Используется PDO для предотвращения SQL-инъекций.

---

### Напоминание о таблице пользователей
Ваш код работает с таблицей:

```sql
CREATE TABLE users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    password VARCHAR(255) NOT NULL
);
```

---

Если что-то ещё нужно уточнить или дополнить (например, восстановление пароля или роль пользователя), дайте знать! 😊



Понял! Вот исправленный файл `login.php`, который позволит входить исключительно по логину и паролю:

---

### Обновленный файл `login.php`

```php
<?php
session_start();
require_once("DB_connect.php");

// Инициализация сообщения об ошибке
$error_message = '';

// Проверка, отправлена ли форма
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $username = htmlspecialchars(trim($_POST['username'] ?? ''));
    $password = htmlspecialchars(trim($_POST['password'] ?? ''));

    // Проверка заполненности полей
    if (!empty($username) && !empty($password)) {
        try {
            // Запрос для проверки логина
            $query = "SELECT user_id, password FROM users WHERE username = :username";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':username', $username, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user && password_verify($password, $user['password'])) {
                // Авторизация успешна
                $_SESSION['logged_in'] = true;
                $_SESSION['user_id'] = $user['user_id'];
                $_SESSION['username'] = $username;

                // Перенаправление на страницу admin.php
                header('Location: admin.php');
                exit();
            } else {
                $error_message = 'Неправильный логин или пароль.';
            }
        } catch (PDOException $e) {
            $error_message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $error_message = 'Пожалуйста, заполните все поля.';
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Вход</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/style_index.css" rel="stylesheet">
</head>
<body>
<script src="https://kit.fontawesome.com/yourkit.js" crossorigin="anonymous"></script>

<?php include(__DIR__ . "/header.inc"); ?>
<?php include(__DIR__ . "/nav.inc"); ?>

<div class="content">
    <h1>Вход</h1>
    <?php if (!empty($error_message)): ?>
        <p style="color: red;"><?= htmlspecialchars($error_message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="login.php">
        <div class="form-group">
            <label for="username">Логин:</label><br>
            <input type="text" id="username" name="username" required><br><br>
        </div>
        <div class="form-group">
            <label for="password">Пароль:</label><br>
            <input type="password" id="password" name="password" required><br><br>
        </div>
        <button type="submit" class="submit-btn">Войти</button>
    </form>
</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

</body>
</html>
```

---

### Основные изменения:
1. **Поле идентификатора**:
   - Убрано поле `email`, теперь вход осуществляется только по `username`.

2. **SQL-запрос**:
   - Пользователь идентифицируется только по `username`.

3. **Сообщения об ошибке**:
   - Сообщение чётко говорит, если логин или пароль неверны.

---

Если понадобится доработка или интеграция с другими страницами, сообщите — всегда рад помочь! 😊



## Забыл пароль его можно обновить forgot_password.php

> что нужно добавить что бы если забыл пароль его можно бы обновить по электронной почте пользователя

Хорошо! Давайте добавим функционал восстановления пароля по электронной почте. Вот как это можно сделать:

1. **Добавьте страницу для запроса сброса пароля (`forgot_password.php`)**, где пользователь вводит свою электронную почту.
2. **Отправьте письмо с уникальной ссылкой для сброса пароля.** Эта ссылка будет содержать токен.
3. **Создайте страницу для установки нового пароля (`reset_password.php`),** которая проверяет токен и позволяет установить новый пароль.

---

### 1. Код страницы `forgot_password.php`

```php
<?php
require_once("DB_connect.php");

$message = '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $email = filter_var($_POST['email'] ?? '', FILTER_SANITIZE_EMAIL);

    if (!empty($email)) {
        try {
            // Проверка, существует ли email в базе данных
            $query = "SELECT user_id FROM users WHERE email = :email";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':email', $email, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user) {
                // Генерация уникального токена
                $token = bin2hex(random_bytes(32));
                $expires = date('Y-m-d H:i:s', strtotime('+1 hour')); // Срок действия 1 час

                // Сохранение токена в базе данных
                $query = "INSERT INTO password_resets (user_id, token, expires_at) VALUES (:user_id, :token, :expires_at)";
                $stmt = $link->prepare($query);
                $stmt->bindParam(':user_id', $user['user_id'], PDO::PARAM_INT);
                $stmt->bindParam(':token', $token, PDO::PARAM_STR);
                $stmt->bindParam(':expires_at', $expires, PDO::PARAM_STR);
                $stmt->execute();

                // Отправка письма для сброса пароля
                $reset_link = "https://yourwebsite.com/reset_password.php?token=" . urlencode($token);
                mail($email, "Восстановление пароля", "Чтобы восстановить пароль, перейдите по ссылке: $reset_link");

                $message = "Инструкция для восстановления пароля отправлена на вашу почту.";
            } else {
                $message = "Электронная почта не найдена.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, введите ваш email.";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Восстановление пароля</title>
    <link rel="stylesheet" href="/style_index.css">
</head>
<body>
<div class="content">
    <h1>Восстановление пароля</h1>
    <?php if (!empty($message)): ?>
        <p><?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="forgot_password.php">
        <label for="email">Электронная почта:</label><br>
        <input type="email" id="email" name="email" required><br><br>
        <button type="submit">Отправить ссылку для восстановления</button>
    </form>
</div>
</body>
</html>
```

---

### 2. Таблица для токенов сброса пароля

Создайте таблицу в базе данных, чтобы хранить токены сброса пароля:

```sql
CREATE TABLE password_resets (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    token VARCHAR(64) NOT NULL,
    expires_at DATETIME NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users(user_id) ON DELETE CASCADE
);
```

---

### 3. Код страницы `reset_password.php`

```php
<?php
require_once("DB_connect.php");

$message = '';
$token = $_GET['token'] ?? '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $new_password = htmlspecialchars(trim($_POST['password'] ?? ''));
    $token = htmlspecialchars(trim($_POST['token'] ?? ''));

    if (!empty($new_password) && !empty($token)) {
        try {
            // Проверка токена
            $query = "SELECT user_id, expires_at FROM password_resets WHERE token = :token";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':token', $token, PDO::PARAM_STR);
            $stmt->execute();

            $reset = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($reset && strtotime($reset['expires_at']) > time()) {
                // Хэширование нового пароля
                $hashedPassword = password_hash($new_password, PASSWORD_DEFAULT);

                // Обновление пароля пользователя
                $query = "UPDATE users SET password = :password WHERE user_id = :user_id";
                $stmt = $link->prepare($query);
                $stmt->bindParam(':password', $hashedPassword, PDO::PARAM_STR);
                $stmt->bindParam(':user_id', $reset['user_id'], PDO::PARAM_INT);
                $stmt->execute();

                // Удаление токена
                $query = "DELETE FROM password_resets WHERE token = :token";
                $stmt = $link->prepare($query);
                $stmt->bindParam(':token', $token, PDO::PARAM_STR);
                $stmt->execute();

                $message = "Пароль успешно обновлен!";
            } else {
                $message = "Срок действия токена истёк или он недействителен.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, заполните все поля.";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Сброс пароля</title>
    <link rel="stylesheet" href="/style_index.css">
</head>
<body>
<div class="content">
    <h1>Сброс пароля</h1>
    <?php if (!empty($message)): ?>
        <p><?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="reset_password.php">
        <input type="hidden" name="token" value="<?= htmlspecialchars($token, ENT_QUOTES, 'UTF-8'); ?>">
        <label for="password">Новый пароль:</label><br>
        <input type="password" id="password" name="password" required><br><br>
        <button type="submit">Обновить пароль</button>
    </form>
</div>
</body>
</html>
```

---

### Как это работает:
1. Пользователь переходит на `forgot_password.php`, вводит email, и получает письмо с уникальной ссылкой для сброса пароля.
2. Ссылка ведёт на `reset_password.php`, где пользователь вводит новый пароль.
3. Новый пароль обновляется в базе данных, и токен сброса удаляется.

Если вам нужно что-то доработать или объяснить, напишите! 😊



### Теперь ссылку на forgot_password.php добавим в login.Вот обновленный код файла `login.php`, где добавлена ссылка на страницу восстановления пароля `forgot_password.php`.

---

### Обновленный файл `login.php`

```php
<?php
session_start();
require_once("DB_connect.php");

// Инициализация сообщения об ошибке
$error_message = '';

// Проверка, отправлена ли форма
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $username = htmlspecialchars(trim($_POST['username'] ?? ''));
    $password = htmlspecialchars(trim($_POST['password'] ?? ''));

    // Проверка заполненности полей
    if (!empty($username) && !empty($password)) {
        try {
            // Запрос для проверки логина
            $query = "SELECT user_id, password FROM users WHERE username = :username";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':username', $username, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user && password_verify($password, $user['password'])) {
                // Авторизация успешна
                $_SESSION['logged_in'] = true;
                $_SESSION['user_id'] = $user['user_id'];
                $_SESSION['username'] = $username;

                // Перенаправление на страницу admin.php
                header('Location: admin.php');
                exit();
            } else {
                $error_message = 'Неправильный логин или пароль.';
            }
        } catch (PDOException $e) {
            $error_message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $error_message = 'Пожалуйста, заполните все поля.';
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Вход</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link rel="stylesheet" href="/style_index.css" rel="stylesheet">
</head>
<body>
<script src="https://kit.fontawesome.com/yourkit.js" crossorigin="anonymous"></script>

<?php include(__DIR__ . "/header.inc"); ?>
<?php include(__DIR__ . "/nav.inc"); ?>

<div class="content">
    <h1>Вход</h1>
    <?php if (!empty($error_message)): ?>
        <p style="color: red;"><?= htmlspecialchars($error_message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="login.php">
        <div class="form-group">
            <label for="username">Логин:</label><br>
            <input type="text" id="username" name="username" required><br><br>
        </div>
        <div class="form-group">
            <label for="password">Пароль:</label><br>
            <input type="password" id="password" name="password" required><br><br>
        </div>
        <button type="submit" class="submit-btn">Войти</button>
    </form>
    <p>
        <a href="forgot_password.php" style="text-decoration: none; color: #007BFF;">Забыли пароль?</a>
    </p>
</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

</body>
</html>
```

---

### Основные изменения:
1. **Добавлена ссылка на восстановление пароля**:
   - Внизу формы входа добавлена строка `<a href="forgot_password.php">Забыли пароль?</a>`.

2. **Оформление ссылки**:
   - Ссылка стилизована с использованием `style` для сохранения общего стиля страницы.

---

Теперь пользователи смогут перейти на страницу `forgot_password.php` для восстановления пароля. Если нужно что-то доработать, просто скажите! 😊



## Как улучшить интерфейс страницы входа?

Есть множество способов сделать интерфейс страницы входа более привлекательным, интуитивным и удобным. Вот несколько рекомендаций:

### 1. **Дизайн и визуальная привлекательность**
- **Стилизуйте кнопку входа:** сделайте её яркой, с небольшим эффектом при наведении мыши.
  ```css
  .submit-btn {
      background-color: #007BFF;
      color: white;
      border: none;
      padding: 10px 20px;
      border-radius: 5px;
      cursor: pointer;
      transition: background-color 0.3s ease;
  }
  
  .submit-btn:hover {
      background-color: #0056b3;
  }
  ```

- **Используйте современные шрифты:** подключите стильный шрифт, например, `Roboto` или `Poppins` из Google Fonts.
  ```html
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  ```
  И добавьте стиль для текста:
  ```css
  body {
      font-family: 'Roboto', sans-serif;
  }
  ```

- **Цветовая схема:** используйте гармоничные цвета, например, сочетание светлых и нейтральных оттенков с акцентными цветами (синим, зелёным, оранжевым).

### 2. **Улучшение навигации**
- Добавьте **иконки** рядом с полями для логина и пароля для визуальной подсказки. Например:
  ```html
  <label for="username"><i class="material-icons">person</i> Логин:</label>
  <label for="password"><i class="material-icons">lock</i> Пароль:</label>
  ```

- **Кнопка "Забыли пароль?"** поместите рядом с полем пароля, чтобы пользователи легко находили её.

### 3. **Удобство использования**
- **Автоматический фокус:** добавьте автоматический фокус на поле логина.
  ```html
  <script>
      document.addEventListener('DOMContentLoaded', () => {
          document.getElementById('username').focus();
      });
  </script>
  ```

- **Показ/скрытие пароля:** добавьте функционал, который позволяет показывать или скрывать введённый пароль.
  ```html
  <input type="password" id="password" name="password" required>
  <button type="button" onclick="togglePassword()">Показать</button>
  <script>
      function togglePassword() {
          const passwordInput = document.getElementById('password');
          const type = passwordInput.getAttribute('type') === 'password' ? 'text' : 'password';
          passwordInput.setAttribute('type', type);
      }
  </script>
  ```

### 4. **Отзывчивость (Responsive Design)**
- Сделайте дизайн мобильным-дружественным, используя CSS Media Queries:
  ```css
  @media (max-width: 768px) {
      .content {
          padding: 15px;
          font-size: 14px;
      }
  
      .form-group {
          margin-bottom: 10px;
      }
  }
  ```

### 5. **Дружественные сообщения**
- Показывайте понятные сообщения об ошибке, например:
  ```html
  <p class="error-message">Неправильный логин или пароль</p>
  ```
  Стили:
  ```css
  .error-message {
      color: red;
      font-size: 14px;
      margin-top: 10px;
  }
  ```

### 6. **Анимация**
Добавьте небольшую анимацию для плавности взаимодействия, например, эффекты загрузки на кнопку:
```html
<button type="submit" class="submit-btn" onclick="showLoader()">Войти</button>
<div id="loader" style="display: none;">Загрузка...</div>
<script>
    function showLoader() {
        document.getElementById('loader').style.display = 'block';
    }
</script>
```

### Итог:
Соблюдая эти рекомендации, ваша страница станет более современной и удобной для пользователя. Хотите, чтобы я помог с реализацией какого-то конкретного элемента? 😊



## В reset_password.php аторизация через электронную почту

> в файле reset_password.php нужно добавить чтобы аторизация происходила через электронную почту пользователя по которой он зарегистрировался на моем сайте

Чтобы авторизация происходила через электронную почту пользователя, зарегистрированного на вашем сайте, нужно внести изменения в код. Мы добавим проверку, чтобы обновление пароля происходило на основе электронной почты пользователя. Вот обновленный код для `reset_password.php`:

---

### Обновленный файл `reset_password.php`

```php
<?php
require_once("DB_connect.php");

$message = '';
$token = $_GET['token'] ?? '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $new_password = htmlspecialchars(trim($_POST['password'] ?? ''));
    $token = htmlspecialchars(trim($_POST['token'] ?? ''));

    if (!empty($new_password) && !empty($token)) {
        try {
            // Проверка токена
            $query = "SELECT user_id, email, expires_at FROM password_resets WHERE token = :token";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':token', $token, PDO::PARAM_STR);
            $stmt->execute();

            $reset = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($reset && strtotime($reset['expires_at']) > time()) {
                // Хэширование нового пароля
                $hashedPassword = password_hash($new_password, PASSWORD_DEFAULT);

                // Обновление пароля пользователя
                $query = "UPDATE users SET password = :password WHERE email = :email";
                $stmt = $link->prepare($query);
                $stmt->bindParam(':password', $hashedPassword, PDO::PARAM_STR);
                $stmt->bindParam(':email', $reset['email'], PDO::PARAM_STR);
                $stmt->execute();

                // Удаление токена
                $query = "DELETE FROM password_resets WHERE token = :token";
                $stmt = $link->prepare($query);
                $stmt->bindParam(':token', $token, PDO::PARAM_STR);
                $stmt->execute();

                $message = "Пароль успешно обновлен!";
            } else {
                $message = "Срок действия токена истёк или он недействителен.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, заполните все поля.";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Сброс пароля</title>
    <link rel="stylesheet" href="/style_index.css">
</head>
<body>
<div class="content">
    <h1>Сброс пароля</h1>
    <?php if (!empty($message)): ?>
        <p><?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="reset_password.php">
        <input type="hidden" name="token" value="<?= htmlspecialchars($token, ENT_QUOTES, 'UTF-8'); ?>">
        <label for="password">Новый пароль:</label><br>
        <input type="password" id="password" name="password" required><br><br>
        <button type="submit">Обновить пароль</button>
    </form>
</div>
</body>
</html>
```

---

### Что изменилось:
1. **Авторизация по электронной почте**:
   - Токен связывается с конкретной электронной почтой пользователя через таблицу `password_resets`.
   - Пароль обновляется в таблице `users` на основе электронной почты (`WHERE email = :email`).

2. **Уточнение запроса**:
   - Вместо `user_id` используется `email` для идентификации пользователя.

---

### Напоминание:
- Убедитесь, что таблица `password_resets` содержит столбец `email` либо токены связаны с `user_id`, который, в свою очередь, связан с `email` в таблице `users`.
- Этот код предполагает, что email уникален для каждого пользователя.

Если нужно ещё что-то улучшить или дополнить, дайте знать! 😊



в файле reset_password.php код выше нужно добавить чтобы аторизация происходила через электронную почту пользователя по которой он зарегистрировался на моем сайте и при этом он должен ввести свой пароль и логин перейти на свою почту и взять там код который нужно ввести reset_password.php перед изменением пароля



Вот доработанный файл `reset_password.php`, который включает авторизацию по электронной почте, проверку логина и пароля, а также использование кода подтверждения, отправленного на почту. Пользователь должен будет ввести код подтверждения, чтобы сбросить свой пароль.

---

### Полный код `reset_password.php`
```php
<?php
require_once("DB_connect.php");

$message = '';
$token = $_GET['token'] ?? '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $email = filter_var($_POST['email'] ?? '', FILTER_SANITIZE_EMAIL);
    $username = htmlspecialchars(trim($_POST['username'] ?? ''));
    $current_password = htmlspecialchars(trim($_POST['current_password'] ?? ''));
    $new_password = htmlspecialchars(trim($_POST['new_password'] ?? ''));
    $entered_code = htmlspecialchars(trim($_POST['verification_code'] ?? ''));

    if (!empty($email) && !empty($username) && !empty($current_password) && !empty($new_password) && !empty($entered_code)) {
        try {
            // Проверка токена и email
            $query = "SELECT user_id, username, password, verification_code, expires_at FROM users WHERE email = :email";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':email', $email, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user && $user['username'] === $username && password_verify($current_password, $user['password'])) {
                // Проверка кода подтверждения
                if ($user['verification_code'] === $entered_code && strtotime($user['expires_at']) > time()) {
                    // Хэширование нового пароля
                    $hashedPassword = password_hash($new_password, PASSWORD_DEFAULT);

                    // Обновление пароля пользователя
                    $query = "UPDATE users SET password = :password, verification_code = NULL, expires_at = NULL WHERE user_id = :user_id";
                    $stmt = $link->prepare($query);
                    $stmt->bindParam(':password', $hashedPassword, PDO::PARAM_STR);
                    $stmt->bindParam(':user_id', $user['user_id'], PDO::PARAM_INT);
                    $stmt->execute();

                    $message = "Пароль успешно обновлен!";
                } else {
                    $message = "Код подтверждения неверный или срок действия истёк.";
                }
            } else {
                $message = "Данные не совпадают или пользователь не найден.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, заполните все поля.";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Сброс пароля</title>
    <link rel="stylesheet" href="/style_index.css">
</head>
<body>
<div class="content">
    <h1>Сброс пароля</h1>
    <?php if (!empty($message)): ?>
        <p><?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="reset_password.php">
        <label for="email">Электронная почта:</label><br>
        <input type="email" id="email" name="email" required><br><br>

        <label for="username">Логин:</label><br>
        <input type="text" id="username" name="username" required><br><br>

        <label for="current_password">Текущий пароль:</label><br>
        <input type="password" id="current_password" name="current_password" required><br><br>

        <label for="verification_code">Код подтверждения:</label><br>
        <input type="text" id="verification_code" name="verification_code" required><br><br>

        <label for="new_password">Новый пароль:</label><br>
        <input type="password" id="new_password" name="new_password" required><br><br>

        <button type="submit">Обновить пароль</button>
    </form>
</div>
</body>
</html>
```

---

### Основные изменения:
1. **Добавлены поля для ввода:**
   - Электронной почты.
   - Логина.
   - Текущего пароля.
   - Кода подтверждения.
   - Нового пароля.

2. **Код подтверждения**:
   - Проверка введённого кода и его срока действия (`verification_code` и `expires_at`).
   - Коды сброса должны быть отправлены на почту заранее.

3. **Улучшение безопасности**:
   - Проверка текущего пароля, логина и электронной почты.
   - Пароль обновляется только если все данные совпадают и код подтверждения действителен.

4. **Очистка данных**:
   - Используются функции `htmlspecialchars` и `filter_var` для защиты от XSS.

---

### Подготовка для работы:
1. **Добавьте в таблицу `users` столбцы для кода подтверждения и срока действия**:
   ```sql
   ALTER TABLE users ADD verification_code VARCHAR(64), ADD expires_at DATETIME;
   ```

2. **Отправка кода на email**:
   Коды подтверждения должны быть отправлены заранее (например, в `forgot_password.php`).

3. **Срок действия кода**:
   Убедитесь, что код недействителен после его использования или истечения срока.

---

Если нужно что-то ещё уточнить или доработать, дайте знать! 😊



> а отправку на почту пользователя кода подтверждения какой файл или функция будет заниматься



Отправку кода подтверждения на почту пользователя обычно выполняет отдельная функция, которая может быть интегрирована в файл, где пользователь запрашивает восстановление пароля, например, `forgot_password.php`.

Вот как это можно реализовать:

---

### Функция отправки кода подтверждения на почту
Добавьте эту функцию в файл `forgot_password.php`:

```php
function sendVerificationCode($email, $verificationCode) {
    $subject = "Код подтверждения для сброса пароля";
    $message = "Ваш код подтверждения: $verificationCode\n\nКод действителен в течение одного часа.";
    $headers = "From: no-reply@yourwebsite.com";

    // Отправка письма
    if (mail($email, $subject, $message, $headers)) {
        return true; // Успех
    } else {
        return false; // Ошибка при отправке
    }
}
```

---

### Как использовать функцию в `forgot_password.php`

1. **Генерация кода подтверждения:**
   ```php
   $verificationCode = bin2hex(random_bytes(16)); // Генерация уникального кода
   $expiresAt = date('Y-m-d H:i:s', strtotime('+1 hour')); // Срок действия кода (1 час)
   ```

2. **Сохранение кода и срока действия в базе данных:**
   ```php
   $query = "UPDATE users SET verification_code = :verificationCode, expires_at = :expiresAt WHERE email = :email";
   $stmt = $link->prepare($query);
   $stmt->bindParam(':verificationCode', $verificationCode, PDO::PARAM_STR);
   $stmt->bindParam(':expiresAt', $expiresAt, PDO::PARAM_STR);
   $stmt->bindParam(':email', $email, PDO::PARAM_STR);
   $stmt->execute();
   ```

3. **Вызов функции отправки письма:**
   ```php
   if (sendVerificationCode($email, $verificationCode)) {
       $message = "Код подтверждения отправлен на вашу электронную почту.";
   } else {
       $message = "Ошибка при отправке письма. Попробуйте позже.";
   }
   ```

---

### Обновленный `forgot_password.php`

Вот пример интеграции:

```php
<?php
require_once("DB_connect.php");

$message = '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $email = filter_var($_POST['email'] ?? '', FILTER_SANITIZE_EMAIL);

    if (!empty($email)) {
        try {
            // Проверка, существует ли email в базе данных
            $query = "SELECT user_id FROM users WHERE email = :email";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':email', $email, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user) {
                // Генерация кода подтверждения
                $verificationCode = bin2hex(random_bytes(16));
                $expiresAt = date('Y-m-d H:i:s', strtotime('+1 hour'));

                // Сохранение кода в базе данных
                $query = "UPDATE users SET verification_code = :verificationCode, expires_at = :expiresAt WHERE email = :email";
                $stmt = $link->prepare($query);
                $stmt->bindParam(':verificationCode', $verificationCode, PDO::PARAM_STR);
                $stmt->bindParam(':expiresAt', $expiresAt, PDO::PARAM_STR);
                $stmt->bindParam(':email', $email, PDO::PARAM_STR);
                $stmt->execute();

                // Отправка письма
                if (sendVerificationCode($email, $verificationCode)) {
                    $message = "Код подтверждения отправлен на вашу электронную почту.";
                } else {
                    $message = "Ошибка при отправке письма. Попробуйте позже.";
                }
            } else {
                $message = "Электронная почта не найдена.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, введите ваш email.";
    }
}

function sendVerificationCode($email, $verificationCode) {
    $subject = "Код подтверждения для сброса пароля";
    $message = "Ваш код подтверждения: $verificationCode\n\nКод действителен в течение одного часа.";
    $headers = "From: no-reply@yourwebsite.com";

    return mail($email, $subject, $message, $headers);
}
?>
```

---

### Примечания:
1. **Настройка отправки почты**:
   - Убедитесь, что на вашем сервере включена функция `mail()`. Если сервер не поддерживает `mail()`, используйте сторонние SMTP-библиотеки, например, [PHPMailer](https://github.com/PHPMailer/PHPMailer).

2. **Безопасность**:
   - Убедитесь, что код подтверждения уникален для каждого запроса.
   - Для большей безопасности используйте HTTPS для вашего сайта.

Если вам нужно что-то ещё, например, интеграция с SMTP или дополнительные проверки, дайте знать! 😊



Файлы `forgot_password.php` и `reset_password.php` взаимодействуют через код подтверждения (verification code), который генерируется в `forgot_password.php`, сохраняется в базе данных, и затем используется в `reset_password.php` для проверки перед сменой пароля. Давайте разберём процесс пошагово:

---

### Шаг 1: Генерация кода подтверждения в `forgot_password.php`
1. Пользователь вводит свою электронную почту в форме на странице `forgot_password.php`.
2. Если электронная почта существует в базе данных:
   - Генерируется уникальный код подтверждения (`verification_code`) и срок его действия (`expires_at`).
   - Эти данные сохраняются в таблице `users` (или другой таблице, например, `password_resets`).
   - Код отправляется на почту пользователя.

```php
$verificationCode = bin2hex(random_bytes(16)); // Генерация кода
$expiresAt = date('Y-m-d H:i:s', strtotime('+1 hour')); // Срок действия кода
```

---

### Шаг 2: Отправка кода пользователю
Код отправляется пользователю через email (с использованием PHP функции `mail()` или библиотеки, например, PHPMailer). Пример ссылки, отправляемой пользователю:
```
https://yourwebsite.com/reset_password.php
```

---

### Шаг 3: Взаимодействие с `reset_password.php`
1. Пользователь получает код подтверждения на почту.
2. Пользователь переходит на страницу `reset_password.php`, где вводит:
   - Свою электронную почту.
   - Логин.
   - Текущий пароль.
   - Код подтверждения из почты.
   - Новый пароль.

3. На сервере `reset_password.php` проверяет:
   - Совпадение электронной почты и кода подтверждения.
   - Срок действия кода (токен считается недействительным, если он истёк).
   - Текущий пароль пользователя.
   - Уникальность нового пароля.

4. Если все проверки успешны:
   - Пароль пользователя обновляется в базе данных.
   - Код подтверждения (`verification_code`) и срок действия (`expires_at`) удаляются из базы.

```php
if ($user['verification_code'] === $entered_code && strtotime($user['expires_at']) > time()) {
    // Обновление пароля
    $hashedPassword = password_hash($new_password, PASSWORD_DEFAULT);
    $query = "UPDATE users SET password = :password, verification_code = NULL, expires_at = NULL WHERE email = :email";
}
```

---

### Шаг 4: Очистка данных
После успешной смены пароля:
- Код подтверждения удаляется из базы, чтобы его нельзя было повторно использовать.
- Пользователь перенаправляется на страницу успешного уведомления.

---

### Итог:
- **`forgot_password.php`** генерирует код подтверждения и отправляет его на email.
- **`reset_password.php`** проверяет код, а также данные пользователя перед тем, как обновить пароль.

Этот процесс обеспечивает как удобство для пользователя, так и безопасность. Если нужно уточнить или доработать логику, просто напишите! 😊



## Как файл forgot_password.php будет взаимодействовать с reset_password.php

Файлы `forgot_password.php` и `reset_password.php` взаимодействуют через процесс восстановления пароля, который состоит из нескольких шагов. Вот как происходит их взаимодействие:

---

### Шаг 1: Запрос кода подтверждения (`forgot_password.php`)
1. Пользователь вводит свой email на странице `forgot_password.php`.
2. Файл `forgot_password.php`:
   - Проверяет, существует ли указанный email в базе данных.
   - Генерирует уникальный код подтверждения (`verification_code`) и определяет срок его действия (`expires_at`).
   - Сохраняет этот код в базе данных (обычно в таблице `users` или отдельной таблице `password_resets`).
   - Отправляет email пользователю с кодом подтверждения.

Пример кода генерации и сохранения кода:
```php
$verificationCode = bin2hex(random_bytes(16)); // Генерация уникального кода
$expiresAt = date('Y-m-d H:i:s', strtotime('+1 hour')); // Срок действия кода

$query = "UPDATE users SET verification_code = :verificationCode, expires_at = :expiresAt WHERE email = :email";
$stmt = $link->prepare($query);
$stmt->bindParam(':verificationCode', $verificationCode, PDO::PARAM_STR);
$stmt->bindParam(':expiresAt', $expiresAt, PDO::PARAM_STR);
$stmt->bindParam(':email', $email, PDO::PARAM_STR);
$stmt->execute();
```

---

### Шаг 2: Получение кода подтверждения
1. Пользователь получает email с кодом подтверждения.
2. Email содержит ссылку на страницу `reset_password.php`, например:
   ```
   https://yourwebsite.com/reset_password.php
   ```

---

### Шаг 3: Проверка кода и смена пароля (`reset_password.php`)
1. Пользователь переходит на страницу `reset_password.php`, где вводит:
   - Email.
   - Логин.
   - Текущий пароль.
   - Код подтверждения.
   - Новый пароль.
2. `reset_password.php`:
   - Проверяет, соответствует ли email указанному коду подтверждения.
   - Убеждается, что срок действия кода не истёк (`expires_at`).
   - Проверяет текущий пароль.
   - Если все проверки пройдены, файл обновляет пароль пользователя в базе данных и удаляет код подтверждения.

Пример проверки кода и смены пароля:
```php
$query = "SELECT user_id, verification_code, expires_at FROM users WHERE email = :email";
$stmt = $link->prepare($query);
$stmt->bindParam(':email', $email, PDO::PARAM_STR);
$stmt->execute();

$user = $stmt->fetch(PDO::FETCH_ASSOC);

if ($user && $user['verification_code'] === $enteredCode && strtotime($user['expires_at']) > time()) {
    // Хэширование нового пароля
    $hashedPassword = password_hash($newPassword, PASSWORD_DEFAULT);

    // Обновление пароля
    $query = "UPDATE users SET password = :password, verification_code = NULL, expires_at = NULL WHERE email = :email";
    $stmt = $link->prepare($query);
    $stmt->bindParam(':password', $hashedPassword, PDO::PARAM_STR);
    $stmt->bindParam(':email', $email, PDO::PARAM_STR);
    $stmt->execute();
}
```

---

### Шаг 4: Очистка данных
После успешной смены пароля:
- Код подтверждения (`verification_code`) и срок его действия (`expires_at`) удаляются из базы данных.
- Пользователь получает уведомление об успешной смене пароля.

---

### Итог:
- **`forgot_password.php`**:
   - Генерирует код подтверждения, сохраняет его в базе данных и отправляет на email.
- **`reset_password.php`**:
   - Проверяет код, email, логин, текущий пароль и срок действия.
   - Обновляет пароль, удаляет код подтверждения и уведомляет пользователя.

Если нужно подробнее рассмотреть какую-либо часть взаимодействия, я рад помочь! 😊



## Три файла (`login.php`, `forgot_password.php`, `reset_password.php`) 

как они между собой взаимосвязаны, где пользователь запрашивает восстановление пароля

Эти три файла (`login.php`, `forgot_password.php`, `reset_password.php`) взаимодействуют через процесс восстановления пароля, чтобы предоставить пользователю возможность восстановить доступ. Взаимосвязь между файлами выглядит следующим образом:

---

### 1. **Запрос восстановления пароля (`forgot_password.php`)**
Пользователь запрашивает восстановление пароля на этой странице:
- На странице формы пользователь вводит свой **email**, привязанный к аккаунту.
- Сценарий проверяет, существует ли email в базе данных.
- Если email найден:
  - Генерируется уникальный код подтверждения (`verification_code`).
  - Код и срок его действия (`expires_at`) сохраняются в базе данных.
  - Код отправляется на электронную почту пользователя.

**Пример интерфейса:**  
Пользователь видит форму с полем для ввода электронной почты, например:
```html
<form method="POST" action="forgot_password.php">
    <label for="email">Электронная почта:</label><br>
    <input type="email" id="email" name="email" required><br><br>
    <button type="submit">Запросить восстановление</button>
</form>
```

**Действие:**  
- Пользователь получает письмо с кодом и инструкцией, например:
  ```
  Ваш код подтверждения: 123456
  Код действителен в течение одного часа.
  Перейдите на страницу https://yourwebsite.com/reset_password.php
  ```

---

### 2. **Переход на страницу сброса пароля (`reset_password.php`)**
Пользователь переходит по ссылке, отправленной в письме, чтобы ввести код подтверждения и сбросить пароль:
- На странице сброса пароля пользователь вводит:
  - **Email** (адрес, который он указывал в запросе восстановления).
  - **Логин**.
  - **Текущий пароль**.
  - **Код подтверждения**.
  - **Новый пароль**.
- Сценарий на сервере:
  - Проверяет, существует ли комбинация email и кода подтверждения в базе данных.
  - Убеждается, что срок действия кода подтверждения ещё не истёк.
  - Проверяет, соответствует ли введённый текущий пароль сохранённому паролю в базе данных.
  - Если все проверки успешны, то:
    - Новый пароль сохраняется в зашифрованной форме (`password_hash`).
    - Код подтверждения (`verification_code`) и срок действия (`expires_at`) очищаются из базы.

**Пример интерфейса:**  
Пользователь видит форму на странице сброса пароля:
```html
<form method="POST" action="reset_password.php">
    <label for="email">Электронная почта:</label>
    <input type="email" id="email" name="email" required><br><br>

    <label for="username">Логин:</label>
    <input type="text" id="username" name="username" required><br><br>

    <label for="current_password">Текущий пароль:</label>
    <input type="password" id="current_password" name="current_password" required><br><br>

    <label for="verification_code">Код подтверждения:</label>
    <input type="text" id="verification_code" name="verification_code" required><br><br>

    <label for="new_password">Новый пароль:</label>
    <input type="password" id="new_password" name="new_password" required><br><br>

    <button type="submit">Обновить пароль</button>
</form>
```

---

### 3. **Авторизация на странице входа (`login.php`)**
После успешного обновления пароля пользователь может вернуться на страницу входа (`login.php`):
- Вводит логин и новый пароль.
- Если данные корректны, происходит авторизация, и пользователь перенаправляется на защищённые страницы сайта (например, `admin.php`).

**Пример интерфейса страницы входа:**
```html
<form method="POST" action="login.php">
    <label for="username">Логин:</label>
    <input type="text" id="username" name="username" required><br><br>
    <label for="password">Пароль:</label>
    <input type="password" id="password" name="password" required><br><br>
    <button type="submit">Войти</button>
</form>
<p>
    <a href="forgot_password.php">Забыли пароль?</a>
</p>
```

---

### Как файлы связаны между собой:
1. **`login.php`:**
   - Позволяет пользователю войти в систему.
   - Содержит ссылку на `forgot_password.php` для восстановления пароля.

2. **`forgot_password.php`:**
   - Позволяет запросить восстановление пароля.
   - Генерирует код подтверждения и отправляет его пользователю на email.
   - Создаёт переходный этап к файлу `reset_password.php`.

3. **`reset_password.php`:**
   - Позволяет пользователю подтвердить код и изменить пароль.
   - Верифицирует данные, введённые пользователем (email, код, текущий пароль и логин).
   - Обновляет пароль, если проверки проходят успешно.

---

### Пример рабочего сценария:
1. Пользователь на странице `login.php` нажимает на ссылку **"Забыли пароль?"** и переходит на `forgot_password.php`.
2. На `forgot_password.php` пользователь вводит свой email. Если email найден, код подтверждения отправляется на указанную почту.
3. Пользователь получает email с кодом подтверждения и ссылкой на `reset_password.php`.
4. На странице `reset_password.php` пользователь вводит email, логин, текущий пароль, код подтверждения и новый пароль.
5. Если проверки успешны, система обновляет пароль в базе данных.
6. Пользователь возвращается на страницу `login.php`, вводит логин и новый пароль для входа.

---

Таким образом, эти файлы связаны через токен (код подтверждения) и базу данных, где обновляются и проверяются данные пользователя. Если требуется доработка, я готов помочь! 😊



## `forgot_password.php`

Вы совершенно правы, что в вашем файле `forgot_password.php` отсутствует HTML-форма, с помощью которой пользователь может ввести свою электронную почту. Для полноценной работы страницы необходимо добавить интерфейс, предоставляющий пользователю возможность отправить запрос на восстановление пароля.

Вот обновлённый код `forgot_password.php` с добавленной HTML-формой:

---

### Полный файл `forgot_password.php`
```php
<?php
require_once("DB_connect.php");

$message = '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $email = filter_var($_POST['email'] ?? '', FILTER_SANITIZE_EMAIL);

    if (!empty($email)) {
        try {
            // Проверка, существует ли email в базе данных
            $query = "SELECT user_id FROM users WHERE email = :email";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':email', $email, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user) {
                // Генерация кода подтверждения
                $verificationCode = bin2hex(random_bytes(16));
                $expiresAt = date('Y-m-d H:i:s', strtotime('+1 hour'));

                // Сохранение кода в базе данных
                $query = "UPDATE users SET verification_code = :verificationCode, expires_at = :expiresAt WHERE email = :email";
                $stmt = $link->prepare($query);
                $stmt->bindParam(':verificationCode', $verificationCode, PDO::PARAM_STR);
                $stmt->bindParam(':expiresAt', $expiresAt, PDO::PARAM_STR);
                $stmt->bindParam(':email', $email, PDO::PARAM_STR);
                $stmt->execute();

                // Отправка письма
                if (sendVerificationCode($email, $verificationCode)) {
                    $message = "Код подтверждения отправлен на вашу электронную почту.";
                } else {
                    $message = "Ошибка при отправке письма. Попробуйте позже.";
                }
            } else {
                $message = "Электронная почта не найдена.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, введите ваш email.";
    }
}

// Функция для отправки кода подтверждения на email
function sendVerificationCode($email, $verificationCode) {
    $subject = "Код подтверждения для сброса пароля";
    $message = "Ваш код подтверждения: $verificationCode\n\nКод действителен в течение одного часа.";
    $headers = "From: no-reply@yourwebsite.com";

    return mail($email, $subject, $message, $headers);
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Запрос восстановления пароля</title>
    <link rel="stylesheet" href="/style_index.css">
</head>
<body>
<div class="content">
    <h1>Восстановление пароля</h1>
    <?php if (!empty($message)): ?>
        <p><?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="forgot_password.php">
        <label for="email">Введите вашу электронную почту:</label><br>
        <input type="email" id="email" name="email" required><br><br>
        <button type="submit" class="submit-btn">Отправить код подтверждения</button>
    </form>
</div>
</body>
</html>
```

---

### Что добавлено:
1. **HTML-форма**:
   - Содержит поле ввода электронной почты пользователя (`email`).
   - Позволяет пользователю отправить запрос на восстановление пароля.

2. **Сообщения об успехе и ошибках**:
   - Если email найден, пользователь видит сообщение: "Код подтверждения отправлен на вашу электронную почту."
   - Если email не найден или произошла ошибка, отображается соответствующее сообщение.

3. **Интерфейс для отправки данных**:
   - Форма отправляет данные на тот же файл (`forgot_password.php`) через метод `POST`.

---

### Как пользователь будет работать с этой страницей:
1. Пользователь переходит на страницу `forgot_password.php`.
2. Пользователь вводит email, привязанный к его аккаунту.
3. Нажимает кнопку "Отправить код подтверждения".
4. Если email найден в базе данных:
   - Код подтверждения отправляется на email.
   - Пользователь видит уведомление об успешной отправке.
5. Далее пользователь переходит на страницу `reset_password.php` из письма, чтобы ввести код подтверждения и изменить пароль.

---

Теперь страница `forgot_password.php` содержит всё необходимое для начала процесса восстановления пароля. Если что-то нужно доработать, дайте знать! 😊



> На forgot_password.php пользователь вводит свой email. Если email найден, код подтверждения отправляется на указанную почту. но какая функция будудет отправлять сообщение на email, в какой форме будет это сообщение с кодом подтверждения и ссылкой на reset_password.php будет отправляется на указанную почту

В файле `forgot_password.php` для отправки сообщения с кодом подтверждения используется функция `mail()`. Эта функция стандартна для PHP, но может быть заменена на более мощное решение, например, библиотеку PHPMailer, если требуется более надежная отправка через SMTP.

---

### Функция отправки письма с кодом подтверждения

Вот пример функции для отправки email с помощью встроенной функции `mail()`:

```php
function sendVerificationCode($email, $verificationCode) {
    $subject = "Восстановление пароля";
    $resetLink = "https://yourwebsite.com/reset_password.php?email=" . urlencode($email);
    $message = "Здравствуйте,\n\n"
             . "Вы запросили восстановление пароля.\n"
             . "Ваш код подтверждения: $verificationCode\n"
             . "Перейдите по ссылке для сброса пароля: $resetLink\n\n"
             . "Срок действия кода: 1 час.\n\n"
             . "Если вы не запрашивали восстановление, просто проигнорируйте это письмо.\n\n"
             . "С уважением,\nКоманда поддержки.";

    $headers = "From: no-reply@yourwebsite.com\r\n";
    $headers .= "Content-Type: text/plain; charset=UTF-8\r\n";

    // Отправка письма
    return mail($email, $subject, $message, $headers);
}
```

---

### Объяснение:

1. **Параметры функции:**
   - `email`: адрес электронной почты пользователя.
   - `verificationCode`: уникальный код подтверждения.

2. **Содержимое сообщения:**
   - Приветствие.
   - Код подтверждения.
   - Ссылка на страницу `reset_password.php` для ввода кода и смены пароля.
   - Указание срока действия кода.

3. **Срок действия:**
   - В сообщении указано, что код подтверждения действителен в течение 1 часа.

4. **Безопасность:**
   - Указано, что, если запрос на сброс пароля не делался, письмо можно проигнорировать.

5. **Заголовки письма:**
   - Отправитель: `no-reply@yourwebsite.com`.
   - Кодировка письма: UTF-8.

---

### Альтернатива с использованием PHPMailer

Если ваш сервер не поддерживает функцию `mail()`, рекомендуется использовать PHPMailer. Вот пример:

```php
use PHPMailer\PHPMailer\PHPMailer;

function sendVerificationCode($email, $verificationCode) {
    $mail = new PHPMailer(true);

    try {
        // Настройки SMTP
        $mail->isSMTP();
        $mail->Host = 'smtp.yourserver.com';
        $mail->SMTPAuth = true;
        $mail->Username = 'your-email@yourserver.com';
        $mail->Password = 'your-email-password';
        $mail->SMTPSecure = PHPMailer::ENCRYPTION_STARTTLS;
        $mail->Port = 587;

        // Отправитель и получатель
        $mail->setFrom('no-reply@yourwebsite.com', 'Your Website');
        $mail->addAddress($email);

        // Содержание письма
        $mail->Subject = 'Восстановление пароля';
        $resetLink = "https://yourwebsite.com/reset_password.php?email=" . urlencode($email);
        $mail->Body = "Здравствуйте,\n\n"
                    . "Вы запросили восстановление пароля.\n"
                    . "Ваш код подтверждения: $verificationCode\n"
                    . "Перейдите по ссылке для сброса пароля: $resetLink\n\n"
                    . "Срок действия кода: 1 час.\n\n"
                    . "Если вы не запрашивали восстановление, просто проигнорируйте это письмо.\n\n"
                    . "С уважением,\nКоманда поддержки.";

        $mail->send();
        return true;

    } catch (Exception $e) {
        return false;
    }
}
```

---

### Формат сообщения с кодом:
Пользователь получит письмо следующего вида:
```
Здравствуйте,

Вы запросили восстановление пароля.
Ваш код подтверждения: 123456
Перейдите по ссылке для сброса пароля: https://yourwebsite.com/reset_password.php?email=example@email.com

Срок действия кода: 1 час.

Если вы не запрашивали восстановление, просто проигнорируйте это письмо.

С уважением,
Команда поддержки.
```

---

### Взаимодействие с `reset_password.php`:
Пользователь переходит по ссылке из письма, вводит код подтверждения, email, логин и новый пароль. Код подтверждения проверяется, и пароль обновляется в базе данных, если проверка успешна.

Если потребуется дополнительная настройка или объяснение, дайте знать! 😊



> вот мой код файла reset_password.php на который Пользователь переходит по ссылке из письма, вводит код подтверждения, email, логин и новый пароль, нужно согласовать и убрать лишние поля для ввода. а так же прокоментируй как Код подтверждения проверяется, и пароль обновляется в базе данных, если проверка прошла успешно.

Чтобы упростить интерфейс и согласовать структуру страницы с реальной функциональностью, мы уберем лишние поля, такие как **текущий пароль** и **логин**, так как для сброса пароля достаточно **email**, **кода подтверждения** и **нового пароля**. Также объясню, как код подтверждения проверяется и как обновляется пароль в базе данных.

---

### Обновлённый код `reset_password.php`

```php
<?php
require_once("DB_connect.php");

$message = '';
$token = $_GET['token'] ?? '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $email = filter_var($_POST['email'] ?? '', FILTER_SANITIZE_EMAIL);
    $new_password = htmlspecialchars(trim($_POST['new_password'] ?? ''));
    $entered_code = htmlspecialchars(trim($_POST['verification_code'] ?? ''));

    // Проверка: все поля должны быть заполнены
    if (!empty($email) && !empty($new_password) && !empty($entered_code)) {
        try {
            // Проверка, существует ли пользователь с указанным email и кодом подтверждения
            $query = "SELECT user_id, verification_code, expires_at FROM users WHERE email = :email";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':email', $email, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            // Проверка кода подтверждения
            if ($user && $user['verification_code'] === $entered_code && strtotime($user['expires_at']) > time()) {
                // Хэширование нового пароля
                $hashedPassword = password_hash($new_password, PASSWORD_DEFAULT);

                // Обновление пароля и удаление кода подтверждения
                $query = "UPDATE users SET password = :password, verification_code = NULL, expires_at = NULL WHERE user_id = :user_id";
                $stmt = $link->prepare($query);
                $stmt->bindParam(':password', $hashedPassword, PDO::PARAM_STR);
                $stmt->bindParam(':user_id', $user['user_id'], PDO::PARAM_INT);
                $stmt->execute();

                $message = "Пароль успешно обновлен!";
            } else {
                $message = "Код подтверждения неверный или срок его действия истёк.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, заполните все поля.";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Сброс пароля</title>
    <link rel="stylesheet" href="/style_index.css">
</head>
<body>
<div class="content">
    <h1>Сброс пароля</h1>
    <?php if (!empty($message)): ?>
        <p><?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="reset_password.php">
        <label for="email">Электронная почта:</label><br>
        <input type="email" id="email" name="email" required><br><br>

        <label for="verification_code">Код подтверждения:</label><br>
        <input type="text" id="verification_code" name="verification_code" required><br><br>

        <label for="new_password">Новый пароль:</label><br>
        <input type="password" id="new_password" name="new_password" required><br><br>

        <button type="submit">Обновить пароль</button>
    </form>
</div>
</body>
</html>
```

---

### Объяснение изменений:
1. **Убраны лишние поля:**
   - **Логин** и **текущий пароль** больше не нужны, так как процесс сброса пароля идентифицирует пользователя по email и коду подтверждения.
   - Новый пароль создаётся независимо от текущего пароля.

2. **Функциональность осталась целостной:**
   - **Email** используется для нахождения пользователя.
   - **Код подтверждения** сверяется, чтобы убедиться в законности запроса.
   - **Срок действия кода** проверяется, чтобы код не мог быть использован после его истечения.

---

### Как проверяется код подтверждения:
1. Запрос из базы данных ищет пользователя по **email** и извлекает:
   - `verification_code`: код подтверждения.
   - `expires_at`: срок действия кода.

2. Проверка:
   - Код подтверждения (`$user['verification_code']`) должен совпадать с введённым пользователем (`$entered_code`).
   - Срок действия не должен истечь: `strtotime($user['expires_at']) > time()`.

---

### Как пароль обновляется:
1. Если код подтверждения прошёл проверку:
   - Новый пароль хэшируется с использованием `password_hash()`.
   - Хэшированный пароль записывается в базу данных.

2. Очистка:
   - Поля `verification_code` и `expires_at` в таблице обнуляются, чтобы код нельзя было использовать повторно:
     ```php
     verification_code = NULL, expires_at = NULL
     ```

3. Уведомление:
   - Пользователь видит сообщение: "Пароль успешно обновлен!"

---

### Упрощённый процесс для пользователя:
1. Получает письмо с кодом подтверждения.
2. Вводит **email**, **код подтверждения** и **новый пароль** на странице.
3. Если проверка успешна, пароль обновляется.

Если вы хотите уточнить детали или усовершенствовать процесс, обращайтесь! 😊



> я думаю что после успешного обновления пароля код файла reset_password.php должен перенаправлять пользователя на главную страницу сайта, а при неудачном резельтате выводить сообщение повторить попытку

Отличная идея! Перенаправление пользователя на главную страницу после успешного обновления пароля сделает процесс более понятным и удобным, а вывод сообщения при неудаче обеспечит ясную обратную связь. Вот как можно доработать ваш файл `reset_password.php`:

---

## Обновлённый код `reset_password.php`

```php
<?php
require_once("DB_connect.php");

$message = '';

if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $email = filter_var($_POST['email'] ?? '', FILTER_SANITIZE_EMAIL);
    $new_password = htmlspecialchars(trim($_POST['new_password'] ?? ''));
    $entered_code = htmlspecialchars(trim($_POST['verification_code'] ?? ''));

    if (!empty($email) && !empty($new_password) && !empty($entered_code)) {
        try {
            // Проверка, существует ли пользователь с указанным email и кодом подтверждения
            $query = "SELECT user_id, verification_code, expires_at FROM users WHERE email = :email";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':email', $email, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user && $user['verification_code'] === $entered_code && strtotime($user['expires_at']) > time()) {
                // Хэширование нового пароля
                $hashedPassword = password_hash($new_password, PASSWORD_DEFAULT);

                // Обновление пароля и удаление кода подтверждения
                $query = "UPDATE users SET password = :password, verification_code = NULL, expires_at = NULL WHERE user_id = :user_id";
                $stmt = $link->prepare($query);
                $stmt->bindParam(':password', $hashedPassword, PDO::PARAM_STR);
                $stmt->bindParam(':user_id', $user['user_id'], PDO::PARAM_INT);
                $stmt->execute();

                // Перенаправление на главную страницу
                header("Location: /index.php");
                exit();
            } else {
                $message = "Код подтверждения неверный или срок его действия истёк. Попробуйте снова.";
            }
        } catch (PDOException $e) {
            $message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $message = "Пожалуйста, заполните все поля.";
    }
}
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <meta charset="UTF-8">
    <title>Сброс пароля</title>
    <link rel="stylesheet" href="/style_index.css">
</head>
<body>
<div class="content">
    <h1>Сброс пароля</h1>
    <?php if (!empty($message)): ?>
        <p style="color: red;"><?= htmlspecialchars($message, ENT_QUOTES, 'UTF-8'); ?></p>
    <?php endif; ?>
    <form method="POST" action="reset_password.php">
        <label for="email">Электронная почта:</label><br>
        <input type="email" id="email" name="email" required><br><br>

        <label for="verification_code">Код подтверждения:</label><br>
        <input type="text" id="verification_code" name="verification_code" required><br><br>

        <label for="new_password">Новый пароль:</label><br>
        <input type="password" id="new_password" name="new_password" required><br><br>

        <button type="submit">Обновить пароль</button>
    </form>
</div>
</body>
</html>
```

---

### Комментарии к изменениям:

1. **Успешное обновление пароля:**
   - При успешной смене пароля выполняется перенаправление пользователя на главную страницу сайта:
     ```php
     header("Location: /index.php");
     exit();
     ```
   - Функция `header()` отправляет HTTP-заголовок для перенаправления, а `exit()` завершает выполнение скрипта.

2. **Неудачная попытка:**
   - Если проверка не проходит (например, неверный код или истёк срок действия), выводится сообщение:
     ```php
     "Код подтверждения неверный или срок его действия истёк. Попробуйте снова."
     ```
   - Пользователь остаётся на странице сброса пароля.

3. **Чёткая обратная связь:**
   - При возникновении ошибок, связанных с заполнением полей или работой базы данных, пользователь сразу получает уведомление.

4. **Сценарий защиты:**
   - Обновление базы данных выполняется только после успешной проверки кода, чтобы избежать злоупотреблений.

---

Теперь процесс выглядит следующим образом:
1. Пользователь вводит данные для сброса пароля.
2. Если проверка успешна, пароль обновляется, и пользователь перенаправляется на главную страницу.
3. В случае ошибки пользователь остаётся на странице `reset_password.php` и видит сообщение с предложением повторить попытку.

Если хотите уточнить детали или добавить функциональность, пишите! 😊



## Блок ссылок всегда справа

Чтобы ваш блок ссылок всегда находился справа при прокрутке страницы, можно использовать CSS со стилем `position: fixed`. Этот стиль закрепит блок на определённой позиции относительно окна браузера.

Вот что нужно сделать:

1. Добавьте ваш HTML-код для блока ссылок в файл `main.php`:

```html
<div class="fixed-links">
  <a href="/main.php"><i class="material-icons">home</i></a>
  <a href="/articles.php"><i class="material-icons">article</i></a>
  <a href="/products.php"><i class="material-icons">inventory</i></a>
  <a href="/photos.php"><i class="material-icons">photo</i></a>
  <a href="/index.php"><i class="material-icons">person_add</i></a>
  <a target="_blank" href="/admin.php"><i class="material-icons">admin_panel_settings</i></a>
</div>
```

2. Добавьте в файл `style_index.css` следующий стиль для класса `.fixed-links`:

```css
.fixed-links {
  position: fixed; /* Фиксирует блок относительно окна браузера */
  top: 50%; /* Устанавливает позицию блока по вертикали (по центру) */
  right: 0; /* Блок находится справа */
  transform: translateY(-50%); /* Центрирование блока по вертикали */
  background-color: #E9ECEF; /* Фон блока */
  padding: 1rem; /* Внутренние отступы */
  border-radius: 0.5rem 0 0 0.5rem; /* Скругление углов */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Тень */
}

.fixed-links a {
  display: block; /* Каждый элемент ссылки занимает всю ширину */
  margin: 0.5rem 0; /* Отступы между ссылками */
  color: #212529; /* Цвет ссылок */
  text-decoration: none; /* Убирает подчеркивание ссылок */
  transition: background-color 0.3s ease; /* Плавный эффект изменения фона */
}

.fixed-links a:hover {
  background-color: #F1F1F1; /* Цвет фона при наведении */
}
```

Теперь блок ссылок будет всегда закреплён справа на странице и останется видимым при прокрутке. Вы можете изменять свойства `top`, `right` и `transform`, чтобы настроить позицию блока более точно. 



Вот как будут выглядеть файлы `main.php` и `style_index.css` с внесёнными изменениями согласно моим рекомендациям:

### Изменённый файл `main.php`:
```php
<?php
require_once("DB_connect.php");
require_once("functions.php");
$page = "main";
$PageInfo = get_Page($page);
//print_r($PageInfo);
?>
<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" type="text/css" href="/style_index.css">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<title><?=$PageInfo['page_title']?></title>
<meta name="description" content="<?=$PageInfo['page_description']?>">
<meta name="keywords" content="<?=$PageInfo['page_keywords']?>">
</head>
<body>
<?php include(__DIR__ . "/header.inc"); ?>
<?php include(__DIR__ . "/nav.inc"); ?>
<div class="content">

  <!-- Блок ссылок, перемещающийся вместе с содержимым -->
  <div class="fixed-links">
    <a href="/main.php"><i class="material-icons">home</i></a>
    <a href="/articles.php"><i class="material-icons">article</i></a>
    <a href="/products.php"><i class="material-icons">inventory</i></a>
    <a href="/photos.php"><i class="material-icons">photo</i></a>
    <a href="/index.php"><i class="material-icons">person_add</i></a>
    <a target="_blank" href="/admin.php"><i class="material-icons">admin_panel_settings</i></a>
  </div>

  <p><?=$PageInfo['page_content']?></p>

</div>
<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav ul');

  // Открытие/закрытие меню при клике на иконку "гамбургер"
  hamburger.addEventListener('click', () => {
    navMenu.classList.toggle('active');
    console.log('Класс active применён:', navMenu.classList.contains('active'));
  });

  // Закрытие меню при клике в любом месте страницы
  document.addEventListener('click', (event) => {
    if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
      navMenu.classList.remove('active');
      console.log('Класс active удалён: меню закрыто');
    }
  });
});
</script>

</body>
</html>
```

### Изменённый файл `style_index.css`:
```css
/* Базовые стили */
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Установленный шрифт */
  margin: 0;
  padding: 0;
  background-color: #F8F9FA; /* Светлый фон страницы */
  color: #212529; /* Нейтральный цвет текста */
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem;
  background-color: #E9ECEF; /* Светло-серый фон заголовка */
  color: #212529;
  text-align: center; /* Центрирует текст в мобильной версии */
  border-bottom: 1px solid #DEE2E6;
  position: relative;
}
/* Стили для кнопки-гамбургера */
.hamburger {
  display: flex; /* Убедиться, что кнопка всегда видна */
  cursor: pointer;
  flex-direction: column;
  gap: 0.4rem;
  position: absolute;
  right: 1rem;
  top: 50%;
  transform: translateY(-50%);
  z-index: 10;
}

.hamburger span {
  display: block;
  width: 25px;
  height: 3px;
  background-color: #212529; /* Темный цвет полосок */
  border-radius: 2px;
}

.nav ul {
  list-style-type: none;
  padding: 0;
  margin: 0;
  display: flex;
  position: fixed;
  left: 0;
  transform: translateX(-100%);
  transition: transform 0.3s ease;
  top: 0;
  height: 100%;
  width: 250px;
  background-color: #E9ECEF; /* Светло-серый фон шторки */
  flex-direction: column;
  gap: 1rem;
  padding-top: 2rem;
  z-index: 5;
}

.nav ul.active {
  transform: translateX(0);
}

.nav a {
  color: #212529; /* Темно-серый текст ссылок */
  text-decoration: none;
  padding: 0.5rem 1rem;
  border-radius: 0.25rem;
  transition: background-color 0.3s ease;
  text-align: left; /* Выравнивание текста по левому краю */
  display: block; /* Обеспечивает корректное выравнивание */
}

.nav a:hover {
  background-color: #F1F1F1; /* Светло-серый фон при наведении */
}

.content {
  background-color: #FFFFFF;
  padding: 2rem;
  margin: 1rem auto;
  max-width: 800px;
  border-radius: 0.5rem;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.content p {
  font-size: 1rem;
  color: #495057; /* Темно-серый текст */
}

.footer {
  background-color: #E9ECEF;
  padding: 1rem;
  text-align: center;
  border-top: 1px solid #DEE2E6;
  color: #6C757D;
  font-size: 0.875rem;
}

@media (max-width: 768px) {
  .nav ul {
    display: none; /* Скрыть меню по умолчанию */
    flex-direction: column;
    position: absolute;
    top: 60px;
    left: 0;
    background-color: #E9ECEF; /* Светло-серый фон для мобильной версии */
    width: 100%;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    padding: 1rem;
  }

  .nav ul.active {
    display: flex;
  }

  .hamburger {
    display: flex; /* Показывать кнопку-гамбургер */
  }
}

/* Стили для элементов регистрации */
.form-group {
  margin-bottom: 1rem; /* Отступ между полями ввода */
  display: flex;
  flex-direction: column; /* Расположить подписи и поля ввода вертикально */
}

.form-group label {
  margin-bottom: 0.5rem; /* Отступ между подписью и полем ввода */
  font-weight: bold; /* Полужирный шрифт для подписей */
  color: #495057; /* Темно-серый цвет */
}

.form-group input {
  width: 100%; /* Поле занимает всю ширину контейнера */
  padding: 0.5rem; /* Внутренний отступ для удобного ввода текста */
  border: 1px solid #DEE2E6; /* Светло-серая рамка */
  border-radius: 4px; /* Слегка скругленные углы */
  font-size: 1rem; /* Удобный размер текста */
  color: #495057; /* Темно-серый цвет текста */
}

.form-group input:focus {
  border-color: #007BFF; /* Синий цвет рамки при фокусе */
  outline: none; /* Убрать стандартное выделение */
  box-shadow: 0 0 5px rgba(0, 123, 255, 0.5); /* Светящийся эффект */
}

.submit-btn {
  display: block; /* Блоковая кнопка занимает всю ширину родителя */
  width: 100%; /* Кнопка по ширине равна контейнеру */
  padding: 0.75rem; /* Внутренний отступ */
  background-color: #007BFF; /* Синий фон */
  border: none; /* Без рамки */
  border-radius: 4px; /* Скругленные углы */
  font-size: 1rem; /* Размер текста */
  color: #fff; /* Белый текст */
  cursor: pointer; /* Курсор в виде руки */
  text-align: center; /* Выравнивание текста внутри кнопки */
  transition: background-color 0.3s ease; /* Плавный переход цвета фона */
}

.submit-btn:hover {
  background-color: #0056b3; /* Темно-синий фон при наведении */
}

i.material-icons {
    font-size: 34px;
    color: #333;
    margin-right: 8px;
}

/* Стили для блока ссылок */
.fixed-links {
  position: fixed; /* Фиксирует блок относительно окна браузера */
  top: 50%; /* Устанавливает позицию блока по вертикали (по центру) */
  right: 0; /* Блок находится справа */
  transform: translateY(-50%); /* Центрирование блока по вертикали */
  background-color: #E9ECEF; /* Фон блока */
  padding: 1rem; /* Внутренние отступы */
  border-radius: 0.5rem 0 0 0.5rem; /* Скругление углов */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Тень */
}

.fixed-links a {
  display: block; /* Каждый элемент ссылки занимает всю ширину */
  margin: 0.5rem 0; /* Отступы между ссылками */
  color: #212529; /* Цвет ссылок */
  text-decoration: none; /* Убирает подчеркивание ссылок */
  transition: background-color 0.3s ease; /* Плавный эффект изменения фона */
}

.fixed-links a:hover {
  background-color: #F1F1F1; /* Цвет фона при наведении */
}
```

С такими изменениями блок ссылок будет двигаться вместе с содержимым страницы, так как его позиция теперь относительно контейнера с классом `content`. 



## Меню под иконкой "гамбургера"

Чтобы сделать меню открывающимся под иконкой "гамбургера" и ограничить размер шторки содержимым, нужно изменить CSS и JavaScript следующим образом:

### Изменённый CSS:
```css
.nav ul {
  position: absolute; /* Располагает меню относительно родительского элемента */
  top: 100%; /* Меню появляется прямо под иконкой "гамбургера" */
  left: 0; /* Выравнивание по левому краю родителя */
  display: none; /* Скрывает меню по умолчанию */
  background-color: #fff; /* Задает фоновый цвет */
  border: 1px solid #ccc; /* Необязательно: добавляет рамку */
  padding: 0; /* Убирает ненужные отступы */
  list-style: none; /* Убирает маркеры списка */
  box-shadow: 0 4px 6px rgba(0,0,0,0.1); /* Необязательно: добавляет тень */
}

.nav ul.active {
  display: block; /* Показывает меню, когда активировано */
  width: auto; /* Устанавливает ширину в пределах содержимого */
}

.nav ul li {
  padding: 10px; /* Отступы для каждого пункта меню */
}
```

### Изменённый JavaScript:
```javascript
document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav ul');

  // Открытие/закрытие меню при клике на иконку "гамбургер"
  hamburger.addEventListener('click', (event) => {
    event.stopPropagation(); // Предотвращает распространение клика
    navMenu.classList.toggle('active'); // Переключает видимость меню
    console.log('Класс active применён:', navMenu.classList.contains('active'));
  });

  // Закрытие меню при клике вне области меню
  document.addEventListener('click', (event) => {
    if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
      navMenu.classList.remove('active');
      console.log('Класс active удалён: меню закрыто');
    }
  });
});
```

Эти изменения позволят отображать меню под иконкой "гамбургер" с корректным поведением, и размер шторки будет ограничен содержимым. 



### Скругленные углы и выпадающее меню чуть ниже кнопки

Вот обновлённый файл `style_index.css` с учётом ваших требований, включая скругленные углы и расположение выпадающего меню чуть ниже кнопки "гамбургер":

```css
/* Базовые стили */
body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; /* Установленный шрифт */
  margin: 0;
  padding: 0;
  background-color: #F8F9FA; /* Светлый фон страницы */
  color: #212529; /* Нейтральный цвет текста */
}

.header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem;
  background-color: #E9ECEF; /* Светло-серый фон заголовка */
  color: #212529;
  text-align: center; /* Центрирует текст в мобильной версии */
  border-bottom: 1px solid #DEE2E6;
  position: relative;
  border-radius: 0.5rem; /* Скругленные углы заголовка */
}

/* Стили для кнопки-гамбургера */
.hamburger {
  display: flex; /* Убедиться, что кнопка всегда видна */
  cursor: pointer;
  flex-direction: column;
  gap: 0.4rem;
  position: absolute;
  right: 1rem; /* Расположить кнопку справа */
  top: 50%;
  transform: translateY(-50%);
  z-index: 10;
}

.hamburger span {
  display: block;
  width: 25px;
  height: 3px;
  background-color: #212529; /* Темный цвет полосок */
  border-radius: 2px; /* Скругленные углы полосок */
}

/* Стили для выпадающего меню */
.nav ul {
  list-style-type: none;
  padding: 0;
  margin: 0;
  display: none; /* Меню скрыто по умолчанию */
  position: absolute; /* Относительное положение */
  top: calc(100% + 2px); /* Меню будет на 2px ниже кнопки "гамбургер" */
  right: 0; /* Выравнивание по правому краю */
  background-color: #E9ECEF; /* Светло-серый фон меню */
  border: 1px solid #DEE2E6; /* Светлая рамка */
  border-radius: 0.5rem; /* Скругленные углы меню */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Легкая тень */
  padding: 0.5rem; /* Внутренние отступы */
  z-index: 5;
  width: auto; /* Ширина по содержимому */
}

.nav ul.active {
  display: block; /* Показывать меню, когда оно активно */
}

.nav ul li {
  padding: 0.5rem 1rem; /* Внутренние отступы элементов */
}

.nav ul li a {
  text-align: left; /* Выравнивание текста по левому краю */
  text-decoration: none; /* Убрать подчеркивание текста */
  color: #212529; /* Темно-серый текст */
  border-radius: 0.25rem; /* Скругленные углы ссылок в меню */
}

.nav ul li a:hover {
  background-color: #F1F1F1; /* Светлый фон при наведении */
}

.nav a {
  color: #212529; /* Темно-серый текст ссылок */
  text-decoration: none;
  padding: 0.5rem 1rem;
  border-radius: 0.5rem; /* Скругленные углы ссылок */
  transition: background-color 0.3s ease;
  text-align: left; /* Выравнивание текста по левому краю */
  display: block; /* Обеспечивает корректное выравнивание */
}

.nav a:hover {
  background-color: #F1F1F1; /* Светло-серый фон при наведении */
}

.content {
  background-color: #FFFFFF;
  padding: 2rem;
  margin: 1rem auto;
  max-width: 1000px;
  border-radius: 0.5rem; /* Скругленные углы содержимого */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

.content p {
  font-size: 1rem;
  color: #495057; /* Темно-серый текст */
}

.footer {
  background-color: #E9ECEF;
  padding: 1em;
  text-align: center;
  border-top: 1px solid #DEE2E6;
  color: #6C757D;
  font-size: 1rem;
  margin: 1rem auto;
  max-width: 1070px; /* Ограничивает максимальную ширину содержимого */
  border-radius: 0.5rem; /* Скругленные углы */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  line-height: 1.5;
  box-sizing: border-box;
}

@media (max-width: 768px) {
  .nav ul {
    display: none; /* Скрыть меню по умолчанию */
    flex-direction: column;
    position: absolute;
    top: calc(100% + 2px); /* Меню будет ниже кнопки */
    right: 0; /* Меню выдвигается справа */
    background-color: #E9ECEF; /* Светло-серый фон для мобильной версии */
    width: 100%;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    padding: 1rem;
    border-radius: 0.5rem; /* Скругленные углы мобильного меню */
  }

  .nav ul.active {
    display: flex;
  }

  .hamburger {
    display: flex; /* Показывать кнопку-гамбургер */
  }
}

/* Стили для элементов регистрации */
.form-group {
  margin-bottom: 1rem; /* Отступ между полями ввода */
  display: flex;
  flex-direction: column; /* Расположить подписи и поля ввода вертикально */
}

.form-group label {
  margin-bottom: 0.5rem; /* Отступ между подписью и полем ввода */
  font-weight: bold; /* Полужирный шрифт для подписей */
  color: #495057; /* Темно-серый цвет */
}

.form-group input {
  width: 100%; /* Поле занимает всю ширину контейнера */
  padding: 0.5rem; /* Внутренний отступ для удобного ввода текста */
  border: 1px solid #DEE2E6; /* Светло-серая рамка */
  border-radius: 0.5rem; /* Слегка скругленные углы */
  font-size: 1rem; /* Удобный размер текста */
  color: #495057; /* Темно-серый цвет текста */
}

.form-group input:focus {
  border-color: #007BFF; /* Синий цвет рамки при фокусе */
  outline: none; /* Убрать стандартное выделение */
  box-shadow: 0 0 5px rgba(0, 123, 255, 0.5); /* Светящийся эффект */
}

.submit-btn {
  display: block; /* Блоковая кнопка занимает всю ширину родителя */
  width: 100%; /* Кнопка по ширине равна контейнеру */
  padding: 0.75rem; /* Внутренний отступ */
  background-color: #007BFF; /* Синий фон */
  border: none; /* Без рамки */
  border-radius: 0.5rem; /* Скругленные углы кнопки */
  font-size: 1rem; /* Размер текста */
  color: #fff; /* Белый текст */
  cursor: pointer; /* Курсор в виде руки */
  text-align: center; /* Выравнивание текста внутри кнопки */
  transition: background-color 0.3s ease; /* Плавный переход цвета фона */
}

.submit-btn:hover {
  background-color: #0056b3; /* Темно-синий фон при наведении */
}

i.material-icons {
  font-size: 34px;
  color: #333;
  margin-right: 8px;
}

/* Стили для блока ссылок */
.fixed-links {
  position: fixed; /* Фиксирует блок относительно окна браузера */
  top: 50%; /* Устанавливает позицию блока по вертикали (по центру) */
  left: 0; /* Блок находится слева */
  transform: translateY(-50%); /* Центрирование блока по вертикали */
  background-color: #E9ECEF; /* Фон блока */
  padding: 1rem; /* Внутренние отступы */
  border-radius: 0.5rem; /* Скругленные углы блока */
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1); /* Тень */
}

.fixed-links a {
  display
```

## Изменённый файл main.php

```html
<?php
require_once("DB_connect.php");
require_once("functions.php");
$page = "main";
$PageInfo = get_Page($page);
//print_r($PageInfo);
?>
<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" type="text/css" href="/style_index.css">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<title><?=$PageInfo['page_title']?></title>
<meta name="description" content="<?=$PageInfo['page_description']?>">
<meta name="keywords" content="<?=$PageInfo['page_keywords']?>">
</head>
<body>
<?php include(__DIR__ . "/header.inc"); ?>

<div class="content">
<?php include(__DIR__ . "/nav.inc"); ?>

<p><?=$PageInfo['page_content']?></p>

</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav ul');

  // Открытие/закрытие меню при клике на иконку "гамбургер"
  hamburger.addEventListener('click', (event) => {
    event.stopPropagation(); // Предотвращает распространение клика
    navMenu.classList.toggle('active'); // Переключает видимость меню
    console.log('Класс active применён:', navMenu.classList.contains('active'));
  });

  // Закрытие меню при клике вне области меню
  document.addEventListener('click', (event) => {
    if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
      navMenu.classList.remove('active');
      console.log('Класс active удалён: меню закрыто');
    }
  });
});

</script>

</body>
</html>
```



## Обновлённый файл `articles.php`

Вот обновлённый файл `articles.php`, приведённый в соответствие с кодом из `main.php`:

```php
<?php
require_once("DB_connect.php");
require_once("functions.php");

$page = "news";
$PageInfo = get_Page($page);
$ArticleInfo = get_Articles(); // Не забываем инициализировать массив
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <link rel="stylesheet" type="text/css" href="/style_index.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title><?= htmlspecialchars($PageInfo['page_title'] ?? 'Новости'); ?></title>
    <meta name="description" content="<?= htmlspecialchars($PageInfo['page_description'] ?? 'Описание отсутствует'); ?>">
    <meta name="keywords" content="<?= htmlspecialchars($PageInfo['page_keywords'] ?? 'новости'); ?>">
</head>
<body>
    <?php include(__DIR__ . "/header.inc"); ?>

    <div class="content">
        <?php include(__DIR__ . "/nav.inc"); ?>

        <h1><?= htmlspecialchars($PageInfo['page_header'] ?? 'Новости'); ?></h1>
        <div class="ArticleList">
            <?php if (!empty($ArticleInfo)): ?>
                <?php foreach ($ArticleInfo as $article): ?>
                    <h2><?= htmlspecialchars($article['article_title'] ?? 'Без заголовка'); ?></h2>
                    <p><em><?= htmlspecialchars($article['article_date'] ?? 'Дата не указана'); ?></em></p>
                    <p><?= htmlspecialchars($article['article_content'] ?? 'Содержание отсутствует'); ?></p>
                <?php endforeach; ?>
            <?php else: ?>
                <p>Нет доступных статей.</p>
            <?php endif; ?>
        </div>
    </div>

    <div class="footer">
        <?php include(__DIR__ . "/footer.inc"); ?>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const hamburger = document.querySelector('.hamburger');
            const navMenu = document.querySelector('.nav ul');

            hamburger.addEventListener('click', (event) => {
                event.stopPropagation();
                navMenu.classList.toggle('active');
                console.log('Класс active применён:', navMenu.classList.contains('active'));
            });

            document.addEventListener('click', (event) => {
                if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
                    navMenu.classList.remove('active');
                    console.log('Класс active удалён: меню закрыто');
                }
            });
        });
    </script>
</body>
</html>
```

### Изменения:
1. **Структура**: Код файла стал проще и лучше соответствует `main.php`, сохраняя оригинальные элементы.
2. **Содержимое**: Поддерживается динамическая генерация новостей через `$PageInfo` и `$ArticleInfo`.
3. **Скрипт**: Логика меню на основе "гамбургера" сохранена и усовершенствована для единообразия.

Если нужно что-то ещё доработать или уточнить — дайте знать! 😊

## Исправленный код файла `index.php`

Конечно, вот исправленный и полный код файла `index.php`, который включает как контент из переменной `$PageInfo['page_content']`, так и форму регистрации:

```php
<?php
require_once("DB_connect.php");
require_once("functions.php");
$page = "main";
$PageInfo = get_Page($page);
//print_r($PageInfo);
?>
<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" type="text/css" href="/style_index.css">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<title><?=$PageInfo['page_title']?></title>
<meta name="description" content="<?=$PageInfo['page_description']?>">
<meta name="keywords" content="<?=$PageInfo['page_keywords']?>">
</head>
<body>
<?php include(__DIR__ . "/header.inc"); ?>

<div class="content">
<?php include(__DIR__ . "/nav.inc"); ?>

<p><?=$PageInfo['page_content']?></p>

<h2>Форма регистрации</h2>
<form action="register.php" method="POST">
    <div class="form-group">
      <label for="username">Имя пользователя:</label>
      <input type="text" id="username" name="username" required>
    </div>
    <div class="form-group">
      <label for="email">Электронная почта:</label>
      <input type="email" id="email" name="email" required>
    </div>
    <div class="form-group">
      <label for="password">Пароль:</label>
      <input type="password" id="password" name="password" required>
    </div>
    <button type="submit" class="submit-btn">Зарегистрироваться</button>
</form>
</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav ul');

  // Открытие/закрытие меню при клике на иконку "гамбургер"
  hamburger.addEventListener('click', (event) => {
    event.stopPropagation(); // Предотвращает распространение клика
    navMenu.classList.toggle('active'); // Переключает видимость меню
    console.log('Класс active применён:', navMenu.classList.contains('active'));
  });

  // Закрытие меню при клике вне области меню
  document.addEventListener('click', (event) => {
    if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
      navMenu.classList.remove('active');
      console.log('Класс active удалён: меню закрыто');
    }
  });
});
</script>

</body>
</html>
```

Теперь файл содержит и динамический контент страницы (`$PageInfo['page_content']`), и форму регистрации. 



## Обновленный код файла `products.php`

Вот обновленный код файла `products.php`, оформленный в том же стиле, что и файл `main.php`:

```php
<?php
require_once("DB_connect.php");
require_once("functions.php");
$page = "beer";
$product_type = "beer";
$PageInfo = get_Page($page);
$ProductInfo = get_Products($product_type);
?>
<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" type="text/css" href="/style_index.css">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<title><?=$PageInfo['page_title']?></title>
<meta name="description" content="<?=$PageInfo['page_description']?>">
<meta name="keywords" content="<?=$PageInfo['page_keywords']?>">
</head>
<body>
<?php include(__DIR__ . "/header.inc"); ?>

<div class="content">
<?php include(__DIR__ . "/nav.inc"); ?>

<p><?=$PageInfo['page_content']?></p>

<h2>Список товаров</h2>
<table>
    <tr>
        <th>Название</th>
        <th>Описание</th>
    </tr>
    <?php if (!empty($ProductInfo)): ?>
        <!-- Вывод данных о продуктах -->
        <?php foreach ($ProductInfo as $product): ?>
        <tr>
            <td><?= htmlspecialchars($product['product_name'], ENT_QUOTES, 'UTF-8'); ?></td>
            <td><?= htmlspecialchars($product['product_desc'], ENT_QUOTES, 'UTF-8'); ?></td>
        </tr>
        <?php endforeach; ?>
    <?php else: ?>
        <!-- Сообщение, если список продуктов пуст -->
        <tr>
            <td colspan="2">Товары отсутствуют.</td>
        </tr>
    <?php endif; ?>
</table>

</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav ul');

  // Открытие/закрытие меню при клике на иконку "гамбургер"
  hamburger.addEventListener('click', (event) => {
    event.stopPropagation(); // Предотвращает распространение клика
    navMenu.classList.toggle('active'); // Переключает видимость меню
    console.log('Класс active применён:', navMenu.classList.contains('active'));
  });

  // Закрытие меню при клике вне области меню
  document.addEventListener('click', (event) => {
    if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
      navMenu.classList.remove('active');
      console.log('Класс active удалён: меню закрыто');
    }
  });
});
</script>

</body>
</html>
```

Теперь дизайн файла `products.php` полностью соответствует стилю `main.php`. 



## Адаптированный код файла `photos.php`

Вот обновленный и адаптированный код файла `photos.php`, который соответствует стилю файла `main.php`, с таблицей для отображения фотографий:

```php
<?php
require_once("DB_connect.php");
require_once("functions.php");
$page = "gallery";
$PageInfo = get_Page($page);
$PhotoInfo = get_Photo();
?>
<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" type="text/css" href="/style_index.css">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<title><?=$PageInfo['page_title']?></title>
<meta name="description" content="<?=$PageInfo['page_description']?>">
<meta name="keywords" content="<?=$PageInfo['page_keywords']?>">
</head>
<body>
<?php include(__DIR__ . "/header.inc"); ?>

<div class="content">
<?php include(__DIR__ . "/nav.inc"); ?>

<p><?=$PageInfo['page_content']?></p>

<h2>Галерея</h2>
<table align="center">
    <?php if (!empty($PhotoInfo)): ?>
        <?php foreach ($PhotoInfo as $photo): ?>
            <tr>
                <td>
                    <?php if (!empty($photo['photo_name'])): ?>
                        <img src="<?= htmlspecialchars($photo['photo_name'], ENT_QUOTES, 'UTF-8'); ?>" alt="Фото">
                    <?php else: ?>
                        <p>Изображение отсутствует.</p>
                    <?php endif; ?>
                </td>
            </tr>
            <tr>
                <td><?= htmlspecialchars($photo['photo_caption'] ?? 'Без подписи', ENT_QUOTES, 'UTF-8'); ?></td>
            </tr>
        <?php endforeach; ?>
    <?php else: ?>
        <tr>
            <td>Фотографии отсутствуют.</td>
        </tr>
    <?php endif; ?>
</table>

</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav ul');

  // Открытие/закрытие меню при клике на иконку "гамбургер"
  hamburger.addEventListener('click', (event) => {
    event.stopPropagation(); // Предотвращает распространение клика
    navMenu.classList.toggle('active'); // Переключает видимость меню
    console.log('Класс active применён:', navMenu.classList.contains('active'));
  });

  // Закрытие меню при клике вне области меню
  document.addEventListener('click', (event) => {
    if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
      navMenu.classList.remove('active');
      console.log('Класс active удалён: меню закрыто');
    }
  });
});
</script>

</body>
</html>
```

Теперь дизайн файла `photos.php` полностью соответствует стилю `main.php`, и в нем реализована таблица для отображения фотографий, как требуется. 



## Обновленный код файла `login.php`

Вот обновленный код файла `login.php`, который оформлен в стиле файла `main.php` и дополнен формой регистрации:

```php
<?php
session_start();
require_once("DB_connect.php");

// Инициализация сообщения об ошибке
$error_message = '';

// Проверка, отправлена ли форма
if ($_SERVER['REQUEST_METHOD'] === 'POST') {
    $username = htmlspecialchars(trim($_POST['username'] ?? ''));
    $password = htmlspecialchars(trim($_POST['password'] ?? ''));

    // Проверка заполненности полей
    if (!empty($username) && !empty($password)) {
        try {
            // Запрос для проверки логина
            $query = "SELECT user_id, password FROM users WHERE username = :username";
            $stmt = $link->prepare($query);
            $stmt->bindParam(':username', $username, PDO::PARAM_STR);
            $stmt->execute();

            $user = $stmt->fetch(PDO::FETCH_ASSOC);

            if ($user && password_verify($password, $user['password'])) {
                // Авторизация успешна
                $_SESSION['logged_in'] = true;
                $_SESSION['user_id'] = $user['user_id'];
                $_SESSION['username'] = $username;

                // Перенаправление на страницу admin.php
                header('Location: admin.php');
                exit();
            } else {
                $error_message = 'Неправильный логин или пароль.';
            }
        } catch (PDOException $e) {
            $error_message = "Ошибка базы данных: " . htmlspecialchars($e->getMessage());
        }
    } else {
        $error_message = 'Пожалуйста, заполните все поля.';
    }
}
?>
<!DOCTYPE html>
<html>
<head>
<link rel="stylesheet" type="text/css" href="/style_index.css">
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
<title>Вход</title>
</head>
<body>
<?php include(__DIR__ . "/header.inc"); ?>

<div class="content">
<?php include(__DIR__ . "/nav.inc"); ?>

<h2>Вход</h2>
<?php if (!empty($error_message)): ?>
    <p style="color: red;"><?= htmlspecialchars($error_message, ENT_QUOTES, 'UTF-8'); ?></p>
<?php endif; ?>
<form method="POST" action="login.php">
    <div class="form-group">
        <label for="username">Логин:</label><br>
        <input type="text" id="username" name="username" required><br><br>
    </div>
    <div class="form-group">
        <label for="password">Пароль:</label><br>
        <input type="password" id="password" name="password" required><br><br>
    </div>
    <button type="submit" class="submit-btn">Войти</button>
</form>
<p>
    <a href="forgot_password.php" style="text-decoration: none; color: #007BFF;">Забыли пароль?</a>
</p>

<h2>Регистрация</h2>
<form action="register.php" method="POST">
    <div class="form-group">
        <label for="reg-username">Имя пользователя:</label><br>
        <input type="text" id="reg-username" name="username" required><br><br>
    </div>
    <div class="form-group">
        <label for="reg-email">Электронная почта:</label><br>
        <input type="email" id="reg-email" name="email" required><br><br>
    </div>
    <div class="form-group">
        <label for="reg-password">Пароль:</label><br>
        <input type="password" id="reg-password" name="password" required><br><br>
    </div>
    <button type="submit" class="submit-btn">Зарегистрироваться</button>
</form>

</div>

<div class="footer">
<?php include(__DIR__ . "/footer.inc"); ?>
</div>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const hamburger = document.querySelector('.hamburger');
  const navMenu = document.querySelector('.nav ul');

  // Открытие/закрытие меню при клике на иконку "гамбургер"
  hamburger.addEventListener('click', (event) => {
    event.stopPropagation(); // Предотвращает распространение клика
    navMenu.classList.toggle('active'); // Переключает видимость меню
    console.log('Класс active применён:', navMenu.classList.contains('active'));
  });

  // Закрытие меню при клике вне области меню
  document.addEventListener('click', (event) => {
    if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
      navMenu.classList.remove('active');
      console.log('Класс active удалён: меню закрыто');
    }
  });
});
</script>

</body>
</html>
```

Этот код добавляет форму регистрации к форме входа, при этом оформлен в том же стиле, что и файл `main.php`. 



# Работа над ошибками

Вот обновлённый файл `articles.php`, приведённый в соответствие с кодом из `main.php`:

```php
<?php
require_once("DB_connect.php");
require_once("functions.php");

$page = "news";
$PageInfo = get_Page($page);
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <link rel="stylesheet" type="text/css" href="/style_index.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title><?= htmlspecialchars($PageInfo['page_title'] ?? 'Новости'); ?></title>
    <meta name="description" content="<?= htmlspecialchars($PageInfo['page_description'] ?? 'Описание отсутствует'); ?>">
    <meta name="keywords" content="<?= htmlspecialchars($PageInfo['page_keywords'] ?? 'новости'); ?>">
</head>
<body>
    <?php include(__DIR__ . "/header.inc"); ?>

    <div class="content">
        <?php include(__DIR__ . "/nav.inc"); ?>

        <h1><?= htmlspecialchars($PageInfo['page_header'] ?? 'Новости'); ?></h1>
        <div class="ArticleList">
            <?php if (!empty($ArticleInfo)): ?>
                <?php foreach ($ArticleInfo as $article): ?>
                    <h2><?= htmlspecialchars($article['article_title'] ?? 'Без заголовка'); ?></h2>
                    <p><em><?= htmlspecialchars($article['article_date'] ?? 'Дата не указана'); ?></em></p>
                    <p><?= htmlspecialchars($article['article_content'] ?? 'Содержание отсутствует'); ?></p>
                <?php endforeach; ?>
            <?php else: ?>
                <p>Нет доступных статей.</p>
            <?php endif; ?>
        </div>
    </div>

    <div class="footer">
        <?php include(__DIR__ . "/footer.inc"); ?>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const hamburger = document.querySelector('.hamburger');
            const navMenu = document.querySelector('.nav ul');

            // Открытие/закрытие меню при клике на иконку "гамбургер"
            hamburger.addEventListener('click', (event) => {
                event.stopPropagation(); // Предотвращение распространения клика
                navMenu.classList.toggle('active'); // Переключение видимости меню
                console.log('Класс active применён:', navMenu.classList.contains('active'));
            });

            // Закрытие меню при клике вне области меню
            document.addEventListener('click', (event) => {
                if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
                    navMenu.classList.remove('active');
                    console.log('Класс active удалён: меню закрыто');
                }
            });
        });
    </script>
</body>
</html>
```

Изменения:

1. **Структура**: Код файла стал проще и лучше соответствует `main.php`, сохраняя оригинальные элементы.
2. **Содержимое**: Поддерживается динамическая генерация новостей через `$PageInfo` и `$ArticleInfo`.
3. **Скрипт**: Логика меню на основе "гамбургера" сохранена и усовершенствована для единообразия.



> это код пишет **Нет доступных статей**, но если ставлю старый код тогда статьи есть, проверь где ошибка

Давайте разберемся. Судя по описанию, проблема может быть связана с тем, что массив `$ArticleInfo` пустой при выводе данных. В старом коде, вероятно, что-то иначе влияет на заполнение `$ArticleInfo`. Вот несколько шагов, чтобы диагностировать и исправить ошибку:

### 1. **Проверьте, что `$ArticleInfo` инициализируется корректно**
В новом коде `$ArticleInfo` вообще не объявлен. Обязательно нужно добавить его и инициализировать, как в старом коде:
```php
$ArticleInfo = get_Articles();
```
Если этого не сделать, массив `$ArticleInfo` будет пустым, и выводится "Нет доступных статей".

Вставьте эту строку перед началом `<!DOCTYPE html>`, как это сделано в старом коде.

---

### 2. **Проверьте функцию `get_Articles()`**
Убедитесь, что:
- Функция `get_Articles()` корректно возвращает данные из базы данных.
- Вызов функции работает так же, как и в старом коде.

Попробуйте временно добавить отладочный вывод, чтобы проверить содержимое массива `$ArticleInfo`:
```php
echo '<pre>';
print_r($ArticleInfo);
echo '</pre>';
```
Поставьте этот код сразу после строки `$ArticleInfo = get_Articles();`. Это поможет увидеть, какие данные возвращаются и убедиться, что массив не пустой.

---

### 3. **Проверьте базу данных**
Убедитесь, что данные статей в базе данных действительно существуют и доступны для выборки. Возможно, логика выборки отличается, и страница `news` использует другой механизм.

---

### 4. **Исправленный код**
Вот как должен выглядеть ваш код с исправлением:
```php
<?php
require_once("DB_connect.php");
require_once("functions.php");

$page = "news";
$PageInfo = get_Page($page);
$ArticleInfo = get_Articles(); // Не забываем инициализировать массив
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <link rel="stylesheet" type="text/css" href="/style_index.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title><?= htmlspecialchars($PageInfo['page_title'] ?? 'Новости'); ?></title>
    <meta name="description" content="<?= htmlspecialchars($PageInfo['page_description'] ?? 'Описание отсутствует'); ?>">
    <meta name="keywords" content="<?= htmlspecialchars($PageInfo['page_keywords'] ?? 'новости'); ?>">
</head>
<body>
    <?php include(__DIR__ . "/header.inc"); ?>

    <div class="content">
        <?php include(__DIR__ . "/nav.inc"); ?>

        <h1><?= htmlspecialchars($PageInfo['page_header'] ?? 'Новости'); ?></h1>
        <div class="ArticleList">
            <?php if (!empty($ArticleInfo)): ?>
                <?php foreach ($ArticleInfo as $article): ?>
                    <h2><?= htmlspecialchars($article['article_title'] ?? 'Без заголовка'); ?></h2>
                    <p><em><?= htmlspecialchars($article['article_date'] ?? 'Дата не указана'); ?></em></p>
                    <p><?= htmlspecialchars($article['article_content'] ?? 'Содержание отсутствует'); ?></p>
                <?php endforeach; ?>
            <?php else: ?>
                <p>Нет доступных статей.</p>
            <?php endif; ?>
        </div>
    </div>

    <div class="footer">
        <?php include(__DIR__ . "/footer.inc"); ?>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const hamburger = document.querySelector('.hamburger');
            const navMenu = document.querySelector('.nav ul');

            hamburger.addEventListener('click', (event) => {
                event.stopPropagation();
                navMenu.classList.toggle('active');
                console.log('Класс active применён:', navMenu.classList.contains('active'));
            });

            document.addEventListener('click', (event) => {
                if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
                    navMenu.classList.remove('active');
                    console.log('Класс active удалён: меню закрыто');
                }
            });
        });
    </script>
</body>
</html>
```

---

### 5. **Резюме**
- Добавьте инициализацию `$ArticleInfo = get_Articles();` в код.
- Проверьте, что функция `get_Articles()` возвращает правильные данные.
- Если массив пустой, используйте отладочный вывод (`print_r`) для диагностики.



Ошибка может быть связана с тем, что переменная `$ArticleInfo` либо неправильно инициализируется, либо не передаёт данные из функции `get_Articles()` в новом коде. Давайте разберём это шаг за шагом:

### Возможные причины ошибки:
1. **Переменная `$ArticleInfo` не инициализирована**:
   - В старом коде вы явно инициализируете `$ArticleInfo` с помощью функции `get_Articles()`.
   - Если эта строка отсутствует в новом коде, то массив будет пустым, и выводится сообщение "Нет доступных статей".
   - Убедитесь, что строка `$ArticleInfo = get_Articles();` присутствует.

2. **Функция `get_Articles()` не возвращает данные**:
   - Проверьте реализацию функции `get_Articles()`. Возможно, она содержит условие или фильтр, из-за которого данные не выбираются.
   - Протестируйте функцию отдельно, временно добавив вывод:
     ```php
     $ArticleInfo = get_Articles();
     echo '<pre>';
     print_r($ArticleInfo);
     echo '</pre>';
     ```

3. **База данных**:
   - Если старый код работает, а новый нет, то проверьте, совпадают ли параметры запроса к базе данных в обоих случаях.
   - Убедитесь, что `$page = "news";` правильно связывается с данными статей.

---

### Исправленный шаг:
Добавьте строку для инициализации `$ArticleInfo` прямо после получения `$PageInfo`:
```php
$ArticleInfo = get_Articles();
```

---

### Пример кода:
Убедитесь, что файл выглядит следующим образом:
```php
<?php
require_once("DB_connect.php");
require_once("functions.php");

$page = "news";
$PageInfo = get_Page($page);
$ArticleInfo = get_Articles(); // Инициализация статей
?>
<!DOCTYPE html>
<html lang="ru-RU">
<head>
    <link rel="stylesheet" type="text/css" href="/style_index.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <title><?= htmlspecialchars($PageInfo['page_title'] ?? 'Новости'); ?></title>
    <meta name="description" content="<?= htmlspecialchars($PageInfo['page_description'] ?? 'Описание отсутствует'); ?>">
    <meta name="keywords" content="<?= htmlspecialchars($PageInfo['page_keywords'] ?? 'новости'); ?>">
</head>
<body>
    <?php include(__DIR__ . "/header.inc"); ?>

    <div class="content">
        <?php include(__DIR__ . "/nav.inc"); ?>

        <h1><?= htmlspecialchars($PageInfo['page_header'] ?? 'Новости'); ?></h1>
        <div class="ArticleList">
            <?php if (!empty($ArticleInfo)): ?>
                <?php foreach ($ArticleInfo as $article): ?>
                    <h2><?= htmlspecialchars($article['article_title'] ?? 'Без заголовка'); ?></h2>
                    <p><em><?= htmlspecialchars($article['article_date'] ?? 'Дата не указана'); ?></em></p>
                    <p><?= htmlspecialchars($article['article_content'] ?? 'Содержание отсутствует'); ?></p>
                <?php endforeach; ?>
            <?php else: ?>
                <p>Нет доступных статей.</p>
            <?php endif; ?>
        </div>
    </div>

    <div class="footer">
        <?php include(__DIR__ . "/footer.inc"); ?>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const hamburger = document.querySelector('.hamburger');
            const navMenu = document.querySelector('.nav ul');

            hamburger.addEventListener('click', (event) => {
                event.stopPropagation();
                navMenu.classList.toggle('active');
                console.log('Класс active применён:', navMenu.classList.contains('active'));
            });

            document.addEventListener('click', (event) => {
                if (!navMenu.contains(event.target) && !hamburger.contains(event.target)) {
                    navMenu.classList.remove('active');
                    console.log('Класс active удалён: меню закрыто');
                }
            });
        });
    </script>
</body>
</html>
```

---

Резюме:

1. Проверьте, что переменная `$ArticleInfo` правильно инициализирована.
2. Используйте `print_r()` для проверки содержимого `$ArticleInfo`.
3. Убедитесь, что функция `get_Articles()` корректно возвращает данные из базы.

Если проблема всё ещё остаётся, напишите детали: например, код функции `get_Articles()`. Мы разберём её вместе! 😊

# Adminer

Удаление столбца в Adminer — это довольно простой процесс. Следуйте этим шагам:

1. **Войдите в Adminer:**
   Откройте Adminer в браузере и войдите с помощью ваших учетных данных.

2. **Выберите базу данных:**
   В левой панели выберите нужную базу данных, содержащую таблицу с удаляемым столбцом.

3. **Откройте таблицу:**
   Найдите таблицу, в которой нужно удалить столбец, и нажмите на её название.

4. **Перейдите к структуре таблицы:**
   В верхней части интерфейса Adminer выберите вкладку "Изменить таблицу". Здесь вы увидите список всех столбцов таблицы.

5. **Удалите столбец:**
   Найдите столбец, который вы хотите удалить, и нажмите кнопку "Удалить" рядом с ним (обычно это крестик или аналогичный значок).

6. **Подтвердите действие:**
   Adminer может попросить подтвердить удаление. Убедитесь, что вы выбрали правильный столбец, так как удаление необратимо.

7. **Сохраните изменения:**
   После подтверждения удаления изменений структура таблицы будет обновлена.



Чтобы изменить кодировку и сопоставление (collation) таблицы в MySQL, например с `cp1251_general_ci` на другую, выполните следующие шаги:

### Шаги изменения кодировки таблицы:

1. **Выберите новую кодировку и сопоставление**:
   Если вы хотите поддерживать полный набор Unicode, рекомендуется использовать `utf8mb4` и сопоставление `utf8mb4_general_ci` или `utf8mb4_unicode_ci`.

2. **Выполните SQL-запрос для изменения кодировки**:

   ```sql
   ALTER TABLE `table_name`
   CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
   ```

   Здесь:

   - `table_name` — имя вашей таблицы.
   - `utf8mb4` — новая кодировка.
   - `utf8mb4_general_ci` — новое сопоставление.

3. **Проверьте структуру таблицы**:
   После выполнения команды убедитесь, что изменения были применены, используя:

   ```sql
   SHOW CREATE TABLE `table_name`;
   ```

### Примечание:

- Перед изменением кодировки обязательно сделайте резервную копию таблицы и данных.
- Если ваши данные уже сохранены в `cp1251`, возможно, вам потребуется выполнить преобразование их кодировки перед изменением структуры, чтобы избежать потери данных.

Если вы столкнулись с какими-либо проблемами при выполнении этих шагов, дайте знать — разберемся! 😊



